// BROUILLON DE CLÉS — pack FRANÇAIS. Voir l'en-tête de `families.en.ts`.
//
// ⚠️ Réécriture, pas calque. Et le REGISTRE se lit dans `fr.ts`:
// `/`, `/pro`, `/couples`, `/families`, `/gyms`, `/communities` VOUVOIENT;
// `/meal-prep`, `/coaches` et `/start` TUTOIENT.
//
// Apostrophe typographique U+2019. Espace insécable U+00A0 avant `:` `;` `!`
// `?` `»` `€` `%` et après `«`. JAMAIS U+202F. Pas de `→`.

export const familiesFr = {} as const;
