// Seed anglais — le namespace `brand`, et lui seul.
// Assemblé dans `../en.ts`; une clé `brand.*` ne vit qu'ici (`dictionaryChunks.int.test.ts`).

export const enBrand = {
  // Brand + public chrome (header/footer shared by the public pages)
  "brand.wordmark": "Sophia",
} as const
