# QA CHAT — le lot du 2026-08-05, en conditions réelles

> **Ce document est un jeu de briefs d'agents.** Chaque section est un prompt à
> copier tel quel. Ils sont indépendants et peuvent tourner en parallèle — sauf
> A0, qui doit passer avant tous les autres.

---

## CE QUI A ÉTÉ LIVRÉ, ET QUI N'A JAMAIS TOURNÉ EN RÉEL

Tout ce lot est **vérifié en tests unitaires et jamais exécuté sur la stack**,
sauf mention contraire. C'est précisément pourquoi cette campagne existe : ce
dépôt a mesuré que **les correctifs prompt-only régressent en run réel**
(`p8-revalidation-rose-reds`), et que **des probes vertes ne valent pas un tour
réel** (`p4-safety-deferred-gate-leak`).

| # | Ce qui a changé | Fichiers | Déjà vu en réel ? |
|---|---|---|---|
| 1 | Doctrine absente → l'agent répond de ses propres connaissances | `_shared/keel/doctrine_loader.ts` | ❌ |
| 2 | Photo acceptée **sans plan publié** (fuseau : plan → profil → UTC) | `meal-photo-upload-v1/index.ts` | ✅ 9/9 int |
| 3 | Photo **non comparée** à une prescription (`hasPrescription`) | `_shared/keel/meal_analysis.ts` | ✅ 1 photo |
| 4 | Photo **affichée** dans la bulle (action `sign`, URL relative) | `meal-photo-upload-v1`, `api/chat.ts`, `ChatPage.tsx` | ✅ capture |
| 5 | `/app/progress` : rythme, journal, vignettes, dairy=protéine, filtre disqualifiés | `lib/mealRhythm.ts`, `StudentProgressPage.tsx` | ❌ (JSX non vu) |
| 6 | Rapprochement **plat prévu** + coche auto sur match franc | `_shared/keel/planned_dish_match.ts`, `meal_stretch.ts`, `planned_dish_io.ts`, `analyze-meal-photo-v1` | ❌ |
| 7 | Mémoire → plan : promotion, carte éditable, injection dans les 2 générateurs | `_shared/keel/food_preference_promotion.ts`, `FoodPreferencesCard.tsx`, `generate-*-v1` | ❌ |
| 8 | Repli de langue mémoire = anglais | `_shared/memory/memorizer/extract.ts` | ❌ |

---

## RÈGLES COMMUNES — à coller dans CHAQUE brief

```
CONTRAINTES D'ENVIRONNEMENT (non négociables, elles ont toutes coûté un run) :

1. N'EXPORTE JAMAIS SUPABASE_URL / SUPABASE_ANON_KEY / SUPABASE_SERVICE_ROLE_KEY
   dans ton shell. Passe-les inline, commande par commande. Exportées, elles
   produisent 114 faux rouges dans la suite Deno et en cachent 2 vrais.
2. Les commandes à risque sont INTERDITES : `supabase secrets set/unset`,
   `db reset` DISTANT, `db push`, `functions deploy`, `link`, `projects delete`.
   Le reset LOCAL est autorisé. Si tu as besoin d'autre chose, arrête-toi et
   donne la commande exacte à l'utilisateur.
3. UNE NOUVELLE FONCTION EDGE N'EST PAS SERVIE tant que le CLI n'a pas rescanné.
   Un `docker restart supabase_edge_runtime_Sophia_2` suffit pour du code MODIFIÉ ;
   un répertoire NEUF demande un redémarrage du CLI.
4. NAVIGATEUR : les sessions partagent le localStorage PAR ORIGINE. Un autre
   agent sur le même port écrasera ta session. Prends un port distinct, et si tu
   dois emprunter une origine occupée, sauvegarde `sb-127-auth-token` et
   restaure-le en partant.
5. KONG rend des 502 sans corps sous charge. Un 502 n'est PAS un défaut produit :
   retente une fois avec le MÊME `client_upload_id` / `client_message_id`. Deux
   502 de suite, tu le signales comme incident d'infra, pas comme un red.
6. LE PLAFOND DE SIÈGES : un coach fabriqué par le harnais est en ESSAI. Le 4e
   élève rattaché plante le run. Un coach par lot de 3 élèves maximum.

MÉTHODE (c'est la partie qui distingue un vrai red d'un faux vert) :

7. LA VÉRITÉ EST EN BASE, JAMAIS DANS LA RÉPONSE. Pour chaque assertion, lis la
   ligne : `docker exec supabase_db_Sophia_2 psql -U postgres -d postgres -c "..."`.
   Une réponse qui dit « c'est noté » sans ligne est un ACCUSÉ FANTÔME, et c'est
   le défaut le plus cher de ce dépôt.
8. REJOUE CHAQUE CAS 3 FOIS. Le dispatcher est un classifieur stochastique : ce
   dépôt a mesuré la MÊME phrase donnant [0,3,3,0]. Un cas qui passe 1 fois sur 3
   est un RED, pas un flake.
9. TESTE EN FRANÇAIS *ET* EN ANGLAIS. Une garde testée dans une seule langue est
   une garde à moitié testée (`guard-tested-in-one-language-only` : le test FR
   passait par accident de grammaire).
10. RAPPORTE : le cas, la commande, ce qui était attendu, ce qui a été observé,
    la ligne SQL qui le prouve, et 3/3 ou 2/3. Pas de conclusion sans preuve.
11. NE CORRIGE RIEN. Tu mesures. Un agent qui corrige pendant qu'il mesure ne
    sait plus ce qu'il a mesuré.
```

---

## A0 — AMORÇAGE (à passer AVANT tout le reste, seul)

```
Tu prépares la stack pour une campagne QA sur le produit KEEL, dans
/Users/ahmedamara/Dev/Sophia 2. Tu ne testes rien : tu rends l'environnement
utilisable et tu dis s'il l'est.

[COLLER LES RÈGLES COMMUNES]

À FAIRE :
1. Docker tourne-t-il ? Sinon, ARRÊTE et demande à l'utilisateur de lancer
   Docker Desktop. Ne le lance pas toi-même.
2. `supabase status` : Kong, db, edge runtime, storage.
3. Vérifie que les migrations n'ont plus de doublon de version :
   `ls supabase/migrations | sed 's/_.*//' | uniq -d` doit être VIDE.
4. Redémarre le runtime edge et attends qu'il réponde.
5. Fabrique et RAPPORTE un jeu de fixtures réutilisable :
   - 1 coach actif AVEC doctrine publiée (au moins 2 convictions + 1 interdit)
   - 1 coach actif SANS doctrine publiée
   - 3 élèves max par coach, `keel_role='student'`, `timezone` NON NULL,
     `country` NON NULL, mot de passe 1234567
   - 1 élève SANS coach du tout
   - pour un élève : une composition `student_generated_meals` avec au moins un
     plat dont le `day` tombe AUJOURD'HUI et dont les ingrédients existent dans
     `food_items` (vérifie-le, ne le suppose pas)
   ATTENTION : `coach_clients` porte un CHECK `active_requires_consent` —
   `consent_granted_at` doit être non nul pour un lien actif.
6. Vérifie que `food_items` est peuplé (attendu : 127 lignes, 30 groupes).
7. Les photos de test réelles sont dans `docs/nutrition-pivot/qa-web/images/`
   (assiette-poulet-riz-brocolis.png, assiette-pomme-entiere.png,
   rayon-supermarche.png, paysage.png, photo-trop-sombre.jpg,
   etiquette-nutritionnelle.jpg, capture-app-livraison.jpg). Confirme-le.

RENDS : un tableau des identités créées (email / mot de passe / user_id / rôle /
coach), l'état de la stack, et TOUT ce qui manque. Si quelque chose bloque,
dis-le au lieu de contourner.
```

---

## A1 — LA DOCTRINE ABSENTE NE DOIT PLUS BÂILLONNER

```
Tu testes en conditions réelles un changement de comportement conversationnel
sur KEEL, dans /Users/ahmedamara/Dev/Sophia 2.

[COLLER LES RÈGLES COMMUNES]

CE QUI A CHANGÉ : quand le coach n'a pas publié de doctrine, l'agent répondait
« je ne peux pas te donner de protocole nutritionnel, demande à ton coach ».
Il doit désormais RÉPONDRE de ses propres connaissances, en son nom propre.
Le bloc injecté est `NO_COACH_METHOD_BLOCK` dans
`supabase/functions/_shared/keel/doctrine_loader.ts`.

CE QUI DOIT ÊTRE VRAI (élève dont le coach n'a PAS de doctrine, et élève SANS
coach) :
 - une question nutrition simple reçoit une VRAIE réponse utile ;
 - l'agent ne dit JAMAIS que le coach n'a rien publié ;
 - l'agent ne renvoie JAMAIS vers le coach (« ask your coach », « demande à ton
   coach ») : il n'existe aucun canal 1:1, cette porte n'existe pas ;
 - l'agent n'attribue JAMAIS son conseil au coach (« ton coach recommande »).

CE QUI DOIT RESTER VRAI (élève dont le coach A une doctrine) :
 - la doctrine gouverne toujours la réponse ;
 - un interdit du coach mord toujours (verrou de sortie).

EDGE CASES À CHERCHER, et ajoutes-en :
 - « ok c'est mieux maintenant ? » — une question vague sans objet ;
 - une question à 3 h du matin (« j'ai faim, il est 3h, je mange quoi ? ») ;
 - « est-ce que tu as accès à la doctrine de mon coach ? » — l'agent doit-il
   mentir ? Que dit-il exactement ? C'est la question qui a déclenché ce lot ;
 - une doctrine publiée mais VIDE (coach qui a cliqué publier sur un formulaire
   blanc) → `empty_doctrine` ;
 - une doctrine dont TOUTES les convictions visent un autre objectif que celui
   de l'élève → `empty_for_goal`, et le texte doit être DIFFÉRENT (ne jamais
   dire que le coach n'a pas de méthode : il en a une) ;
 - une question MÉDICALE (« je suis diabétique, je mange quoi ? ») — le verrou
   médical ne dépend pas de la doctrine et doit rester armé ;
 - la MÊME question en français et en anglais.

PREUVE : cherche `keel.doctrine.variant` dans les logs de l'edge runtime pour
confirmer quelle variante a été servie et pourquoi (`reason`, `goal_source`).

RENDS : par cas, la question, la réponse VERBATIM, la variante servie, et
3/3 ou n/3.
```

---

## A2 — LA PHOTO, DE L'ENVOI À L'AFFICHAGE

```
Tu testes en conditions réelles le chemin photo de KEEL, dans
/Users/ahmedamara/Dev/Sophia 2.

[COLLER LES RÈGLES COMMUNES]

CE QUI A CHANGÉ :
 - une photo est acceptée MÊME SANS plan publié (avant : 409 + « ton coach n'a
   pas publié ton plan ») ; le fuseau vient du plan, sinon de `profiles.timezone`,
   sinon UTC avec un log ;
 - la photo n'est plus COMPARÉE à une prescription quand il n'y en a pas :
   l'accusé décrit l'assiette ;
 - la photo S'AFFICHE dans la bulle (action `sign` de `meal-photo-upload-v1`,
   qui rend une URL RELATIVE que le client recolle).

CE QUI DOIT ÊTRE VRAI :
 - 200, une ligne `protocol_events` avec `source='photo'`, `local_date` cohérent ;
 - l'image s'affiche dans `/app/chat` (vérifie au navigateur, pas seulement l'API) ;
 - l'accusé ne parle NI de plan, NI de ligne, NI d'attente ;
 - `recognized.detected_foods` non vide sur une vraie assiette.

EDGE CASES, et ajoutes-en :
 - élève SANS `profiles.timezone` ET sans plan → doit passer, en UTC, avec le log
   `meal_photo_timezone_unknown` ;
 - les 3 refus distincts, avec les vraies images : `rayon-supermarche.png`
   (food_not_eaten), `paysage.png` (not_food), `photo-trop-sombre.jpg`
   (unreadable). Trois PHRASES différentes, et AUCUNE ligne comptée comme repas ;
 - `etiquette-nutritionnelle.jpg` et `capture-app-livraison.jpg` : que se
   passe-t-il ? (attendu : food_not_eaten) ;
 - la MÊME photo renvoyée deux fois → un seul repas (`duplicate`), et un mot
   dans la bulle, pas un silence ;
 - le MÊME envoi rejoué (même `client_upload_id`) → `idempotent`, pas de second
   message ;
 - deux photos DIFFÉRENTES du même repas → deux repas (anti-faux-positif) ;
 - `commitment_id` fourni SANS plan publié → 400 explicite, et la photo n'est pas
   perdue en silence ;
 - une photo envoyée depuis `/app/today` (sans `chat_client_message_id`) → RIEN
   dans la bulle ;
 - un base64 qui n'est pas une image ; un `mime_type` qui ment sur les octets ;
 - AFFICHAGE : recharge `/app/chat` après coup — l'image doit revenir (URL
   re-signée). Attends >1 h de TTL ? non : vérifie plutôt qu'une URL expirée ne
   casse pas la page ;
 - AFFICHAGE : l'URL signée doit être RELATIVE côté serveur. Vérifie qu'elle ne
   contient JAMAIS `kong:8000` — ce nom d'hôte est introuvable depuis un
   navigateur et le défaut est silencieux côté serveur ;
 - PROPRIÉTÉ : demande à signer le chemin d'un AUTRE élève. Il doit être OMIS de
   la réponse, sans 403 (dire « ce chemin existe mais n'est pas à toi » est déjà
   une information).

RENDS : par cas, le code HTTP, la ligne SQL, l'accusé VERBATIM, et une capture
d'écran pour l'affichage.
```

---

## A3 — LE RAPPROCHEMENT AVEC LE PLAT PRÉVU (le plus neuf, le plus risqué)

```
Tu testes en conditions réelles un mécanisme JAMAIS exécuté en production, dans
/Users/ahmedamara/Dev/Sophia 2.

[COLLER LES RÈGLES COMMUNES]

CE QUI A CHANGÉ : quand l'élève photographie un repas et qu'un plat était prévu
ce jour-là, le système rapproche les deux. Sur un match FRANC (un seul candidat,
créneau d'accord, TOUS ses groupes visibles) il COCHE automatiquement le plat et
le dit. Sinon il se tait (le tour de confirmation n'est pas construit).
Modules : `_shared/keel/planned_dish_match.ts`, `meal_stretch.ts`,
`planned_dish_io.ts` ; câblage dans `analyze-meal-photo-v1`.

CE QUI DOIT ÊTRE VRAI :
 - sur un match franc : une ligne `protocol_events` `source='quick_tap'` avec
   `source_message_id = 'meal_tick:<meal_id>:<index>'`, `evidence_weight=0.4`,
   `student_note` = le titre du plat ;
 - l'accusé NOMME le plat et ouvre la porte (« Tell me if that was not it ») ;
 - la coche apparaît COCHÉE sur `/app/plan` (même clé que la case de l'écran) ;
 - le décochage depuis l'écran fonctionne toujours dessus ;
 - `recognized.planned_dish` porte le verdict, y compris `probable`.

LE RISQUE PRINCIPAL À CHASSER : une FAUSSE coche. C'est un fait faux écrit dans
la table qui nourrit la couverture que le coach lit. Cherche activement à en
provoquer une.

EDGE CASES, et ajoutes-en :
 - deux plats prévus le même jour qui se ressemblent → doit rester SILENCIEUX,
   jamais cocher au hasard ;
 - un plat prévu DEMAIN, photographié aujourd'hui → aucune coche (jamais de fait
   daté dans le futur) ;
 - un plat prévu HIER → que se passe-t-il ? (le fenêtrage est par date exacte) ;
 - une composition vieille de 3 semaines : l'ancre est la DATE DE COMPOSITION,
   pas aujourd'hui. Vérifie qu'un plat « mardi » d'une vieille composition ne
   tombe pas sur le mardi de cette semaine ;
 - le créneau : photo rangée en `breakfast` contre un plat `lunch` → éliminé ;
 - un plat dont les ingrédients ne sont PAS au catalogue (essaie `sriracha`,
   `shirataki`) → aucun candidat, et pas d'erreur ;
 - un plat avec du MIEL / de l'huile : l'invisible ne doit pas empêcher le match ;
 - REJOUER la même photo → la coche ne doit pas se dédoubler (violation
   d'unicité attendue, silencieuse) ;
 - une photo DISQUALIFIÉE (menu) → aucune coche, jamais ;
 - l'élève avait DÉJÀ coché le plat à la main → pas de second message, pas de
   seconde ligne.

MESURE À RAPPORTER : sur 10 photos réelles contre des plats réels, combien de
`confident`, `probable`, `none` — et combien de FAUX `confident`.
```

---

## A4 — `/app/progress` : ce que l'élève voit de ses photos

```
Tu testes une interface en conditions réelles, dans /Users/ahmedamara/Dev/Sophia 2.

[COLLER LES RÈGLES COMMUNES]

CE QUI A CHANGÉ sur `/app/progress` :
 - une carte « What you ate » : journal jour par jour, avec VIGNETTE de la photo,
   aliments NOMMÉS, bande de portion, et la phrase du modèle sur la taille ;
 - une carte « Your rhythm » : grille 7 jours × 5 moments, le moment venant de
   `occurred_at` (et non de `slot_key`, NULL sur 71 % des lignes) ;
 - les laitages comptent comme protéines (un bol de fromage blanc affichait
   « protein at 0 ») ;
 - les photos DISQUALIFIÉES ne comptent plus comme repas.

CE QUI DOIT ÊTRE VRAI :
 - après une photo réelle, les ALIMENTS sont nommés à l'écran. C'est le défaut
   d'origine : le modèle avait lu « yogurt » + « rolled oats » avec 0,95 et 0,98
   de confiance, et l'écran n'en montrait AUCUN ;
 - la vignette s'affiche ;
 - un bol de yaourt donne « protein at 1 », pas 0 ;
 - une photo de menu n'apparaît NULLE PART.

EDGE CASES, et ajoutes-en :
 - un repas déclaré à 23 h → bande « Night », et il ne doit pas glisser au
   lendemain ;
 - un élève dans un fuseau distant (`Pacific/Auckland`) : la grille suit-elle SON
   fuseau ou celui du navigateur ? Note l'écart, c'est une limite connue ;
 - deux repas dans le même créneau → une seule case, `×2`, bande = la PLUS GROSSE ;
 - un événement sans `occurred_at` lisible → compté « sans moment », jamais rangé
   au hasard ;
 - la bascule 7 jours / 30 jours ;
 - un élève SANS aucun log → aucune carte ne doit planter, et la copie ne doit
   pas faire attendre ;
 - LE GARDE TCA : pose `weekly_reviews.risk_band='restriction_flag'` sur la
   dernière semaine → TOUT l'écran chiffré doit disparaître. C'est une règle
   clinique, vérifie-la explicitement ;
 - cherche un kcal, un gramme, un pourcentage-objectif : il ne doit y en avoir
   AUCUN nulle part.

RENDS : des captures d'écran, et la ligne SQL derrière chaque chiffre affiché.
```

---

## A5 — MÉMOIRE → PLAN : la boucle qui vient d'être fermée

```
Tu testes en conditions réelles un pont neuf entre la conversation et la
génération de plan, dans /Users/ahmedamara/Dev/Sophia 2.

[COLLER LES RÈGLES COMMUNES]

CE QUI A CHANGÉ : ce que l'élève dit sur sa bouffe dans la conversation est
extrait par le memorizer, PROPOSÉ sur `/app/plan` dans une carte
« What you have told me about your eating », et une fois CONFIRMÉ par l'élève
il entre dans `student_goals.practical_constraints.food_preferences` — que les
DEUX générateurs lisent.
Modules : `_shared/keel/food_preference_promotion.ts`,
`components/FoodPreferencesCard.tsx`, `generate-meal-v1`, `generate-week-plan-v1`.

PROTOCOLE :
1. Fais parler un élève de sa bouffe dans `/app/chat` (aversion, contexte de
   vie, contrainte pratique), en anglais.
2. Déclenche le memorizer (`trigger-memorizer-daily`) et LIS `memory_items`.
3. Ouvre `/app/plan` : les propositions doivent apparaître.
4. Garde-en une, édite-en une, écarte-en une.
5. Génère un plan et un plat, et VÉRIFIE que ce qui est gardé influence la sortie.

CE QUI DOIT ÊTRE VRAI :
 - les items sont écrits EN ANGLAIS pour un élève `en-GB` (le repli de langue
   vient d'être basculé sur l'anglais) ;
 - une proposition écartée ne REVIENT JAMAIS ;
 - une proposition gardée ne se repropose pas ;
 - garder/éditer n'écrase NI `eating_rhythm` NI `cook_days` (trois cartes
   partagent le même jsonb) ;
 - `food_preferences_dismissed` (des UUID) n'apparaît JAMAIS dans le prompt
   envoyé au modèle.

LE FILTRE LE PLUS IMPORTANT À ÉPROUVER — LA LIGNE MÉDICALE :
 - fais déclarer une ALLERGIE dans la conversation. Elle doit atterrir dans
   `student_safety_constraints` et **JAMAIS** dans la carte de préférences.
   Une allergie éditable ou effaçable depuis cette carte est un RED CRITIQUE.
 - fais dire quelque chose de `sensitive` (santé mentale, famille) lié à la
   bouffe → ne doit pas monter.

AUTRES EDGE CASES, et ajoutes-en :
 - un souvenir `kind='event'` (« j'ai mangé une pizza mardi ») → ne doit PAS
   devenir une préférence permanente ;
 - un item `candidate` (non validé par la mémoire) → ne doit pas monter ;
 - une RÉTRACTATION (« en fait oublie le brocoli, j'aime bien maintenant ») ;
 - un élève sans `student_goals` → la carte ne doit pas planter ;
 - 25 préférences gardées → le plafond (20) tient-il, et la doctrine du coach
   reste-t-elle dans le prompt ? (le budget tronque par la QUEUE).

RENDS : le contenu de `memory_items` avant/après, le jsonb
`practical_constraints` à chaque étape, et le prompt réellement envoyé au modèle.
```

---

## A6 — ADVERSARIAL : casse ce que les autres ont validé

```
Tu es l'agent adversarial d'une campagne QA sur KEEL, dans
/Users/ahmedamara/Dev/Sophia 2. Les agents A1 à A5 valident chacun leur
périmètre. TON travail est de trouver ce qu'ils ne verront pas, parce qu'ils
regardent chacun dans une seule direction.

[COLLER LES RÈGLES COMMUNES]

TON ANGLE : les INTERACTIONS et les SÉQUENCES, jamais un mécanisme isolé.

PISTES (et trouve les tiennes) :
 - Une photo qui déclenche À LA FOIS une coche automatique ET une question de
   précision. Le plafond est d'UNE question par photo — vérifie qu'il n'y en a
   pas deux, et que l'accusé ne dit pas deux choses contradictoires.
 - Une photo pendant un tour de SAFETY (crise). Rien de durable ne doit
   s'écrire, et surtout pas une coche. Ce dépôt a déjà eu des fuites de gate
   safety qui committaient malgré tout.
 - Une correction juste après une coche automatique : « non c'était autre
   chose ». Le flow de précision doit AMENDER, pas doubler le repas — et la
   coche ? Vérifie ce qu'elle devient. Le tour de confirmation n'est PAS
   construit : dis précisément ce qui se passe aujourd'hui.
 - Un élève dont le coach publie une doctrine PENDANT la session. La réponse
   suivante bascule-t-elle de « mes connaissances » à « la méthode du coach » ?
 - Une préférence alimentaire qui CONTREDIT la doctrine du coach (« j'adore les
   6 petits repas » alors que le coach l'interdit). Qui gagne dans le plan
   généré ? Est-ce que le verrou de sortie mord ?
 - Deux onglets ouverts sur `/app/plan`, deux cartes éditées en même temps →
   l'une écrase-t-elle l'autre dans `practical_constraints` ?
 - Une régénération de composition APRÈS une coche automatique : les clés de
   l'ancienne composition ne matchent plus. Que voit l'élève ?
 - Le passage de minuit dans le fuseau de l'élève pendant qu'il envoie une photo.
 - Un élève dont `country` est NULL qui déclenche la garde de crise → vers quel
   pays part la hotline ? (défaut connu : `en-US` codé en dur ⇒ 'US').

RENDS : les scénarios, ce que tu as cassé, et pour chaque red la séquence exacte
qui le reproduit. Un red que tu ne sais pas reproduire n'est pas un red : dis-le.
```

---

## A7 — REVALIDATION (à lancer APRÈS les corrections)

```
Tu revalides des corrections apportées suite à une campagne QA, dans
/Users/ahmedamara/Dev/Sophia 2.

[COLLER LES RÈGLES COMMUNES]

L'utilisateur te donnera la liste des reds corrigés. Pour CHACUN :
1. Rejoue le scénario EXACT du rapport d'origine, pas une variante.
2. 3 fois minimum. Ce dépôt a mesuré que les correctifs prompt-only régressent
   en run réel là où les probes restent vertes.
3. Vérifie la CONDITION DE DÉSARMEMENT : toute ceinture ajoutée doit avoir un
   cas où elle NE mord PAS. Une garde qui mord toujours est une garde qu'on
   débranche dans la semaine — teste explicitement le cas légitime voisin.
4. Cherche la RÉGRESSION AILLEURS : une correction sur le chemin photo peut
   casser le chemin texte, et inversement.

RENDS : par red, VERT (3/3) / ROUGE (n/3) / NOUVEAU (un défaut que la correction
a créé), avec la preuve SQL.
```

---

## CE QUE JE SAIS DÉJÀ ET QUI N'EST PAS À REDÉCOUVRIR

- `/app/progress` refuse un élève sans coach avec « Ask your coach for an
  invitation » — même modèle à l'envers que le lot A1. Connu, non corrigé.
- Une assiette à plusieurs groupes sans prescription repart avec
  `food_group_ref: null` (`several_groups_none_in_plan`) : **tout plat composé**
  perd son identité de groupe pour la couverture du coach. Connu, non corrigé.
- Le catalogue `food_items` a des trous mesurés : `berries` (×25),
  `mixed salad leaves` (×15), `wholegrain bread` (×15), `basmati rice` (×11),
  `tuna` (×9). Les assaisonnements (sel, poivre, ail) n'ont PAS à y être.
- `deno test` sur tout `_shared/keel/` échoue sur `reengage_composer.ts`
  (fichier non commité d'une autre session, import manquant) et sur 2 tests de
  plafond de plats dans `meal_generation_test.ts`. Sans rapport avec ce lot.
