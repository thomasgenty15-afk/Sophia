# Bug Sheet - adjust_plan_item local dispatcher real invalid R1 - 2026-06-09

| Bug id | Tours | Famille | Domaine owner | Symptome | Preuves | Correction attendue | Statut |
| --- | --- | --- | --- | --- | --- | --- | --- |
| ADJUST-PLAN-LD-R1-B01 | Preflight/T1 | BF-TEST-01 | Runtime QA local / Supabase local / test-send-message | Le run IA reel ne peut pas demarrer: l'endpoint local alterne entre `200` sur `/auth/v1/settings` et refus de connexion sur `auth/v1/token`, avec `supabase status` intermittent dans le sandbox. | `curl /auth/v1/settings` retourne parfois `200`; appels suivants vers `auth/v1/token` echouent avec `curl: (7) Failed to connect to 127.0.0.1 port 54321`; `supabase status --output json` alterne entre sortie lisible et permission Docker refusee. | Stabiliser Supabase local et l'acces sandbox/Docker avant relance; verifier 3 appels consecutifs: `/auth/v1/settings`, `auth/v1/token`, `/functions/v1/test-send-message force_full_ai=true`. | open |
| ADJUST-PLAN-LD-R1-B02 | Cleanup | BF-TEST-01 | QA cleanup local | La connexion QA temporaire creee ne peut pas etre nettoyee parce que l'API locale refuse la connexion au moment du cleanup. | `scripts/qa-cleanup-run-connection.sh qa-skill all_skills_adjustplan_local_20260609_qa1` echoue sans status; retry avec URL locale et service role local echoue `curl: (7) Failed to connect`. | Une fois l'API locale stable, relancer le cleanup cible de `all_skills_adjustplan_local_20260609_qa1`; ne pas faire de reset global. | open |
