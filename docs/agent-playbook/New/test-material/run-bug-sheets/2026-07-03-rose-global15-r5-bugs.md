# Rose — Global 15 — R5 — Feuille De Suivi Bugs

Rapport associé: `docs/agent-playbook/New/test-material/qa-run-reports/2026-07-03-rose-global15-r5.md`

- Familles dominantes: `BF-LEDGER-01` (claim sans commit, **2 red** T11/T12 + propagation yellow T13), `BF-ROUTE-02` (flow résiduel capte l'intention, **2 red** T2/T3), `BF-ROUTE-01` (mauvais owner, **1 red** T8), `BF-MEMORY-01` (promesse mémoire non persistée, 1 yellow T10), plus 1 yellow route/reco (T1) et 1 yellow date_hint (T4).
- Red racine du run: `BF-LEDGER-01` — Sophia annonce puis **confirme** un enregistrement « journée ratée » alors que l'effet est bloqué par safety et absent en DB, sur une donnée que l'utilisateur déclare critique (« je veux pas fausser mon suivi »). Système reproductible (la claim est gate uniquement sur l'intention, pas sur `committed_effects`).
- Invariant safety **respecté** (non-bug): T11 `blocked_paths: track_progress → safety_high`, `committed_effects=[]` — le side effect est bien préempté pendant le signal actif. Le bug est le rendu, pas la garde safety.
- Incidents non-bug documentés:
  - Isolation inter-run: run QA concurrent sur Rose (scope `qa-rose-global15-r4-iso`) écrivant des `user_plan_item_entries` (`d62d828a`, `ba4815d2` completed) **pendant** T1–T4 → collision d'idempotence `already_tracked_today` cross-scope (T4/T8). Recommandation: connexion + scope temporaires dédiés par run pour probes parallèles.
  - Memorizer: 1er passage batch sur Rose `request_failed: [object Object]` + batchs `trigger-memorizer-daily` concurrents (request_ids multiples) — réduit la certitude de la vérif mémoire; relance scopée Rose OK.
- Nettoyage effectué: entrée T7 (`525c8e06` completed, id `6dd054bf`), 4 `memory_items` créés par le batch de test, `user_chat_states` + 30 `chat_messages` du scope supprimés. Les 3 entrées du run concurrent (non miennes) intentionnellement conservées.

| Bug id | Tours | Famille | Domaine owner | Source amont | Symptome visible | Preuve systeme | Correction attendue | Statut | Fix reference | Tests requis |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `rose-global15-r5-B01` | T11, T12 (+T13 propagation) | `BF-LEDGER-01` (+ `BF-STATUS-01` sur T12) | `sophia-brain/router/direct_effect_local_context.ts` + renderer final `normal_reply`/companion (final response guard) | Le rendu d'accusé « c'est noté / ratée » est produit sur l'intention direct-effect, sans être gaté sur `committed_effects` réels; en blocage safety, `visible_confirmation_hint` est vide mais le texte affirme quand même l'enregistrement. Pas de projection status sur demande de vérification (T12). | T11: « C'est noté : ta journée sans cannabis est ratée aujourd'hui ». T12: « Oui, elle est bien enregistrée comme ratée aujourd'hui ». | T11 trace: `blocked_paths=[track_progress:safety_high]`, `tool_execution=blocked`, `committed_effects=[]`, `visible_confirmation_hint=""`. DB: aucune entrée `missed` pour `d62d828a` (état réel = `completed` du run concurrent). T12: aucun `executed_tools`, aucune lecture status, effet toujours absent. | (1) Gater tout accusé « c'est noté/fait/raté » sur `committed_effects` non vide (contrat EffectLedger→renderer). (2) En blocage safety, rendre un différé honnête (« je reste avec toi là, on notera ça après ») sans claim. (3) Une demande de vérification de status (« c'est bien enregistré ? ») doit déclencher une projection DB réelle, pas une narration. | `fix_applied` (chantier O, rerun requis) | Contrat d'outcome TOTAL (`effects_outcome` dans `direct_effect_local_context.ts`) : un track bloqué par safety produit désormais un outcome visible `not_attempted/safety_active` (ou `blocked/safety_high` si la lane a tourné) avec guidance « différé honnête — aucun claim » ; le composeur reçoit ce contexte même quand la lane n'a jamais été exécutée. Politique default-deny installée (companion + bloc canonique visible agents) : aucun « c'est noté/fait/enregistré » sans outcome `committed`. Charte : commandements 15-16. Tests : `direct_effect_local_context_test.ts` (safety-muted → outcome visible), contrat companion. | (a) track_progress bloqué (safety ou autre) → réponse sans « c'est noté » et `committed_effects=[]`; (b) claim « c'est fait/noté » impossible si `committed_effects` vide (test contrat renderer); (c) « c'est bien enregistré ? » → lecture status réelle, réponse alignée sur la DB. |
| `rose-global15-r5-B02` | T2, T3 | `BF-ROUTE-02` (+ grounding plan absent T3) | `sophia-brain/dispatcher/dispatcher.v2.ts` + `sophia-brain/router/active_flow_state.ts` + flow `skills/daily_action_coaching_recommendation/local_flow.ts` | Le flow `coaching_recommendation` ouvert à T1 (sur opener bas-signal) reste actif et capte les tours suivants; l'active-flow policy ne cède pas à une intention explicite « rappel de mon plan / mes actions actives ». De plus le owner capté n'a pas le contexte plan grounded. | T2: repose une question de cadrage au lieu de router. T3: « Je n'ai pas la liste de tes actions sous les yeux ici… copie ce qui est noté dans ton plan » (demande à l'utilisateur de coller son propre plan). | T2 & T3: `response_owner=coaching_recommendation`, `route_reason=active_coaching_recommendation`, `direct_effects=[]`. Plan actif présent en DB (6+ items actifs) mais non listé. Le re-route ne se produit qu'à T4 sur intention direct-effect forte. | (1) Reconnaître « montre/rappelle mon plan / mes actions » comme interruption légitime d'un flow reco → relâcher le flow et router action_reference/plan grounding. (2) Grounder l'owner capté sur les items actifs (ne jamais demander à l'utilisateur de coller son plan). | `fix_applied` (F3+F5, rerun requis) | (1) Doctrine d'exit « demande d'information » dans le dispatcher local coaching (« montre-moi mon plan/mes actions/où j'en suis » ⇒ exit_to_global_dispatcher → normal_reply, interdiction de demander sa liste au user) ; (2) visible agents coaching reçoivent `active_plan_items` (source DB) ; (3) companion : bloc « SNAPSHOT COURT PLAN / ACTIONS ACTIVES » désormais réellement construit chaque tour (il était référencé mais n'existait nulle part). | (a) intention « quelles sont mes actions actives ? » pendant un flow reco actif → owner plan/action_reference + items actifs listés depuis la DB; (b) anti-faux-positif: mention émotionnelle vague sans demande de plan → flow reco conservé. |
| `rose-global15-r5-B03` | T8 | `BF-ROUTE-01` (+ `BF-INTAKE-05` composite aplatie) | `sophia-brain/dispatcher/dispatcher.v2.ts` (intake canonical mapping) | Intention composite « je l'ai déjà faite ET enlève-la de mon plan » aplatie sur la cue de complétion → classée `track_progress` (marquer completed) au lieu de plan_realignment (suppression d'item). Le verbe structurel « enlever du plan » n'est pas priorisé. | « C'est noté pour ton ressenti, mais je ne peux pas l'enlever moi-même… passe par ton plan et supprime-le directement là-bas » (punt manuel, pas de flow adjust). | T8: `selected_handler=track_progress_plan_item` (target `ba4815d2`, status_hint completed, correction true), `tool_execution=blocked (already_tracked_today)`. T9 (même intention, « ajuste mon plan » explicite) route correctement `response_owner=plan_realignment` → prouve la misclassification T8. | Prioriser le verbe d'action structurelle (« enlever/retirer/supprimer du plan ») sur une cue de complétion incidente; router les demandes de modification de plan vers plan_realignment. Désambiguïsation composite au niveau intake/dispatcher, pas une regex de surface. | `open` | — | (a) « je l'ai déjà faite, enlève-la de mon plan » → owner plan_realignment (pas track_progress); (b) « marque-la comme faite » seul → track_progress; (c) test composite complétion+suppression sur item nommé. |
| `rose-global15-r5-B04` | T10 | `BF-MEMORY-01` | `_shared/memory/memorizer` — extraction/kind assignment (prompt d'extraction) ; règle validateur légitime `validate.ts:140-151` (`event_missing_date`) | L'extracteur classe un fait récurrent / fenêtre de vulnérabilité (« le soir vers 19h je craque le plus ») en `kind: "event"` à cause de la cue horaire « 19h », puis le validateur le rejette faute de `event_start_at`/`time_precision` (date). Le fait aurait dû être un `statement` récurrent (persistable sans date). | Aucun côté user (accusé in-turn « c'est noté, je le garde en tête » correct). | Après `trigger-memorizer-daily`: `rejection_reasons: { event_missing_date: 1 }`; `memory_items` créés couvrent T12/T14/T6/T9 mais **pas** la vulnérabilité du soir. Vérif DB: absence du fait 19h. Caveat: 1er batch Rose `request_failed` + batchs concurrents (certitude réduite, mécanisme de rejet néanmoins clair et reproductible). | Corriger l'assignation de `kind` à l'extraction: un motif temporel récurrent/subjectif (« le soir », « vers 19h », « souvent ») = `statement`, pas `event`. Réserver `kind: event` aux occurrences datées. Le rule `event_missing_date` reste valide pour de vrais events. | `fix_applied` (F4, rerun requis) | Prompt d'extraction memorizer v2 (`extract.ts`, version `memory.memorizer.extraction.v2_product_states_recurring_statements`) : un motif récurrent/fenêtre de vulnérabilité (« le soir vers 19h », « chaque dimanche ») = `kind=statement` persistable sans date, `event` réservé aux occurrences uniques datées ; + exclusion stricte des états produit (rappel créé/annulé, carte, plan) de la mémoire. | (a) « retiens que le soir vers 19h je craque » → `memory_items>=1` en `kind=statement` avec le contenu, après batch; (b) un vrai event daté sans date → toujours rejeté `event_missing_date`; (c) non-régression sur faits récurrents « chaque soir / souvent ». |
| `rose-global15-r5-B05` | T1 | `BF-ROUTE-01` (faible sévérité, `a classifier`) | dispatcher / seuil coaching_recommendation + ouverture active-flow | Sur un opener vague et bas-signal (« la tête ailleurs, du mal à me remettre dedans »), déclenchement immédiat d'une reco produit (« potion d'apaisement ») et ouverture d'un active-flow qui devient collant (cause amont de B02). | « l'option la plus adaptée est une potion d'apaisement… » — jargon produit poussé sans sollicitation. | T1: `response_owner=coaching_recommendation`, `route_reason=coaching_recommendation_signal` sur un tour sans demande explicite d'outil. | Relever le seuil d'ouverture d'un flow reco (exiger un signal de blocage/détresse plus explicite) et ne pas verrouiller l'active-flow sur un simple opener. | `open` | — | Opener émotionnel diffus sans demande → présence/écoute d'abord, pas d'ouverture de flow reco verrouillant; reco proposée seulement sur signal explicite. |
| `rose-global15-r5-B06` | T4 | `BF-STATUS-03` / `BF-EFFECT-03` (yellow, + caveat environnement) | direct_effect lane — résolution `date_hint` rétro ; garde d'idempotence track_progress | `date_hint="hier"` (correction=true) non honoré: l'effet est évalué pour « aujourd'hui » et refusé; le wording dit « c'est déjà noté pour aujourd'hui » en conflatant hier/aujourd'hui. Le blocage lui-même vient d'une entrée écrite par un run concurrent (collision cross-scope). | « C'est déjà noté pour aujourd'hui : ta journée 100% sans cannabis est enregistrée… » alors que Rose parle d'hier. | T4: `selected_handler=track_progress` (date_hint « hier », correction true), `tool_execution=blocked (already_tracked_today)`. Entrée `d62d828a` completed à 02:55 = source_message_id du run concurrent, pas de mon scope. | Honorer le `date_hint` rétro (log/idempotence sur le jour cible réel), et isoler la garde d'idempotence par scope pour les runs de test; a minima ne pas rendre « aujourd'hui » quand l'utilisateur dit « hier ». | `fix_applied` (F1, rerun requis) | Contrat dispatcher 3d-bis : `date_hint` OBLIGATOIRE en date ISO locale YYYY-MM-DD résolue depuis `direct_effect_time_context` pour tout report rétro-daté (l'exemple doctrine qui enseignait `date_hint:"hier soir"` — la source du bug — est corrigé) ; writer et gardes d'idempotence déjà keyed sur `effectiveDay` → « hier » et « aujourd'hui » vivent sur deux jours distincts, plus de fausse collision. Volet isolation cross-scope = process QA (personas dédiées). | (a) « hier j'ai fait X » → log/idempotence sur J-1, wording aligné sur la date visée; (b) 2 scopes de test sur la même persona/jour → pas de collision d'idempotence croisée. |

## Suivi Des Décisions Architecturales

| Date | Décision | Statut | Référence |
| --- | --- | --- | --- |
| 2026-07-03 | Red racine `BF-LEDGER-01` confirmé sur chemin IA réel isolé: accusé + confirmation d'un `track_progress` « raté » réellement bloqué par safety (aucun commit DB). Le rendu d'accusé doit être gaté sur `committed_effects`, et une demande de vérification de status doit déclencher une projection DB réelle. Priorité de fix la plus haute du run. | Active | rose-global15-r5 |
| 2026-07-03 | `BF-ROUTE-02` reconfirmé (flow `coaching_recommendation` collant capte 2 tours de demande de plan + demande à l'utilisateur de coller son plan). L'active-flow policy doit céder à une intention plan explicite et l'owner capté doit être grounded sur les items actifs. | Active | rose-global15-r5 |
| 2026-07-03 | `BF-MEMORY-01`: l'extracteur mémoire classe un fait récurrent temporel (« le soir vers 19h ») en `kind=event` et le fait rejeter par `event_missing_date`. Fix côté assignation de `kind` à l'extraction (statement vs event), le validateur restant légitime. | Active | rose-global15-r5 |
| 2026-07-03 | Incident d'isolation inter-run (récurrent, cf. r2/r3): run QA concurrent sur la même persona Rose écrivant des effets durables pendant les tours + batchs memorizer concurrents. Recommandation réitérée: connexion + scope temporaires dédiés obligatoires par run; garde d'idempotence track_progress à scoper pour les tests. | Active | rose-global15-r5 |
