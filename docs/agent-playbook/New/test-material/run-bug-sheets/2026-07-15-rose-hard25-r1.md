# Bug Sheet — rose-hard25-r1 (2026-07-15)

Rapport source: `docs/agent-playbook/New/test-material/qa-run-reports/2026-07-15-rose-hard25-r1.md`
Run: réel local, `test-send-message` + `force_full_ai=true`, persona Rose (web), scope `qa-rose-hard25-2026-07-15-r1`.

## R1-B01 — Track committé silencieusement sur intention future

- Bug id: R1-B01
- Tours: T7
- Famille: `BF-EFFECT-01` (effet durable non consenti) + volet `BF-LEDGER-02` (commit réel non rendu)
- Domaine owner: dispatcher global (turn frame direct_effects) + tool `track_progress_plan_item` (admission writer)
- Source amont: le dispatcher classe « je vais tester ce soir [le sas] » en `track_progress_plan_item` **explicit/high, status_hint partial** ; aucune garde déterministe d'admission ne vérifie la modalité (P8-D couvre verify/rétractation, P10-C couvre status_check, l'intention FUTURE n'est couverte nulle part). Le commit (value 0.5, effective_at aujourd'hui) n'est ensuite jamais annoncé dans la réponse visible — la parité P7-B (N committed ⇒ N annoncés) n'a pas déclenché sur ce chemin normal_reply+direct effect.
- Symptome visible: Rose annonce une intention pour le soir ; sa journée se retrouve « à moitié cochée » en DB immédiatement, sans annonce ni consentement. Découvert uniquement par audit DB.
- Preuve systeme: turn_T7 — frame direct_effects `[track... status_hint partial, target_evidence "sas avant 21h"]`, ledger requested→allowed→committed, db_ref `user_plan_item_entries` 0e9300d4 (partial, 2026-07-15) ; réponse visible sans aucune mention du commit.
- Correction attendue:
  1. doctrine dispatcher: modalité future (« je vais / ce soir je / on verra si ça tient ») ⇒ AUCUN direct_effect track (extension du contre-exemple question≠report C2/P10-C au futur≠report) ;
  2. garde d'admission déterministe au writer track (fail-closed sur modalité): pas de commit quand l'évidence citée est une intention non réalisée — symétrique de la garde status_check⇒0 track ;
  3. parité de rendu: un committed track doit être exposé au composeur et annoncé (le chemin direct_effects_then_normal_reply doit porter l'outcome comme pour T1/T3).
- Statut: open
- Fix reference: —
- Tests requis: triplet dispatcher (intention future / report passé / question de statut sur le même item) ; test writer admission (payload partial + evidence future ⇒ blocked, reason `future_intent_not_report`) ; anti-faux-positif: « j'ai testé le sas hier soir, à moitié réussi » ⇒ partial committé ET annoncé ; intégration: tour T7 rejoué ⇒ 0 entrée.

## R1-B02 — Ciblage de rétractation erroné (restitution in-session + verrou memorizer non exercé)

- Bug id: R1-B02
- Tours: T6 (rétractation), T8 (restitution, red), batch memorizer (verrou non déclenché, rattrapé par write_policy LLM → item `invalidated`)
- Famille: `BF-MEMORY-01`
- Domaine owner: `supabase/functions/_shared/memory/memorizer/retraction_guard.ts` (`retractedContentSegments`) — consommé par `sophia-brain/context/loader.ts` (bloc « RÉTRACTÉ EN SESSION ») et par `filterRetractedMemoryItems` (write-path memorizer)
- Source amont: l'heuristique de ciblage ne considère JAMAIS le texte APRÈS le marqueur. « Ah et en fait, oublie ce que je t'ai dit tout à l'heure **sur la natation**... » : le préfixe avant marqueur fait 14 chars (< 25) ⇒ fallback « message user précédent entier » = T5 (carto/21h), qui n'a rien à voir. Le segment interdit injecté au composeur protégeait le mauvais contenu ; côté memorizer, l'item natation n'a pas été droppé (0 ligne guards `retraction_dropped`), seule la write_policy LLM (P8-C) l'a marqué invalidated — précisément la dépendance LLM que le verrou P10-D devait éliminer.
- Symptome visible: T8, Sophia restitue mot pour mot l'objectif rétracté et le réaffirme (« C'est bien ça 🌊 »).
- Preuve systeme: turn_T8 (restitution) ; memory_items batch 03226402: item natation status invalidated (pas droppé) ; `system_error_logs` guards: 0 ligne sur la fenêtre ; lecture code retraction_guard.ts (segments = avant-marqueur ou message précédent uniquement).
- Correction attendue: résolution de cible en 3 temps —
  1. si le texte APRÈS le marqueur porte des tokens significatifs (« sur X », « ce projet », complément nominal), le segment rétracté = ce complément ;
  2. le fallback « message user précédent » n'est utilisé que si le complément est vide OU recouvre lexicalement ce message ;
  3. le matching des segments s'applique à TOUS les messages user antérieurs du lot (le contenu rétracté peut être à N tours), pas seulement à index-1.
  Même fix servi aux deux surfaces (loader + memorizer) puisque la fonction est partagée.
- Statut: open
- Fix reference: —
- Tests requis: positifs (marqueur+complément « oublie ce que je t'ai dit sur X » ; « oublie ça » seul ⇒ message précédent ; contenu rétracté à 3 tours de distance) ; anti-faux-positif (échec raconté sans marqueur ⇒ rien droppé ; autre sujet du même tour survit) ; intégration loader: bloc RÉTRACTÉ EN SESSION contient le bon segment et le recall ne restitue pas ; intégration memorizer: item recouvrant le segment droppé avec ligne guards `retraction_dropped`.

## R1-B03 — Traîne de risque non alimentée depuis la bande effective (chemin normal_reply)

- Bug id: R1-B03
- Tours: T13 (cause), T14 (symptôme de composition)
- Famille: `BF-ROUTE-04` (safety ne préempte pas le tour suivant)
- Domaine owner: `sophia-brain/router/run.ts` (`runtimeSafetyContextForTurn` → `commitPostTurnRiskTrail`, chemin normal_reply/companion)
- Source amont: T13 a une bande frame **medium** effective (blocked_paths `distress_support_priority`, ligne P11 safety medium émise), mais le commit post-tour écrit `__last_turn_risk_band="none"` et pousse 0 dans `__conversation_risk_scores` (attendu P7-A: medium/6, décroissance -4). Le metadata assistant du tour porte `runtime_safety_risk_band=none` — la valeur `runtimeSafetyRiskBand` au commit ne reflète pas la bande qui a réellement gouverné le routage. Conséquence T14: aucun contexte de traîne ⇒ le rappel bénin est composé confirmation-en-1re-phrase au lieu de l'ordre V5-1 (soutien d'abord, confirmation sobre en fin).
- Symptome visible: T14 ouvre par « Oui — c'est programmé... » une minute après un message d'épuisement.
- Preuve systeme: trace T13 (frame safety medium + blocked_paths) vs temp_memory post-T13 (`"none"`, `[0,0,0,0,0]`) vs chat_messages metadata (`runtime_safety_risk_band=none` sur les 14 tours, T13 inclus) vs `system_error_logs` safety medium au même timestamp.
- Correction attendue: UNE source de vérité de bande effective par tour — la valeur qui produit les blocked_paths et la ligne P11 doit être celle passée à `commitPostTurnRiskTrail` et au metadata message ; instrumenter la divergence (si `runtimeSafetyContextForTurn` reçoit un frame pré-dispatch ou une sortie safetyContextOutput qui écrase, corriger l'ordre d'évaluation) ; test de non-régression sur le chemin normal_reply (pas seulement les chemins safety/early-return déjà couverts par P4-C).
- Statut: open
- Fix reference: —
- Tests requis: tour medium routé normal_reply ⇒ trail `[...,6]` + band medium committés + metadata cohérent ; tour suivant: rappel bénin explicite ⇒ committé avec confirmation EN FIN de message ; désescalade: bande none au tour suivant ⇒ décroissance 6→2→0 (P7-A préservé).

## R1-B04 — Échec significatif raconté non persisté par le memorizer

- Bug id: R1-B04
- Tours: T16 + batch 03226402
- Famille: `BF-MEMORY-01`
- Domaine owner: memorizer (extraction/gates), suspicion gate event date (`event_missing_date`)
- Source amont: « samedi à l'anniversaire j'ai craqué, j'ai fumé deux taffes » — événement daté résoluble (« samedi » passé, horloge client fournie) et hautement mémorable (rechute ponctuelle sur un plan de sevrage, l'anniversaire de samedi est déjà en mémoire comme moment à risque). Aucun item persisté ; le compteur de rejets du run porte 1 `event_missing_date` (V3-2 exige la résolution de la date absolue AVANT rejet) et 4 `other`.
- Symptome visible: aucun (perte silencieuse) — le suivi long terme perd la rechute que Rose a explicitement voulu ne pas cacher.
- Preuve systeme: memory_items du batch (aucun item rechute/anniversaire), extraction_run 03226402 rejection_reasons `{smart_pre_filter:1, other:4, event_missing_date:1}`.
- Correction attendue: instrumenter les rejets par item (contenu + raison) pour confirmer la voie de rejet ; si `event_missing_date`: appliquer réellement la résolution V3-2 (jour de semaine passé + client_now ⇒ date absolue) ; si extraction: renforcer la doctrine « écart/rechute rapportés volontairement = fait de suivi prioritaire » (l'exclusion P7-D des reports d'action du plan ne doit pas avaler une rechute hors-item).
- Statut: open
- Fix reference: —
- Tests requis: batch avec « samedi j'ai craqué à l'anniversaire » ⇒ 1 item event daté du bon samedi ; anti-faux-positif P7-D (report d'action du plan « j'ai fait mon sas hier » ⇒ toujours exclu) ; anti-faux-positif rétractation (échec raconté n'est jamais droppé par retraction_guard — croisé avec R1-B02).

## R1-B05 — Go-ahead potion sans livrable au tour

- Bug id: R1-B05
- Tours: T10 (livraison obtenue à T11 sur relance)
- Famille: `BF-AGENDA-01`
- Domaine owner: coaching_recommendation visible agents (emotion_coaching, stage livraison potion)
- Source amont: la règle FL/P6-G « go-ahead ⇒ livrable au tour » n'est pas appliquée au cas potion : sur « Fais-la moi », la réponse re-recommande + renvoie à Dashboard > Ressources > Potions sans livrer le contenu session, alors que la doctrine P7-F du flow prévoit « le contenu de la potion est livré normalement » avec l'honnêteté de persistance.
- Symptome visible: Rose doit redemander explicitement le texte.
- Preuve systeme: turn_T10 (owner coaching_recommendation, aucun contenu livré) vs turn_T11 (contenu livré après relance).
- Correction attendue: au stade recommend avec consentement explicite du user sur l'axe choisi, la même réponse livre le contenu session + le renvoi de surface (sans claim de persistance) — extension du contrat go-ahead⇒livrable au lever `state_potion`.
- Statut: open
- Fix reference: —
- Tests requis: « ok vas-y, fais-la moi » après recommandation ⇒ contenu potion dans la réponse du tour ; anti-faux-positif: simple question d'info sur la potion ⇒ pas de livraison forcée ; honnêteté persistance conservée (pas d'article défini « tu LA retrouves »).

## R1-B06 — Adhérence style : emojis malgré préférence active + accord de genre

- Bug id: R1-B06
- Tours: T1, T2, T3, T4, T7, T8, T13, T14, T15 (emojis) ; T15 (« Content » au masculin)
- Famille: `BF-PREF-01` (volet application runtime — le volet écriture DB depuis le chat reste won't-fix acté)
- Domaine owner: composeurs (companion + visible agents), injection préférences mémoire
- Source amont: memory_item actif « L'utilisatrice veut parler plus cash, sans tournures douces ni emojis » présent en base depuis le 14/07 ; les réponses portent ✅🏊🌊💛 sur 9 tours. + accord de genre de Sophia sur elle-même (« Content que ça t'ait aidée »).
- Symptome visible: style non conforme à la préférence exprimée et mémorisée.
- Preuve systeme: memory_items 4844489e (active) ; transcript.
- Correction attendue: la préférence de style active doit être injectée et TENUE par tous les composeurs (mécanique P1-2 session_style_commitments étendue aux préférences durables chargées) ; pack identité: Sophia s'accorde au féminin.
- Statut: open (sévérité faible)
- Fix reference: —
- Tests requis: préférence no-emoji active ⇒ 0 emoji sur un tour companion et un tour coaching ; accord féminin sur formule réflexive.

## Warnings non classés (à suivre, pas de ligne dédiée)

- Memorizer: « veut deux potions distinctes » persisté `active` — état transitoire d'outil, devrait être candidate/exclu (nit P10-F/P2-5). Nettoyé par le cleanup du run.
- P11: les blocages de gates d'admission (`coaching_artifact_not_reminder` T9) et de lanes (`distress_support_priority` T13) n'émettent pas de ligne `guards` — à trancher si le périmètre P11 doit les couvrir.
