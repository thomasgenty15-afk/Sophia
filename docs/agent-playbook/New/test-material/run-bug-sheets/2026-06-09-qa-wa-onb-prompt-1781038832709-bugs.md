# Bug Sheet — qa-wa-onb-prompt-1781038832709

## R1-B01

- Bug id: R1-B01
- Tours: Tour 1
- Famille: BF-TEST-01 — Trace/test incoherent ou suite malsaine
- Domaine owner: `whatsapp-webhook`
- Source amont: imports runtime du webhook vers `sophia-brain/router/dispatcher.ts`
- Symptome visible: aucune reponse Sophia; le webhook retourne `503 BOOT_ERROR`.
- Preuve systeme:
  - Edge Runtime: `handlers_optin_bilan.ts` importe `analyzeSignalsV2`, export absent.
  - `deno check supabase/functions/whatsapp-webhook/index.ts`: `TS2305 no exported member 'analyzeSignalsV2'`.
  - Aucun outbound WhatsApp, aucun `chat_messages`, aucun `user_plan_item_entries`.
- Correction attendue: aligner le webhook avec l'API dispatcher actuelle ou restaurer un wrapper `analyzeSignalsV2` exporte, puis ajouter une verification de boot local du webhook.
- Statut: open
- Fix reference: a creer
- Tests requis:
  - Positif: webhook local `loopback` retourne `200` sur un inbound texte simule.
  - Integration: run WhatsApp onboarding `onboarding_pref_challenge` avec preference nuancee appelle `whatsapp_onboarding.local_dispatcher`.
  - Anti-regression: `deno check supabase/functions/whatsapp-webhook/index.ts` ne signale plus d'import manquant bloquant.
