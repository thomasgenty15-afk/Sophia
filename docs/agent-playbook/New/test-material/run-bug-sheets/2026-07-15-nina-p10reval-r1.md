# Bug Sheet — nina-p10reval-r1 (2026-07-15)

Rapport : `docs/agent-playbook/New/test-material/qa-run-reports/2026-07-15-nina-p10reval-r1.md`
Contexte : revalidation P10 (blocs A/B/E verts sur le nominal) ; tous les bugs ci-dessous vivent sur la chaîne « composite multi-jours même objet × filet anti-doublon » (T6→T13) + 2 yellows de rendu.

## R1-B01 — Jours nommés bornés classés récurrents

- Bug id: R1-B01
- Tours: T6
- Famille: BF-INTAKE-05 (sémantique composite aplatie)
- Domaine owner: dispatcher direct effects (payload_hint cardinality)
- Source amont: classification `cardinality` — « jeudi et vendredi à 18h » (2 jours nommés, même heure, sans marqueur d'habitude) → `recurring` au lieu de `once×2` ; blocked `recurring_not_supported` + redirection Initiatives, 0 ligne.
- Symptome visible: refus de créer deux rappels ponctuels bornés ; renvoi Dashboard > Initiatives pour une course d'une semaine.
- Preuve systeme: turn_frame T6 `cardinality: recurring`, blocked_effects `recurring_not_supported`, 0 ligne DB.
- Correction attendue: contrat de cardinalité au dispatcher : jours calendaires NOMMÉS dénombrables sans marqueur d'habitude (« tous les », « chaque ») = fan-out once×N même à heure commune ; recurring exige le marqueur d'habitude. (La capacité fan-out existe : T7 a émis 2 once après reformulation.)
- Statut: fix_applied
- Fix reference: P12-B (ceinture runtime « jours nommés sans marqueur d'habitude ⇒ fan-out once×N » + doctrine dispatcher DISTINCTION JOURS NOMMES + ancres contrat)
- Tests requis: triplet « jeudi et vendredi à 18h » (once×2) / « tous les jeudis et vendredis à 18h » (recurring) / « jeudi à 18h » (once) ; anti-faux-positif : « chaque soir à 18h » reste recurring.

## R1-B02 — Fan-out phantom : résolution UTC par-item fausse + double committed même id

- Bug id: R1-B02
- Tours: T7 (exposé par T8)
- Famille: BF-EFFECT-03 (racine, payload durable faux) + BF-LEDGER-01 (symptôme, claim sans commit)
- Domaine owner: compilateur de payload direct effects + exécuteur create_one_shot_reminder + EffectLedger
- Source amont: sur le fan-out 2 items, l'item « vendredi à 18h » est compilé avec `scheduled_for 2026-07-16T16:00Z` (= jeudi) et `local_label vendredi 17 juillet à 18:00` (UTC≠label) ; l'exécuteur reçoit 2 payloads identiques (instant+instruction), n'insère qu'UNE ligne et le ledger enregistre 2 `committed` portant le MÊME id `d37195fe`. La parité rendu=ledger (P7-B/P10-B) passe car le ledger lui-même est faux.
- Symptome visible: « c'est bien deux rappels ponctuels… jeudi à 18h et vendredi à 18h » — 1 seule ligne DB (jeudi). Récidive exacte de nina-hard24 R1-B02 / rose-p7verify sur le chemin « fan-out après correction de cardinalité ».
- Preuve systeme: requested_effects T7 (2 entrées, même UTC, labels différents) ; committed_effects (2 entrées, même id) ; DB : 1 ligne `d37195fe` 16/07 16:00Z ; récap T8 groundé sans vendredi.
- Correction attendue: (1) résolution déterministe (jour nommé, heure) PAR ITEM par le parseur (doctrine P3-B : parseur > UTC_time LLM ; le when_hint « vendredi à 18h » était résoluble seul) avec invariant UTC↔label cohérents ; (2) invariant exécuteur/ledger : deux entrées `committed` ne partagent JAMAIS un id — une dédup produit un statut terminal non-committed (P2-6) rendu nominativement (« vendredi n'est PAS posé »).
- Statut: fix_applied
- Fix reference: P12-A (résolution PAR item : parseur résout nativement le jour de semaine nu, fallback when_hint isolé + conditions de possession couche 3 ; invariant « jamais 2 committed même id » au merge + idempotence executor scopée à l'instant) + P12-V (distribution d'heure commune bornée)
- Tests requis: fan-out N jours nommés ⇒ N timestamps distincts vérifiés avant commit ; test intégration : 2 items même instruction, jours différents ⇒ 2 lignes DB ; anti-phantom : si l'exécuteur ne commit qu'1, le rendu nomme le volet manquant ; contrôle UTC=label par item.

## R1-B03 — Create additif coercé en replace destructif (2 occurrences) + rendu qui nie le cancel

- Bug id: R1-B03
- Tours: T9, T10
- Famille: BF-EFFECT-01 (cancel non consenti) + BF-INTAKE-03 (contrainte « tu gardes vendredi » perdue) + BF-LEDGER-02 (T10 : « Le rappel de vendredi reste tel quel » alors que son cancel est committé au même tour)
- Domaine owner: lane replace/reschedule one_shot_reminder (résolution de cible + filet anti-doublon R-2) ; composer (parité cancels)
- Source amont: (a) T9 : antécédent nommé « celui de vendredi » inexistant en pending ⇒ le replace retombe sur le pending lexicalement le plus proche (jeudi laverie) au lieu de dégrader en create (P0-4/P9-A non appliqués à la dimension jour) ; (b) T10 : marqueur additif explicite (« rajoute… et tu gardes celui de vendredi ») ignoré par le filet same-instruction qui cancel la cible protégée ; (c) le composer rend l'intention utilisateur, pas l'outcome ledger.
- Symptome visible: T9 détruit jeudi en « remettant » vendredi (phrase illisible « ne m'est pas revenu dans cet enregistrement ») ; T10 détruit vendredi en recréant jeudi tout en affirmant que vendredi est intact.
- Preuve systeme: committed T9 = cancel `d37195fe` + create `1cbfe4e5` ; committed T10 = cancel `1cbfe4e5` + create `938b56eb` ; DB vérifiée après chaque tour.
- Correction attendue: condition d'antécédent résoluble étendue au CRÉNEAU nommé (objet seul insuffisant) ⇒ sinon create pur ; marqueur additif/contrainte « garde Y » ⇒ le filet anti-doublon perd le droit de cancel (create ou clarify, jamais replace silencieux) ; invariant composer bidirectionnel : interdit d'affirmer l'intact d'une cible dont le cancel est committé au tour (extension P7-B).
- Statut: fix_applied
- Fix reference: P12-D3/D4 (marqueur additif ⇒ zéro cancel ; antécédent créneau nommé ⇒ create pur) + P12-C (interdit « reste tel quel » sur cancel committé — ensureCommittedRenderParity)
- Tests requis: « il manque X [jour B], remets-le » avec pending X [jour A] ⇒ create B + A intact ; « rajoute X [jour A] et garde X [jour B] » ⇒ zéro cancel ; paraphrases (« en plus », « aussi ») ; anti-faux-positif : « décale X à [jour B] » reste un replace ; test rendu : cancel committé ⇒ jamais « reste tel quel ».

## R1-B04 — Gate same_instruction_pending : option AJOUTER inexécutable (cul-de-sac) + template fuité

- Bug id: R1-B04
- Tours: T11 (yellow), T12 (red)
- Famille: BF-STATE-02 (réponse au pending clarify non consommée) + BF-INTAKE-03 (T11)
- Domaine owner: gate anti-doublon one_shot_reminder + reducer pending clarify + backstop de rendu
- Source amont: le gate bloque en `same_instruction_pending` et propose « DÉPLACER ou AJOUTER un deuxième » ; la branche AJOUTER n'a aucun chemin runtime (pas d'allow_duplicate), donc la réponse-option exacte re-déclenche le même blocage avec la même question verbatim, placeholder « [heure] » du template exposé. Leçon P9 (« toute ceinture porte sa condition de désarmement ») non appliquée ; symétrique du clarify méridiem pré-P7-C.
- Symptome visible: T11 redemande une contrainte déjà donnée (« c'est un ajout ») ; T12 boucle auto-contradictoire ; l'utilisateur ne s'en sort qu'en changeant le libellé (T13).
- Preuve systeme: blocked `same_instruction_pending` identique T11 et T12, 0 commit ; texte T12 avec « [heure] ».
- Correction attendue: (1) un clarify ne propose que des options exécutables — brancher `allow_duplicate` (create assumé du doublon) ou ne pas offrir AJOUTER ; (2) la réponse-option consomme le pending et désarme le gate (symétrie P5-D/P7-C) ; (3) marqueur additif au tour INITIAL (« n'annule rien, c'est un ajout ») pré-désarme le gate ; (4) purge des templates placeholder du chemin backstop.
- Statut: fix_applied
- Fix reference: P12-D2c (gate same_instruction arme son pending, options exécutables AJOUTER/DÉPLACER, purge placeholders)
- Tests requis: réponse « en ajouter un deuxième » ⇒ create committé, 2 pendings même instruction ; contrainte additive au tour initial ⇒ pas de clarify redondant ; anti-faux-positif : « remets-le à 19h » après le gate ⇒ replace ; invariant : aucun `[` placeholder `]` dans un texte visible.

## R1-B05 — Strip claim-sans-commit emporte la question de clarify (past_time)

- Bug id: R1-B05
- Tours: T2 (yellow)
- Famille: BF-LEDGER-02 (rendu d'outcome incomplet)
- Domaine owner: garde `commit_claim_stripped` (P9-C) × composer needs_clarify
- Source amont: sur blocked past_time, le composer a émis un verbe de commit ; la garde a strippé et le texte restant a perdu la relance « un autre horaire, ou demain ? » pourtant présente dans `visible_confirmation_hint`. L'état clarify est resté armé (T3 a fusionné correctement) — friction pure.
- Symptome visible: « L'heure demandée est déjà passée aujourd'hui, donc je n'ai rien créé. » sans question.
- Preuve systeme: `system_error_logs` guards `commit_claim_stripped` turn_id `ff044455` ; visible_confirmation_hint vs contenu rendu.
- Correction attendue: le fallback de la garde réécrit vers le texte d'outcome canonique (raison + clarify_question), invariant H3/O3 « clarify_question non vide ⇒ question visible » vérifié aussi sur le chemin strippé.
- Statut: fix_applied
- Fix reference: P12-G (strip P9-C ré-appende la question de la lane — run.ts)
- Tests requis: blocked past_time avec claim strippé ⇒ la question de clarify survit ; anti-régression : le strip continue de retirer le verbe de commit.

## R1-B06 — Clarify d'ambiguïté de cible non nominatif + jargon interne

- Bug id: R1-B06
- Tours: T14 (yellow)
- Famille: BF-INTAKE-04 (nuance : ambiguïté RECONNUE, clarify de qualité insuffisante)
- Domaine owner: composer du clarify reschedule (candidats de la projection)
- Source amont: 3 pendings en fenêtre soir ⇒ blocage correct sans choix au hasard (P10-E tenu), mais le clarify demande « l'intitulé exact » au lieu d'énumérer les candidats (exigence nominative P10-B) et fuit « le contexte visible » (vocabulaire système).
- Symptome visible: « le contexte visible ne dit pas quel rappel du soir tu vises. Donne-moi l'intitulé exact… »
- Preuve systeme: blocked `reschedule_not_supported` T14 ; projection T8/T16 dispose des candidats.
- Correction attendue: clarify nominatif par candidat de la fenêtre (« ce soir 20h grand-mère / jeudi 18h laverie / vendredi 18h linge ? ») ; bannir « contexte visible » du texte user-facing.
- Statut: fix_applied
- Fix reference: P12-D5 (clarify nominative énumérant les candidats, vocabulaire système banni)
- Tests requis: ambiguïté 2+ candidats ⇒ clarify liste chaque candidat (créneau+objet) ; réponse par désignation partielle ⇒ replace de la bonne cible (T15 déjà vert).

## Observabilité (trou noté, pas un bug de tour)

- T7 (accusé phantom) et T10 (déni de cancel) n'ont produit AUCUNE ligne `guards` : la parité actuelle est aveugle quand le ledger est faux (double committed même id) et ne couvre pas la négation d'un cancel committé. À fermer avec R1-B02/R1-B03 (les gardes doivent lire des invariants DB-groundés, pas seulement le ledger).
  → fermé par P12-C/P12-V (gardes commit_omitted_in_render, cancel_claim_without_commit, retracted_mention).
