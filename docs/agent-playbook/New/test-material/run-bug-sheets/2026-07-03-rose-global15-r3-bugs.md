# Rose — Global 15 — R3 — Feuille De Suivi Bugs

Rapport associé: `docs/agent-playbook/New/test-material/qa-run-reports/2026-07-03-rose-global15-r3.md`

- Familles dominantes: `BF-LEDGER-02` (commit réel mal rendu, **1 red**), `BF-ROUTE-01` (mauvais owner, 3 occurrences), `BF-EFFECT-03` (payload/id durable faux, 2 occurrences), `BF-STATUS-01`/`BF-STATUS-02` (projection recap, 2 occurrences), `BF-ROUTE-02` (flow résiduel, 1 occurrence). `BF-EFFECT-04` (double-exécution) est la cause racine du red T7.
- 1 tour `red` (T7). Le red est un bug **système reproductible** (aussi observé lors du run `web` contaminé avant relance isolée), pas un aléa.
- Régression **résolue confirmée** : le bug compteur `BF-EFFECT-03`/`R2-W01`/`rose-global15-r2-B01/B02` (`current_reps` non incrémenté) n'est plus reproduit — `current_reps` passe 0→1 sur les deux habitudes (T1, T2). Le fix `planItemPatchForCompletedEntry` de r2 tient.
- Incidents non-bug documentés : isolation inter-session (session QA concurrente sur scope `web` + mémoire per-user contaminée) ; 3× HTTP 502 gateway transitoires (retry OK).

| Bug id | Tours | Famille | Domaine owner | Source amont | Symptome visible | Preuve systeme | Correction attendue | Statut | Fix reference | Tests requis |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `rose-global15-r3-B01` | T7 | `BF-LEDGER-02` (+ cause `BF-EFFECT-04`) | `sophia-brain/tools/always_on/one_shot_reminder/executor.ts` (`executeOneShotReminder`, garde anti-dup L365-390) + point d'appel direct-effects (`router.ts` / pipeline) | Executor `create_one_shot_reminder` exécuté **deux fois** dans le tour : passe 1 crée la ligne, passe 2 relit la table et bloque sur sa propre écriture (`duplicate_pending`, match sur `scheduled_for` seul, L375-378). Le rendu final utilise l'outcome bloqué. | Sur une 1re demande de rappel, Sophia répond « Tu as déjà un rappel identique en attente… je ne le recrée pas » (fausse confirmation) | `scheduled_checkins` `06fcc40c` **committée** en DB (`scheduled_for=2026-07-03T07:15:00Z`, wording exact du user, `created_at` pile le tour, 1 seule ligne) MAIS `effect_ledger`: `requested`→`blocked/duplicate_pending`, `committed_id=null`, `counts.committed=0`. Corroboré T8 (« je vois bien qu'il existe »). | (1) Idempotence : exécuter l'effet une seule fois par tour (clé sur `source_message_id`/`requestId`) pour que la passe 2 ne relise pas sa propre écriture. (2) Garde anti-dup : matcher sur instant **+ contenu/idempotency key**, pas `scheduled_for` seul. (3) Contrat de rendu (`direct_effect_local_context.ts`) : n'annoncer « déjà existant » que sur une preuve de doublon **antérieure au tour**, jamais sur la ligne committée ce tour. | `open` | — | Positif: 1re demande de rappel neuf -> 1 ligne + confirmation de **création** (jamais « déjà existant »). Idempotence: un même tour n'exécute l'effet qu'une fois. Anti-collision: 2 rappels distincts à la même minute -> les 2 créés (pas de faux `duplicate_pending`). Intégration: assertion DB + ledger cohérents après `test-send-message` réel. |
| `rose-global15-r3-B02` | T4, T9 | `BF-EFFECT-03` | `sophia-brain/agents/companion.ts` / builder `action_context` du skill `coaching_recommendation` | Le `plan_item_id` de `action_context` est produit en sortie structurée LLM sans validation contre les ids réels du plan | Aucun côté user (titre d'action correct dans la réponse) | `skill_signals.coaching_recommendation.context.action_context.plan_item_id = "3db6278b-d744-4fc6-a7ed-fdccb195447a"` = UUID inexistant (préfixe item sas `3db6278b` + suffixe item journée `-d744-...`). Récurrent T4 et T9. | Résolution **déterministe** du `plan_item_id` : le LLM ne renvoie qu'un sélecteur (titre/index) validé contre la liste des items du plan ; l'id final est mappé côté déterministe (cf. `00-architecture-doctrine.md`, routing déterministe depuis sorties structurées). | `open` | — | `action_context.plan_item_id` ∈ {ids réels du plan actif} ; incohérence titre/id impossible ; test sur un plan à ≥2 items avec préfixes/suffixes proches. |
| `rose-global15-r3-B03` | T12 | `BF-STATUS-01` | status/recap projection (`sophia-brain/router/plan_snapshot_runtime.ts` ou builder recap plan) | La projection de recap ne lit pas l'état plan réel (`current_reps`/`user_plan_item_entries`) et se rabat sur le fil conversationnel | Recap « où j'en suis » ne cite aucune progression committée (journée 100% 1/2, sas 1/3) et liste le sas — loggé fait au T2 — comme « Prévu ce soir » | 2 entries + `current_reps` corrects en DB au moment du tour, absents du recap ; item committé présenté comme non fait | Construire le recap depuis l'état plan réel (items, `current_reps`/`target_reps`, entries récentes) ; un item loggé fait ce tour ne doit pas réapparaître en « prévu ». | `open` | — | Recap positif (progrès committé cette session cité avec compteurs) vs recap sans progrès (ne rien inventer) ; item loggé fait -> jamais « prévu ». |
| `rose-global15-r3-B04` | T5 | `BF-ROUTE-01` | dispatcher/route policy (flow actif vs item de plan nommé) + registre tool skills framework | Un flow `coaching_recommendation` actif (issu de T4) préempte l'activation du framework de plan explicitement nommé | Framework `Cartographier les envies` demandé « maintenant » -> réponse texte générique, pas d'intake structuré | `route_reason=active_coaching_recommendation`, `skill_signals={}`, aucune activation du framework `525c8e06` | Avant qu'un flow actif générique ne capte le tour, vérifier si le user nomme un item de plan actif avec skill dédié et prioriser ce skill ; si le framework n'a pas de skill structuré, câbler la couverture. | `open` | — | Positif: item framework de plan nommé -> activation du skill dédié même sous flow coaching actif. Anti-faux-positif: mention vague sans item -> flow courant conservé. |
| `rose-global15-r3-B05` | T9 | `BF-ROUTE-01` | dispatcher/route policy (accueil émotionnel vs pitch technique) + contrat `coaching_recommendation` | Sur culpabilité aiguë (safety `low` + auto-attaque), owner `coaching_recommendation` en technique-first sans accueil émotionnel préalable | « Je m'en veux à mort / repartir à zéro » -> réponse ouvre directement sur « Je partirais sur une carte de défense… » | `response_owner=coaching_recommendation`, `safety.risk_band=low` ; aucun accueil émotionnel avant la technique | Imposer une séquence accueil émotionnel -> puis technique sur signal émotionnel/culpabilité aigu, ou router d'abord vers l'owner de soutien. | `open` | — | Message émotionnel/culpabilité aigu -> réponse ouvre par validation émotionnelle avant toute proposition de carte/technique. |
| `rose-global15-r3-B06` | T13 | `BF-STATUS-02` | status projection des rappels (fenêtre/limite) + planner `memory_plan` (cohérence note d'intent) | Le builder « rappels en attente » applique une borne d'horizon non exposée ; la note d'intent nie la présence de la liste fournie | « liste **tous** mes rappels » -> 5/6 pending listés (omis le rappel à +2 jours) ; conversions horaires exactes | DB=6 pending, réponse=5 (omis `e1605e47` 08:40 le 07-04) ; `memory_plan.intent` dit « le contexte ne contient pas de liste exploitable » alors qu'une liste exacte est produite | Sur « tous mes rappels », lister l'ensemble des pending ou annoncer explicitement la borne d'horizon ; aligner la note d'intent sur la présence réelle de la liste. | **éliminé** (triage concurrence 2026-07-03) — l'item omis appartient à une session concurrente ; le pattern réel de troncature est suivi via `rose-global15-r1` R1-B02 (où un rappel créé EN séance est omis) | — | « tous mes rappels » -> liste complète (ou borne annoncée) ; note d'intent cohérente avec le contexte injecté. |
| `rose-global15-r3-B07` | T14 | `BF-ROUTE-01` | dispatcher/route policy (`feature_opportunity` vs tool `update_coach_preferences`) | Demande explicite d'enregistrer une préférence de ton routée en `feature_opportunity` (pointeur UI), aucune persistance | « Tu peux enregistrer cette préférence ? » -> « ça relève de tes Préférences coach… je vais déjà te répondre comme ça » | `response_owner=feature_opportunity`, ledger vide, aucun tool `update_coach_preferences` | Si l'écriture de préférence est supportée en tool, router la demande explicite d'enregistrement vers ce tool (avec confirmation si engageant) ; si UI-only par design, le dire clairement (« je ne peux pas l'enregistrer d'ici »). | resolved_by_design (confirmé 2026-07-03) — le write-skill `update_coach_preferences` a été retiré en V2, le redirect Preferences coach est le design assumé (décision du 2026-07-02, en-tête STATUT V2 dans `update-coach-preferences.md`) ; le wording honnête « je vais déjà te répondre comme ça » correspond à la doctrine anti-répétition livrée | — | Demande explicite de persistance de préférence -> écriture durable (si supportée) ou refus clair sur la capacité ; pas de pointeur ambigu laissant croire à une action système. |
| `rose-global15-r3-B08` | T15 | `BF-ROUTE-02` | active flow / interruption policy | Le flow `feature_opportunity` (ouvert T14) n'est pas relâché sur un message de clôture neutre | « Bonne nuit » -> `response_owner=feature_opportunity` (`active_feature_opportunity`) | Réponse visible correcte (clôture propre) mais owner faux ; `should_exit_flows` non déclenché | Un message de clôture/neutre doit relâcher un flow `feature_opportunity` actif et retomber sur `normal_reply`. | `open` (impact utilisateur nul, trace faussée) | — | Message de clôture après un flow `feature_opportunity` actif -> owner `normal_reply`, flow relâché. |

## Suivi Des Décisions Architecturales

| Date | Décision | Statut | Référence |
| --- | --- | --- | --- |
| 2026-07-03 | Confirmation que le fix compteur `current_reps` (`rose-global15-r2-B01/B02`, `BF-EFFECT-03`/`R2-W01`, `planItemPatchForCompletedEntry`) est **effectif** : `current_reps` passe 0→1 sur les deux habitudes via le chemin conversationnel réel (T1 count, T2 boolean). Le symptôme de recap dérivé (`rose-global15-r2-B03`, `BF-STATUS-01`) reste néanmoins présent sous une autre forme (T12) : la projection recap ignore désormais un compteur pourtant correct → la source du recap est bien une projection, pas seulement le compteur amont. | Active | rose-global15-r3 |
| 2026-07-03 | Nouveau red système `BF-LEDGER-02`/`BF-EFFECT-04` : double-exécution de `create_one_shot_reminder` produisant une fausse confirmation de doublon sur un rappel réellement committé. Reproductible (observé aussi sur run `web` contaminé). Priorité de fix la plus haute du run. | Active | rose-global15-r3 |
| 2026-07-03 | Incident d'isolation inter-session confirmé (2e signalement après r2) : session QA concurrente sur compte + scope `web` partagés de Rose, contaminant historique conversationnel ET `memory_items` (memorizer per-user). Recommandation : connexion + scope temporaires dédiés obligatoires par run. | Active | rose-global15-r3 |
