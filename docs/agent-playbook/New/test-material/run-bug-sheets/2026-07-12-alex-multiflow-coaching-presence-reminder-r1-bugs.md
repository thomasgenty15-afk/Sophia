# Run Bug Sheet - alex-multiflow-cpr-2026-07-12-r1

## Metadata

- Date: 2026-07-12
- Run report: `docs/agent-playbook/New/test-material/qa-run-reports/2026-07-12-alex-multiflow-coaching-presence-reminder-r1.md`
- Run id: `alex-multiflow-cpr-2026-07-12-r1` (scenario 15.1)
- Persona / scenario: Alex — multiflow coaching / presence / normal + one-shot reminder (15 tours)
- Verdict run: red
- Validite QA: valide (15 tours IA reels, `POST /functions/v1/test-send-message` + `force_full_ai=true`, trace lue en direct par tour, history pilotee explicitement, verifs DB par `user_id`). Incident env: runs QA concurrents sur le meme Supabase local (scopes `qa-rose-*`) — verifications isolees par `user_id`, lectures live.
- Agent owner: QA agent (Claude)

## Synthese

- Familles dominantes: `BF-LEDGER-01` (rappel confirme sans commit, chemin sortie-de-flow) ; `BF-PREF-01` (engagement de style session non installe / non tenu par presence).
- Bug le plus bloquant: `ALEX-CPR-B01` — `create_one_shot_reminder` demande pendant la sortie du flow presence renvoie un `committed_effects` avec id fantome et confirme au user, **sans persister** de ligne `scheduled_checkins`. Effet durable faux + confirmation ratee.
- Ecartement de l'incident environnement (point clef vs Eva r1): A/B controle dans le **meme** environnement concurrent — un rappel en contexte `normal_reply` **pur** (probe isole scope `diag`, `id=8760d754…`) **persiste et est verifiable par id immediatement** ; le rappel sur chemin sortie-de-flow (T6) n'est jamais trouvable ; reproduit au T8 (hors flow → persiste). Le differenciateur est le **chemin d'execution du direct-effect apres sortie de flow local**, pas la concurrence.
- Fix architectural prioritaire: cohérence ledger↔writer du direct-effect rappel sur le chemin presence-exit reroute / direct-effect lane (`run.ts`). Un `committed create_one_shot_reminder` doit impliquer une ligne DB verifiee ; sinon `insert_failed` honnete.
- Rerun requis: oui apres fix (variante N.2: rappel demande pendant coaching actif ET pendant presence active, verif ligne DB par id juste apres le tour).

## Bug Ledger

| Bug id | Tours | Famille | Domaine owner | Source amont | Symptome visible | Preuve systeme | Correction attendue | Statut | Fix reference | Tests requis |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `ALEX-CPR-B01` | T6 | `BF-LEDGER-01` (+ `BF-EFFECT-02`) | router direct-effect lane / effect ledger — `sophia-brain/router/run.ts` (presence exit reroute ~L1121-1140 ; direct-effect lane pre-loop ~L1283 / reexec ~L1325-1365) | Sur sortie de flow local (presence `tool_pull`), le direct-effect `create_one_shot_reminder` produit un `committed_effects.id` sans upsert `scheduled_checkins` reellement lande (commit fantome) ; le garde final « claim sans commit » ne rattrape pas car le ledger porte un id committed | « Oui — ce soir a 22h30, je te le rappellerai… » (confirmation) alors qu'aucun rappel n'existe | trace T6: `tool_skill_run=create_one_shot_reminder`, `status=success`, `committed_effects=[{id:4c2ef032…, scheduled_for:2026-07-12T20:30:00Z}]` ; DB Alex: 0 ligne par `id`, 0 `event_context like 'one_shot_reminder:%'`, count inchange (14), aucun log `insert_failed`. A/B: probe plain `diag` `8760d754…` persiste (count 14→15) ; T8 hors flow persiste (`adf444e7…`) | `committed_effects(create_one_shot_reminder)` lie a un upsert reellement retourne/persiste du meme writer ; sinon `insert_failed` + relance honnete ; garde final: interdire un committed rappel sans ligne DB lisible par id | `fix_applied` (2026-07-12, chantier P0-1) | Double fix source : (1) la branche EXIT des 3 flows locaux (coaching/feature/plan_realignment, `skill.ts`) exécute le `direct_effect_request` AVANT le return — le writer n'est plus court-circuité au tour de sortie ; (2) write-through du contexte de confirmation : `loadRecentDirectEffectConfirmationContext` (`context/loader.ts`) REJETTE un committed du ledger dont la ligne DB n'existe plus (id introuvable) — la fabrique du « c'est noté » fantôme est fermée. Tests unitaires (exit→executor appelé, anti-FP sans request) + probe A/B live rejouée 2× GREEN : rappel au tour de sortie presence→tool_pull PERSISTE (id committed = ligne DB lisible), contrôle plain PERSISTE, zéro committed sans ligne | (1) rappel demande pendant presence/coaching actif → ligne `scheduled_checkins` lisible par `id` juste apres le tour ; (2) `committed create_one_shot_reminder` ⇒ jamais 0 ligne ; (3) A/B plain vs sortie-de-flow persistent tous deux |
| `ALEX-CPR-B02` | T7 | `BF-STATUS-02` | status renderer / final response pipeline | Renderer de statut reutilise la formulation d'engagement de l'historique T6 (confirmation fantome) malgre `pending_count=0` | « Oui, pour ce soir a 22h30 : aucun rappel ponctuel n'est en attente en ce moment. » (auto-contradictoire) | trace T7: `one_shot_reminder_status`, `committed_effects=[{pending_count:0}]` ; DB `pending_oneshot=0` | Quand `pending_count=0`, rendu univoque sans ré-affirmer un horaire issu de l'historique ; racine = supprimer le commit fantome T6 | `fix_applied` (racine P0-1, 2026-07-12) | La racine (commit fantôme T6) est supprimée (write-through + exit-executor) ; la guidance status interdit déjà de ré-affirmer un horaire hors liste (« un rappel ABSENT de la liste n'est plus actif », anti-fusion). Rejeu attendu au prochain run | statut `pending_count=0` → aucun rendu affirmant un horaire de rappel |
| `ALEX-CPR-B03` | T8 | `BF-LEDGER-02` | renderer create one-shot / final response pipeline | Composeur influence par l'historique (T6/T7) rend une creation fraiche (`reason_code=payload`) comme « deja prevu » | « C'est deja prevu pour ce soir a 22h30 : preparer ton carnet. » alors que c'est la 1re creation reelle | trace T8: `committed id=adf444e7…`, `reason=payload` ; DB: nouvelle ligne `pending` (count 14→15) | Rendu de creation fidele au `reason_code` (payload=fraiche → « c'est note / je te le rappellerai »), decouple de l'historique | `fix_applied` (racine P0-1, 2026-07-12) | Racine supprimée (plus d'historique pollué par une confirmation fantôme) ; la guidance committed impose déjà « comme venant d'être fait… never present it as pre-existing ». Rejeu attendu au prochain run | creation fraiche (`reason_code=payload`) → jamais rendue « deja prevu » |
| `ALEX-CPR-B04` | T12, T14 | `BF-PREF-01` | preference runtime policy + composeurs (presence) | Preference de style durable exprimee en chat: aucun `__session_style_commitments` installe (verifie absent post-tour) → style non tenu ; reponse promet « je retiens » sans honnetete durabilite ni renvoi Preferences | T12 « Oui, je retiens… ✅ » (promesse de persistance nue) ; T14 (presence, soir) repond en long pave malgre « sois plus courte le soir » | `temp_memory.web` sans `__session_style_commitments` apres T12 ; T14 owner `presence_conversation`, message long ~20h Paris | Installer un engagement de style session (tenu par tous les composeurs, presence comprise) + reponse 3 volets (applique / honnetete « reglage durable pas encore depuis le chat » / renvoi Preferences) ; pas d'ecriture `user_relation_preferences` (won't-fix acte) | `fix_applied` (2026-07-12, chantier P1-2) | Champ RACINE `session_style_commitment_hint` du TurnFrame (survit meme sans signal skill detecte) + regle dispatcher (capture quel que soit l'owner) + installation runtime `installSessionStyleCommitment` (session only, fenetre 3, dedup) RE-APPLIQUEE post-generation (le companion reconstruisait temp_memory depuis l'etat pre-routing et perdait la cle) + bloc CONTRAINTE DE STYLE SESSION injecte aussi dans le contexte presence (l'allowlist le retirait). Volet « reponse 3 volets » (honnetete durabilite + renvoi Preferences) reste ouvert cote composeurs. Probe live 12/07 : `__session_style_commitments` present en DB post-tour, zero emoji tenu N+2 tours (2 passes GREEN) | engagement style session tenu N tours et a travers presence/coaching ; pas de promesse « pour toujours » nue |

## Watch-points (non-bug ferme ce run)

| Id | Tours | Famille candidate | Observation | Suivi |
| --- | --- | --- | --- | --- |
| `ALEX-CPR-W01` | tout le run | environnement | Runs QA concurrents sur le meme Supabase local (`qa-rose-multiflow-2026-07-12-r1` = 30 msgs/30min, `qa-rose-diag*`, users tiers). `scheduled_checkins` volatil, lignes tierces apparaissant/disparaissant en cours de lecture. N'affecte PAS les findings (verifs isolees par `user_id`, history pilotee explicitement). | Isolation par connexion temporaire / DB dediee par run ; verifier qu'aucun cleanup concurrent ne cible des rows recentes |
| `ALEX-CPR-W02` | T6 (cross-run) | `BF-LEDGER-01` | Le run Eva r1 (`2026-07-12-eva-…`) a observe le meme « rappel committed puis ligne disparue » (T6 `2e7e8755…`) mais l'a classe **incident environnement** (decision du 2026-07-12). Le present A/B controle suggere un **meme bug produit sous-diagnostique** (rappel pendant flow local actif → non persiste). | Reouvrir la classification T6 d'Eva r1 a la lumiere de `ALEX-CPR-B01` ; corriger le writer, pas seulement l'env |

## Decisions / Arbitrages

| Date | Decision | Pourquoi | Owner | Reference |
| --- | --- | --- | --- | --- |
| 2026-07-12 | T6 classe **bug produit** (`ALEX-CPR-B01`), pas incident env | A/B controle: meme env concurrent, probe plain persiste et verifiable par id, chemin sortie-de-flow non ; reproduit 2x | QA agent | report §Tour 6 |
| 2026-07-12 | T13 accuse memoire = comportement attendu | Intention memoire (fait personnel) → « c'est note » sans write in-turn ; ecriture portee par memorizer nocturne | QA agent | report §Tour 13 ; guidelines Memoire |
| 2026-07-12 | T11 renvoi produit = attendu | Aucun executor appele depuis le chat ; carte de defense materialisee cote produit (Dashboard) | QA agent | report §Tour 11 ; doctrine architecture |

## Verification

| Date | Bug id | Verification | Resultat | Reference |
| --- | --- | --- | --- | --- |
| 2026-07-12 | ALEX-CPR-B01 | DB `scheduled_checkins` par `id=4c2ef032…`, par `event_context`, count Alex, logs edge, juste apres T6 | Confirme: 0 ligne, count inchange (14), aucun `insert_failed` | psql superuser + PostgREST service-role |
| 2026-07-12 | ALEX-CPR-B01 | A/B: rappel en contexte `normal_reply` pur (probe scope `diag`) | Persiste (`8760d754…`, count 14→15), verifiable par id ; nettoye ensuite | DB local |
| 2026-07-12 | ALEX-CPR-B01 | Reprise hors flow au T8 | Persiste (`adf444e7…`, `pending`, 2026-07-12T20:30:00Z, instruction correcte) ; nettoye en fin de run | DB local |
| 2026-07-12 | ALEX-CPR-B02 | Statut lu au T7 | `pending_count=0` correct, rendu contradictoire | trace live + DB |
| 2026-07-12 | ALEX-CPR-B04 | `temp_memory.web` apres T12 | `__session_style_commitments` absent ; T14 long en soiree | DB local + trace |
| 2026-07-12 | (reset) | Effets du run nettoyes | `chat_messages=92` (today_msgs=0), `pending=0`, aucun checkin cree par le run, `memory_items=48`, pas de `__active_skill_state`, scopes probe supprimes | DB local |
| 2026-07-12 | (env) | 14 checkins pre-existants (potion annules 07-10) purges pendant le run (14→0) | Non cause par ce run (delete cible `adf444e7…` seul) ; incident env concurrent (W01) | DB local |
