# Bug Sheet — Rose Global15 Multiflow — R1 (2026-07-03)

Rapport source: `docs/agent-playbook/New/test-material/qa-run-reports/2026-07-03-rose-global15-multiflow-r1.md`
Persona: Rose (`02dc9ae2-4128-412b-b0be-56712bf775a8`) · Scope dédié `qa-g15-rose-20260703-r1` · IA réelle locale, `force_full_ai=true`.
Réserve: run mené sous contention de ≥4 runs concurrents sur la même user ; effets durables attribués par `source_message_id`/slug unique.

| Tour | Famille | Owner runtime | Source amont probable | Symptôme | Correction recommandée | Tests d'invariant | Statut |
| --- | --- | --- | --- | --- | --- | --- | --- |
| T2 | BF-ROUTE-02 (+ BF-LEDGER-01) | dispatcher / active_flow policy + track_progress | Flow `coaching_recommendation` sticky avale le report d'action ; `action_reference=none` (item non résolu) ; item = *task* one-off | Report « soirée zéro vide faite » accusé verbalement, **aucun** commit track_progress (aucune entry avec `source_message_id`) | Relâcher le flow coaching sur un report d'action ; router vers track_progress ; étendre `router_parallel_tracking_v2` aux completions de *tasks* | Report d'action sur item identifiable ⇒ owner track_progress + entry DB (ou accusé honnête « check-in enregistré ») | open |
| T3 | BF-ROUTE-02 (+ BF-LEDGER-01) | active_flow policy + track_progress | Flow coaching sticky ; handler coaching confabule une limite (« never executes tools ») | Ordre **explicite** « marque comme fait » sur *habit* → Sophia **nie la capacité** (« je ne peux pas depuis le chat »), 0 commit | Prioriser un report d'action explicite au-dessus du flow coaching ; supprimer le message de déni faux | « marque comme fait » sur item identifiable ⇒ jamais « je ne peux pas depuis le chat » ; commit ou accusé de check-in | open |
| T6 | BF-EFFECT-03 | direct-effect time resolver / intake temporel | « ce soir » ancré au calendrier de la veille en petites heures (02:07) | Rappel « ce soir 23h » résolu à `2026-07-02T21:00Z` (passé) → blocked ; offre « demain » viserait le mauvais jour | Post-minuit + ancre « ce soir » ⇒ résoudre au soir du jour civil courant, sinon lever ambiguïté (BF-INTAKE-04) | « ce soir » à 02:00 ⇒ datetime futur du jour courant, jamais passé | open |
| T7 | BF-LEDGER-02 | EffectLedger / executor admission `create_one_shot_reminder` | Écriture pending avant garde `duplicate_pending` (auto-collision) | Rappel `preparer_ma_journee` (07:00Z) committé en DB (`ce113666`) mais rendu `blocked/duplicate` — attribution ambiguë (slug générique) | (voir T8) | fusionné avec `rose-global15-r3-B01` (auto-collision double-exécution) ; part concurrence non exclue |
| T8 | BF-LEDGER-02 (mécanisme BF-EFFECT-04) | EffectLedger / executor admission + final response guard | Le stage `requested` persiste la ligne pending ; le garde `duplicate_pending` de l'executor matche **sa propre écriture in-flight** → `committed_id=null` | Slug **unique** « zenitude turquoise » committé en DB (`4c8d6ef3`, 13:23Z), **une seule ligne, aucun pré-existant**, mais rendu « déjà présent, je ne peux pas en recréer » | Dedup **avant** persistance, ou exclure la ligne du turn courant ; rendu piloté par `committed_id`/état DB | 1 seule ligne pending matchée (celle du turn) ⇒ `committed`, jamais `blocked/duplicate_pending` ; `committed_id!=null` ⇒ confirmation positive | open |
| T13 | BF-LEDGER-02 + BF-EFFECT-03 | EffectLedger/executor + intake type de rappel | Auto-collision (voir T8) ; extraction récurrent aplatie en one-shot | Demande **récurrente** classée `create_one_shot_reminder` (mono-datetime), committée silencieusement (`fa29cdd2`, 21:45Z, slug unique) alors que Sophia redirige vers Initiatives ; absente du recap T14 | Ne pas émettre de one-shot quand `feature_opportunity` redirige un récurrent vers Initiatives ; corriger l'auto-collision | Récurrent redirigé vers Initiatives ⇒ 0 `create_one_shot_reminder` ; `committed_id!=null` ⇒ visible dans recap | open |
| T14 | BF-STATUS-02 | status / recap projection | Recap incomplet ; écritures concurrentes pendant la requête | Rappels pending omis (dont T13 « poser mon téléphone » committé mais invisible) ; expose aussi des rappels de runs concurrents | Recap exhaustif sur les pending de la user+scope ; garantir la présence des rappels `committed_id!=null` | Tout rappel pending `committed` apparaît dans le recap | **éliminé** (triage concurrence 2026-07-03) — les omissions/expositions constatées impliquent les écritures de runs parallèles ; le pattern réel de troncature (~5 sans annonce) est suivi via `rose-global15-r1` R1-B02 |

## Notes transverses (hors BF Sophia)

- **Environnement — isolation par run (bloquant pour la vérif des effets durables).** ≥4 runs QA concurrents sur la **même user Rose** (`web`, `qa-rose-broad15`, `qa-rose-g15-r3-iso`, `qa-rose-global15`). Mutations partagées sur `scheduled_checkins`, `user_plan_items`, mémoire. Recommandation: une **connexion/user dédiée par run** (ou étendre `qa-create-run-connection.sh` à une persona clonée avec plan), pas seulement un scope. Sans ça, la vérif mémoire batch et l'attribution des rappels restent fragiles.
- **Environnement — instabilité upstream.** ~6 `502 {"An invalid response was received from the upstream server"}` (edge runtime rechargé sous contention LLM). Retentés après vérif d'absence d'effet + suppression de l'orphelin. Non maquillés en fallback.

## Nettoyage effectué (fin de run)

- Supprimés (uniquement attribuables): `scheduled_checkins` `4c8d6ef3` (T8 zenitude), `fa29cdd2` (T13 téléphone) ; 30 `chat_messages` + `user_chat_states` du scope `qa-g15-rose-20260703-r1`.
- Laissé en place (attribution ambiguë, verify-before-delete): `scheduled_checkins` `ce113666` (T7 « préparer ma journée sans fumer »).
- Non déclenché: memorizer daily (aurait ingéré les tours des runs concurrents) → vérif `memory_items` reportée.
