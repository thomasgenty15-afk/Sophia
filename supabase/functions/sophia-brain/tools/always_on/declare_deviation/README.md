KEEL W4.3 — durable effect `declare_deviation`: writes one row into
`planned_deviations`. Flex is declared IN ADVANCE; the rule and its three
named disarm conditions live in `advance_rule.ts`. Runtime wiring is W4.4.
