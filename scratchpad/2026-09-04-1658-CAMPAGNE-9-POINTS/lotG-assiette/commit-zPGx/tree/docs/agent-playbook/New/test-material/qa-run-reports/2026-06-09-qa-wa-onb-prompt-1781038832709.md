# QA Run — WhatsApp Onboarding Local Dispatcher Prompt

## 1. Contexte Du Test

- Date: 2026-06-09
- Run: `qa-wa-onb-prompt-1781038832709`
- Persona: connexion temporaire locale `qa-wa-onb-prompt-1781038832709@example.com`, user id `4c0983f0-f24a-4869-b571-8900cee67330`
- Objectif: verifier en conditions reelles que le prompt renforce du dispatcher local WhatsApp onboarding conserve une preference nuancee sans faux exit.
- Trajectoire: user en `onboarding_pref_challenge`, plan actif, message de preference nuancee: challenge normal mais plus direct si esquive.
- Surfaces visees: `whatsapp-webhook`, `whatsapp_onboarding.local_dispatcher`, reducer local, visible agent, preferences runtime, absence de `track_progress_plan_item`.
- Cadre IA reel: Supabase local, webhook WhatsApp local en transport `loopback`; deviation documentee car `/functions/v1/test-send-message` ne traverse pas le dispatcher WhatsApp onboarding cible.
- Validite QA: invalide / red. Le webhook a renvoye `503 BOOT_ERROR` avant toute reponse Sophia.

Incidents de setup avant run exploitable:
- `qa-wa-onb-prompt-1781038747488`: fixture invalide, enum `habit_state=active`.
- `qa-wa-onb-prompt-1781038774141`: fixture invalide, enum `tracking_type=binary`.
- `qa-wa-onb-prompt-1781038812023`: fixture invalide, enum `plan_dimension=tasks`.
- Ces essais n'ont pas appele Sophia. Le runner temporaire a ete corrige, puis le run `qa-wa-onb-prompt-1781038832709` a atteint le webhook.

Nettoyage:
- Nettoyage cible des profils `qa-wa-onb-prompt-*` execute apres le run.
- Derniere verification de nettoyage: aucun profil QA correspondant restant.

## 2. Tours De Conversation

### Tour 1

**Verdict du tour:** red

**Famille de bugs si yellow/red:** BF-TEST-01 — Trace/test incoherent ou suite malsaine

**User**
> Pour le niveau de challenge, garde un truc normal, mais si je commence a esquiver sois plus directe.

**Sophia**
> Aucune reponse Sophia. Le webhook a retourne `503 BOOT_ERROR`.

**Trace courte**
- http_status: `503`
- response_owner: aucun, worker non demarre
- selected_handler: aucun, boot avant routing
- route_reason: non disponible
- safety: non evalue
- direct_effects: aucun outbound, aucun `chat_messages`, aucun `user_plan_item_entries`
- operation: aucune
- pending_confirmation: aucune
- memory_plan: non evalue
- executed_tools: aucun
- durable_effect: aucun effet durable observe
- runtime_error: `Worker failed to boot`

**Analyse si yellow/red**
- Symptome: le worker `whatsapp-webhook` ne boote pas, donc le run reel ne peut pas atteindre `whatsapp_onboarding.local_dispatcher`.
- Source amont probable: import runtime casse dans le webhook.
- Owner runtime: `supabase/functions/whatsapp-webhook`.
- Preuve: logs Edge Runtime: `handlers_optin_bilan.ts` importe `analyzeSignalsV2` depuis `../sophia-brain/router/dispatcher.ts`, mais cet export n'existe pas.
- Confirmation statique: `deno check supabase/functions/whatsapp-webhook/index.ts` echoue sur `TS2305: no exported member 'analyzeSignalsV2'`.
- Meilleure correction selon les guidelines: restaurer un export compatible ou migrer les imports WhatsApp vers l'API dispatcher actuelle, puis ajouter un check d'integration/boot du webhook avant les QA conversationnels WhatsApp.
- Pourquoi ce n'est pas un patch local: le probleme empeche toute activation du worker; corriger une phrase ou un prompt local onboarding ne peut pas rendre le chemin QA reel valide.

## 3. Analyse De Fluidite Humaine

**Verdict: red**

**Ce qui marche**
- Rien de conversationnel n'a pu etre evalue: Sophia n'a pas repondu.

**Problemes**
- Tour 1: absence totale de reponse utilisateur a cause du boot error. Famille: BF-TEST-01. Impact: experience inutilisable. Severite: red.

**Fix propose**
- Source amont: imports et boot du webhook WhatsApp.
- Correction recommandee: remettre le webhook dans un etat bootable, notamment l'import `analyzeSignalsV2`, puis relancer le meme scenario avec IA reelle.
- Tests d'invariant attendus: boot local du webhook, appel loopback `200`, outbound WhatsApp cree, `selected_handler=whatsapp_onboarding`, aucun progress item logge.

## 4. Analyse Systeme

**Verdict: red**

**Routage**
- Aucun routage effectif: l'Edge Function echoue au boot.
- Le dispatcher local onboarding n'a pas ete appele.
- Le dispatcher global n'a pas ete appele non plus.

**Skills / Operations / Tools**
- Aucun skill, tool ou visible agent n'a ete execute.
- Le transport WhatsApp loopback n'a pas eu l'occasion de simuler l'envoi.

**Memory / Effets durables**
- Profil, plan et state QA ont ete crees pour le run, puis nettoyes.
- Aucune progression de plan n'a ete loggee.
- Aucun outbound WhatsApp n'a ete cree.

**Problemes**
- Tour 1: worker boot error. Famille: BF-TEST-01. Impact systeme: impossible de verifier le nouveau prompt en conditions reelles. Severite: red.

**Fix propose**
- Source amont: contrat entre `whatsapp-webhook` et `sophia-brain/router/dispatcher.ts`.
- Correction recommandee: aligner l'import `analyzeSignalsV2` avec les exports reels du dispatcher ou reintroduire un wrapper exporte compatible, puis couvrir le boot du webhook par `deno check` cible et un appel loopback minimal.
- Tests d'invariant attendus: `deno check supabase/functions/whatsapp-webhook/index.ts` sans erreur bloquante, webhook local `loopback` repond `200`, puis run onboarding pref nuancee produit une reponse visible et garde le flow local.

## Verdict Global

- Verdict: red
- Raison principale: le webhook WhatsApp ne boote pas (`analyzeSignalsV2` importe depuis un module qui ne l'exporte pas), donc le run IA reel est invalide.
- Follow-up prioritaire: corriger le boot du webhook, puis relancer exactement ce QA avec une fixture locale propre.
