# Bug Sheet — Nina hard22 r1 (2026-07-14)

Run: `nina-hard22-r1` — 15 tours, mode difficile, Nina (web), chemin IA réel local, `force_full_ai=true`.
Rapport: `docs/agent-playbook/New/test-material/qa-run-reports/2026-07-14-nina-hard22-r1.md`.

## Lignes de bug

| Tour | Famille BF | Owner runtime | Source amont probable | Correction recommandée | Tests d'invariant attendus | Statut |
|------|-----------|---------------|-----------------------|------------------------|----------------------------|--------|
| T15 | `BF-STATUS-01` (aggravant `BF-LEDGER-01`) | status projection / final response LEDGER guard | Verify adopte le cadre utilisateur (« 2 cartes enregistrées ») au lieu de projeter la DB ; ouvre en confirmant un faux compte, qualifie d'« enregistrées » des cartes inexistantes (DB `user_attack_cards`=0, `user_defense_cards`=0), hedge contradictoire après. | Projection verify **DB-first et frame-agnostique** : énumérer ce qui existe (2 suivis), **nier explicitement** les objets absents (« aucune carte créée/sauvegardée »), interdire le mot « enregistré/sauvegardé » sans `db_ref`. | Verify avec faux compte d'objets → correction chiffrée ancrée DB ; jamais « X enregistrés » avant lecture DB ; garde LEDGER bloque « enregistré » sans `db_ref`. | `fix_applied` (chantier P7, 2026-07-14) — P7-F: directive snapshot « vérification FRAME-AGNOSTIQUE » (nier explicitement les objets absents avant d'énumérer le réel ; « enregistré/sauvegardé/créé » réservé aux données présentes ou à un outcome committed) |
| T7 | `BF-ROUTE-02` | active flow / interruption policy `coaching_recommendation` local flow | Flow coaching actif (sticky) capture un report de complétion (« j'ai réussi à boire mon verre d'eau ») ; `action_reference` non évalué, aucun `track_progress` proposé. | Sous flow coaching actif, évaluer `action_reference` sur un énoncé de complétion et **proposer/commit** le suivi (préemption comme le safety). | Complétion pendant flow coaching → track proposé ou commit ; contraste avec la même formulation explicite qui doit toujours passer. | `fix_applied` (chantier P7, 2026-07-14) — P7-E: règle CO-DEMANDE TRANSACTIONNELLE au dispatcher local coaching (report de complétion → exit vers le global, l'écriture part) ; probe live P7-7 (1 entry pendant le flow) |
| T11 | `BF-ROUTE-02` | active flow / interruption policy + memory planner gate sous flow | Intention mémoire explicite (« retiens un truc sur moi… ») avalée par le flow coaching ; `memory_mode=none`, aucun accusé. Non-perte (batch l'extrait ex-post) mais miss in-turn. | Laisser une intention mémoire explicite préempter / être accusée dans un flow coaching actif (cf. T12 forcé qui sort du flow). | « retiens que… » pendant flow coaching → accusé mémoire in-turn + candidate ; pas besoin de « arrête de poser des questions » pour être entendu. | `fix_applied` (chantier P7, 2026-07-14) — P7-E: même règle (intention mémoire explicite → exit + accusé in-turn) ; probe live P7-7 T3 |
| Post-run (memorizer) | `à classifier` (frontière domaine memorizer) | memorizer extraction / write policy | Un report de suivi (« a réussi à prendre un petit-déjeuner posé le 12/07 ») persisté comme `candidate` mémoire, avec date fausse (12/07 vs 13/07 réel). Report d'action du plan = domaine `track_progress`, pas mémoire personnelle. | Filtre memorizer rejetant les faits de type « a fait l'action X du plan » (déjà couverts par `track_progress`). | Batch sur un run contenant un report de complétion → 0 memory_item (candidate inclus) pour ce fait. | `fix_applied` (chantier P7, 2026-07-14) — P7-D: exclusion durcie (« NI aucun autre item, quel que soit le statut visé, candidate inclus ») + exemple verbatim du batch nina ; à re-observer au prochain batch réel |

## Positifs à capitaliser (invariants à protéger)

- T3 discrimination de technique (mot-de-bascule forcé refusé → carte d'attaque cohérente) — garder non-régressé.
- T4 pattern récurrent → carte de défense, structure correcte.
- T6 honnêteté de durabilité carte libre (`not_required`) : aucun faux objet, redirection app.
- T8/T9 `track_progress` : bonne cible, bonne date, isolation sous co-mention, additif sans friction — anti-régression Paul-hard21 T1 / eva-hard21 T10.
- T10 garde-fou d'ambiguïté (clarify avant opération).
- T13 rétractation mémoire : owner `normal_reply` (pas de mis-route `plan_realignment`) + **rétractation honorée par le batch** (fait absent des persistés) — contraste eva-hard21 T8/post-run.
- T14 récap grounded exact (2 entries DB) — contraste nina-global20 T12/T14.

## État DB en fin de run

Restauré : `user_plan_item_entries`=0, `memory_items`=0, `user_attack_cards`=0, `user_defense_cards`=0, `scheduled_checkins`=0 ; items de plan inchangés (reps 1/1/7/5) ; `memory_message_processing`/`memory_extraction_runs` du run supprimés ; messages du run purgés (1 message pré-existant conservé) ; `temp_memory` sans flow résiduel.
