# Run Bug Sheet - paul-global15-r8

## Metadata

- Date: 2026-07-07
- Run report: `docs/agent-playbook/New/test-material/qa-run-reports/2026-07-07-paul-global15-r8.md`
- Run id: `qa-paul-global15-20260707-r8`
- Persona / scenario: Paul, mode difficile, surfaces non couvertes des runs 06–07/07 (potion central, initiatives actif + frontières, needs_research, coach_preferences/session_style_commitment, product_help explanation-only, transverses track/date_hint)
- Verdict run: red
- Validite QA: valide (2 incidents gateway edge-runtime documentés, tours rejoués après nettoyage vérifié des effets partiels)
- Agent owner: QA agent (Claude), session 2026-07-07 soir

## Synthese

- Familles dominantes: BF-EFFECT-02 (patch d'item bloqué par triggers DB), BF-LEDGER-01 variante (claim de recherche sans exécution), BF-EFFECT-03 (direct effect interdit émis), BF-INTAKE-05 (composite aplati)
- Bug le plus bloquant: R8-B01 — la DERNIÈRE rep d'une habitude (atteinte de cible) ne patch jamais compteur/statut/principe sous rôle `authenticated` ; échec avalé, chat confirme quand même
- Fix architectural prioritaire: triggers DB `guard_unlocked_principles_update` / `unlock_v2_principles_from_item_transition` — autoriser la transition légitime (SECURITY DEFINER + pg_trigger_depth()/GUC) et remonter `item_patch_failed` en ledger `failed`
- Rerun requis: oui après fix R8-B01 (scénario : 3e rep d'une habitude 2/3 → vérifier `current_reps=3`, `in_maintenance`, principe débloqué) ; run research dédié après décision produit R8-B02

## Bug Ledger

| Bug id | Tours | Famille | Domaine owner | Source amont | Symptome visible | Preuve systeme | Correction attendue | Statut | Fix reference | Tests requis |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `R8-B01` | T12 | `BF-EFFECT-02` | track_progress executor + triggers DB | Patch item → transition `in_maintenance` → trigger `unlock_v2_principles_from_item_transition` → `unlock_transformation_principle('hara_hachi_bu')` → rejeté par `guard_unlocked_principles_update()` (`auth.role()` ≠ service_role, client construit avec JWT user, y compris prod `sophia-brain/index.ts:139`) ; échec avalé « non-bloquant » (`track_progress_plan_item/db.ts:414`, cas rose-r3 B01) | Chat dit « enregistrées comme faites le 06/07 » mais la carte reste 2/3, jamais `in_maintenance`, principe jamais débloqué | Entry `8ff3e9b6` committée (`effective_at=2026-07-06`) + item inchangé (`current_reps=2/3, active_building`) sur 2 essais ; rollback-test DB : le guard raise même en `service_role` SQL (seul le claim JWT passe) ; périmètre = uniquement transitions `in_maintenance`/`stalled` | Triggers DB : distinguer modification directe vs transition légitime (SECURITY DEFINER + `pg_trigger_depth()`/GUC reconnu par le guard) ; remonter `item_patch_failed` en ledger `failed` | `fix_applied` — chantier V4-2 (2026-07-08, won't-fix RÉOUVERT sur décision utilisateur). Migration `20260708100000_principle_unlock_cascade_marker.sql` : `unlock_transformation_principle` pose un marqueur GUC transactionnel le temps de son UPDATE, le guard le reconnaît (cascade légitime) ; update direct toujours rejeté (testé rollback). + volet cmd 15 : `item_patch_applied=false` remonte en outcome (guidance « ne confirme NI compteur NI statut NI déblocage »). **Probe live scénario rouge exact** : 3e rep via chat sous authenticated → entry + 3/3 + in_maintenance + hara_hachi_bu débloqué | migration + track_progress + guidance | positif (3e rep ⇒ 3/3 + in_maintenance + principe) + rôle `authenticated` + reps intermédiaires inchangées (anti-régression) |
| `R8-B02` | T7 | `BF-LEDGER-01` (variante informationnelle) | router global / composer + contrat dispatcher | Refactor `3de0b9a2` (2026-06-30, -8417 lignes `router/run.ts`) a supprimé le pipeline research ; `needs_research` (dispatcher règle 8, `turn_frame.v1.ts`) reste émis et propagé mais plus consommé ; companion sans consigne « pas d'accès internet » | User demande « vérifie sur internet » ; Sophia répond « ce que montrent les études récentes » avec chiffres, sans recherche ni aveu de limite | `needs_research value=true confidence=0.98` dans le turn frame ; route `normal_reply_default` (vs `needs_research_forces_normal_reply` du run 2026-06-13) ; aucun marqueur `research_requested/executed/sources` dans trace/metadata | Décision produit : (a) restaurer un consommateur research, ou (b) pruner le signal du contrat dispatcher ET ajouter la consigne d'honnêteté companion sur demande de vérification externe | `fix_applied` — chantier V4-1 (2026-07-08, décision produit : REBRANCHER). Exécuteur `router/research_grounding.ts` (signal structuré → recherche Gemini grounding gatée safety → bloc « RECHERCHE WEB » injecté ; échec → directive d'honnêteté interdisant tout claim de vérification) ; events `research_grounding` ré-émis ; contre-exemple product_help au dispatcher. Probes live : voie succès (vraie recherche, event `success`) ET voie échec (« de mémoire, à vérifier ») vertes. Cf. rose-r6 B01 (fiche détaillée) | `router/research_grounding.ts` | positif (demande vérif ⇒ recherche exécutée OU aveu explicite) + anti-FP (question personnelle « vérifie où j'en suis » ⇒ pas de flag) — l'anti-FP est déjà vert (T9) |
| `R8-B03` | T5 | `BF-EFFECT-03` | dispatcher global (turn frame) | Prompt dispatcher : demande récurrente avec horaire précis (« que toi tu m'envoies un message vers 18h30... tous les jours de semaine ») ré-attire la lane one_shot malgré la règle « jamais direct_effects.create_one_shot_reminder » pour le récurrent (récidive famille eva-r2 T13) | Aucun (garde-fou aval a bloqué ; réponse visible correcte, renvoi Initiatives sans claim) | direct_effect émis `create_one_shot_reminder cardinality=recurring` ; ledger `requested=1, blocked=1 (recurring_not_supported), committed=0` ; DB 0 reminder/checkin | Contrat dispatcher : `cardinality=recurring` ⇒ interdiction d'émettre `create_one_shot_reminder`, avec cas dans `dispatcher_prompt_contract_test` reprenant ce message | `fix_applied` — chantier V4-7 (2026-07-08) : exemple INVALIDE verbatim de ce run ancré au bloc canonique dispatcher (« que toi tu m'envoies un message vers 18h30 tous les jours de semaine » → direct_effects=[] + initiatives) ; ceintures V3-3 (gate + lane) restent la 2e ligne | `one_shot_reminder_prompt_contract.ts` | positif (récurrent+horaire ⇒ 0 direct effect) + anti-FP (« rappelle-moi demain 18h30 » ponctuel ⇒ one_shot conservé) |
| `R8-B04` | T4 | `BF-INTAKE-05` | skill coaching_recommendation (local dispatcher/reducer) | Après convergence du flow sur une recommandation, une demande composite « ok pour la carte + un déclic à me dire en attendant » est aplatie en re-pitch de la carte ; le mot de bascule (cas canonique du contrat) n'est ni servi ni discuté | Réponse quasi identique au tour précédent, besoin immédiat ignoré, structure répétée | T3 et T4 : même contenu carte de défense ; aucun traitement du micro-cadre « mot de bascule » ; routing pourtant correct (coaching, pas initiatives malgré « tous les soirs ») | Contrat local dispatcher coaching : micro-cadre « demande complémentaire après acceptation » (accepted_recommendation + nouvelle technique demandée ⇒ adresser la technique, pas répéter l'étape) — même mécanisme structurel que alex-r1 B04 / paul-r7 B02 | `fix_applied` — chantier V4-5 (2026-07-08) : micro-cadre « demande composite après acceptation » au dispatcher local coaching — l'acceptation se consomme en une phrase, le reste du tour adresse la nouvelle demande (technique servie ou discutée via technique_coherence), jamais un re-pitch | `coaching_recommendation/local_flow.ts` | positif (« ok pour X, donne Y en attendant » ⇒ Y adressé) + paraphrase + anti-FP (simple « ok pour la carte » ⇒ pas de sur-réponse) |

## Decisions / Arbitrages

| Date | Decision | Pourquoi | Owner | Reference |
| --- | --- | --- | --- | --- |
| 2026-07-07 | Incidents gateway T11/T12 classés environnement (restarts `supabase_edge_runtime`), pas produit | Corrélation StartedAt container / échecs ; retries propres après nettoyage effets partiels vérifié | QA agent | rapport r8 §1 |
| 2026-07-07 | Le flag `need_explanation` demandé au cadrage n'existe pas dans le code | Grep exhaustif `supabase/functions` + docs ; surface la plus proche (`product_help` explanation-only) testée à la place (T11, green) | QA agent | rapport r8 §1 |

## Verification

| Date | Bug id | Verification | Resultat | Reference |
| --- | --- | --- | --- | --- |
| 2026-07-07 | R8-B01 | Rollback-test SQL de l'update `in_maintenance` (rôle postgres puis `set local role service_role`) | Guard raise dans les deux cas (seul le claim JWT `service_role` passe) ; périmètre confirmé = transitions `in_maintenance`/`stalled` uniquement | rapport r8 T12 |
| 2026-07-07 | R8-B02 | Comparaison trace T7 vs rapport `2026-06-13-research-routing-r1` + `git log -S needs_research_forces_normal_reply` | Consommateur supprimé au commit `3de0b9a2` ; signal toujours émis | rapport r8 T7 |
