# Rose — Global 15 — R6 — Feuille De Suivi Bugs

Rapport associé: `docs/agent-playbook/New/test-material/qa-run-reports/2026-07-03-rose-global15-r6.md`

- Familles: `BF-MEMORY-01` (memorizer quotidien inopérant, **transverse**, cause racine `.in()` non borné), `BF-EFFECT-02` (progrès rapporté non loggé + clarify non surfacé, T2), `BF-ROUTE-02` (flow actif capte un item framework nommé, T5).
- **0 tour red**, 2 tours yellow (T2, T5). Le point le plus sérieux est **système et transverse** (memorizer), pas un tour.
- **Régressions r3 NON reproduites (confirmé)** : `BF-LEDGER-02`/`BF-EFFECT-04` (faux doublon / double-exécution reminder — create/cancel propres T7/T9) ; `BF-EFFECT-03` (UUID `plan_item_id` halluciné — `action_context.plan_item_id: null` au T4) ; `BF-STATUS-01` (recap déconnecté — recap fidèle à l'état plan au T13) ; `BF-ROUTE-02` sur clôture (owner `normal_reply` au T15). Améliorations UX : accueil émotionnel avant technique (T10, vs r3 T9 technique-first).
- Incidents non-bug documentés : cron memorizer local tournant en parallèle (autres users) ; rappel baseline `11111d8b` passé `pending→awaiting_user` par `process-checkins` (cron, pas mon effet) ; résidu de baseline `d62d828a.current_reps=1` sans entry (orphelin d'un run antérieur, confond la lecture du compteur au T3).

| Bug id | Tours | Famille | Domaine owner | Source amont | Symptome visible | Preuve systeme | Correction attendue | Statut | Fix reference | Tests requis |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `rose-global15-r6-B01` | (transverse — découvert à la vérif mémoire T6) | `BF-MEMORY-01` | `supabase/functions/trigger-memorizer-daily/index.ts` → `loadUnprocessedMessages` (L124-166) | Le filtre des messages déjà-traités utilise `.in("message_id", ids)` avec **tous** les ids user de la fenêtre (30h). Pour un user à fort volume, l'URL GET PostgREST `memory_message_processing?...&message_id=in.(<N uuids>)` dépasse la limite de longueur → l'appel jette avant `messages_loaded`, le batch entier échoue, **aucun `memory_items` écrit**. | `trigger-memorizer-daily` renvoie HTTP 500 pour Rose (log `[object Object]`, l'erreur n'étant pas une instance `Error`). Sous ce chemin, une promesse mémoire (T6) ne serait jamais persistée. | `system_error_logs`: `title=daily_memorizer_failed`, `message="URI too long"`. Rose = **219 messages user / 15 scopes en 30h**. Batch nominal (30h) échoue reproductiblement ; batch borné `hours:1` (18 msgs) réussit (`extraction_run_id=3f9a4e8a`, 8 items écrits, T6 retrouvé actif) → planner/writer sains, seul le chargement est fragile. | Remplacer le `.in()` non borné par un **anti-join / RPC** (`NOT EXISTS` sur `memory_message_processing`) ou un **chunking** des ids sous la limite d'URI. Corollaire : logger l'erreur réelle (sérialiser l'objet Supabase, pas `[object Object]`). | `fix_applied` | Chantier H1 (2026-07-04) : `loadProcessedMessageIds` chunke le `.in()` par paquets de 100 (union des résultats, aucun id perdu) ; `readableErrorMessage` sérialise message/details/code des erreurs Supabase (fin du `[object Object]`). Tests unitaires (519 ids, chunks ≤100, erreur réelle remontée) + **validation réelle** : batch Rose fenêtre à 201 ids → HTTP 200 (le chargement passe ; l'extraction a ensuite été dédupliquée par batch_hash, ce qui prouve que le stade extraction est atteint). | `trigger-memorizer-daily` DOIT réussir pour un user à ≥ 500 messages/ids sur la fenêtre sans erreur `URI too long` ; l'erreur remontée DOIT être lisible (message réel) ; test d'intégration : batch nominal (30h) sur un compte à fort volume écrit bien les `memory_items` attendus. |
| `rose-global15-r6-B02` | T2 | `BF-EFFECT-02` | intake `track_progress_plan_item` (evidence gate) + final response pipeline (surfaçage `needs_clarify`) | (1) Seuil d'evidence trop strict : un rapport de complétion en langage naturel (« pas touché une seule fois… journée pleine ») n'est pas rattaché à l'item `d62d828a` → `target_not_evidenced`. (2) Le `needs_clarify` du tool skill n'est pas surfacé dans la réponse visible. | Sophia valide émotionnellement (« c'est solide ») mais **ne loggue rien et ne pose aucune question**. L'utilisatrice peut croire l'action trackée. | `tool_skill_status=needs_clarify` / `reason=target_not_evidenced` ; ledger `requested→blocked`, `committed=0` ; DB : `user_plan_item_entries` inchangé, `d62d828a.current_reps` inchangé. Le T3 (reformulation explicite) commit correctement → c'est bien le pont implicite→durable + le surfaçage qui manquent. | Soit résoudre la cible par **mapping sémantique déterministe** sur les items actifs (« journée sans consommer » ↔ `Journée 100% sans cannabis`) et commit ; soit, s'il reste un doute, imposer par contrat qu'un `needs_clarify` sur effet durable **produise une question de clarification visible**, jamais un silence validant. | `fix_applied` | (1) Moitié « évidence trop stricte » : morte avec le chantier G1 (le tour datait de l'ère F2/stopwords) — **probe réelle 2026-07-04** : le message T2 verbatim → commit sur `d62d828a`, réponse « journée 100% sans cannabis validée » adossée au commit. (2) Moitié « silence validant » : invariant structurel chantier H3 — un outcome `needs_clarify` ne sort JAMAIS sans `clarify_question` (fallback par effect_type dans `deriveEffectsOutcome`) + test de contrat. | Positif : rapport de complétion implicite sur un item actif identifiable → commit OU question de clarification visible, jamais une validation émotionnelle sans effet. Anti-régression : le chemin explicite (T3) reste committant. |
| `rose-global15-r6-B03` | T5 | `BF-ROUTE-02` (+ `BF-ROUTE-01`) | active flow / interruption policy + registre tool skills framework | Un flow `coaching_recommendation` actif (issu de T4) préempte l'évaluation d'ownership et absorbe un item **framework de plan explicitement nommé** ; aucun tool skill framework dédié n'est priorisé. | « on peut faire *Cibler le joint réflexe* de mon plan maintenant ? » → réponse texte générique, pas d'intake structuré, l'item de framework n'avance pas. | `response_owner=coaching_recommendation`, `route_reason=active_coaching_recommendation`, `skill_signals={}`, aucune activation de `bd2ef2a9`. | Avant qu'un flow actif générique ne capte le tour, détecter qu'un item de plan actif à skill dédié est explicitement nommé et **le prioriser** ; si le framework n'a pas de skill structuré, câbler la couverture. | `open` | — | Positif : item framework de plan nommé → activation du skill dédié même sous flow coaching actif. Anti-faux-positif : mention vague sans item → flow courant conservé. |

## Nits non bloquants (pas de ligne bug dédiée)

| Tours | Description | Owner probable | Note |
| --- | --- | --- | --- |
| T8, T9 | `route_decision.direct_effects_to_run` affiche `["create_one_shot_reminder"]` alors que l'effet réel est `cancel_one_shot_reminder`. Métadonnée de trace seulement ; l'effet DB est correct. | trace/route_decision builder | À corriger pour fiabilité de la trace ; n'affecte pas le résultat. |
| T4 | Phrase « Je tiens jusqu'à 22h » un peu bancale quand 22h EST le pic (on voudrait tenir *au-delà*). | `coaching_recommendation` contenu | Qualité de contenu, non systémique. |
| T10 | `reason_codes` safety inclut `imminent_relapse_risk` sur une rechute **passée** (non imminente). | safety pregate classification | Band `medium` et réponse appropriés ; imprécision de label. |
| T13 | Recap fidèle aux statuts mais n'expose pas la granularité du compteur (`Journée 100%` en « En cours » sans 1/2). | recap projection | Amélioration possible, pas un bug. |

## Suivi Des Décisions Architecturales

| Date | Décision | Statut | Référence |
| --- | --- | --- | --- |
| 2026-07-03 | Nouveau constat système `BF-MEMORY-01` : `trigger-memorizer-daily` échoue (`URI too long`) pour un user à fort volume de messages sur la fenêtre, à cause d'un `.in("message_id", ids)` non borné dans `loadUnprocessedMessages`. Casse le sous-système mémoire durable via le chemin nominal. Amplifié par l'accumulation de données de test (15 scopes) mais défaut de robustesse réel. Priorité de fix la plus haute du run. | Active | rose-global15-r6 |
| 2026-07-03 | Confirmation de non-régression sur les rouges/jaunes de r3 : reminder create/cancel propres (`BF-LEDGER-02`/`BF-EFFECT-04` non reproduits), `action_context.plan_item_id: null` (`BF-EFFECT-03` non reproduit), recap fidèle à l'état plan (`BF-STATUS-01` non reproduit), owner `normal_reply` sur clôture (`BF-ROUTE-02` T15 non reproduit), accueil émotionnel avant technique (amélioration vs r3 T9). | Active | rose-global15-r6 |
| 2026-07-03 | Persistance du `BF-ROUTE-02`/`BF-ROUTE-01` sur item framework nommé capté par flow coaching actif (déjà signalé r3 T5) : arbitrage flow-actif-vs-item-de-plan-nommé toujours ouvert. | Active | rose-global15-r6 / rose-global15-r3 |
