# L4 — LA DIRECTION, ÉCRITE AVANT DE CODER

Écrit le 2026-08-22 à 17:00 CEST, **après** la `mesure AVANT` et **avant** toute
ligne de code. Rien ici ne sera réécrit après le résultat.

## Ce que je crois qu'il va se passer

1. ⬇️ **Apports fixes déclarés rangés en `lean_protein` : 7/7 → 0/7** sur les sept
   qui existent en base, parce qu'AUCUN ne nomme de groupe et que le repli
   cesse d'être une affirmation.
2. ⬆️ **Le repli devient hors-vocabulaire**, donc `groupBandsFrom` le saute
   (`composition_fill.ts:181` filtre déjà sur `FOOD_GROUP_REFS`) et
   `sentinelCarriersOf` le range dans un seau que personne n'interroge.
3. ⬇️ **La borne d'énergie d'un terme inconnu déclaré `lean_protein` redescend**
   de **538 kcal à 201 kcal** à 150 g (cas d'un porteur de whey), c'est-à-dire
   au niveau de l'index de base.
4. ⬆️ **`lean_protein` REDEVIENT porteur de la sentinelle `zincSource`** dans les
   7 cas — il tombe aujourd'hui à la première déclaration (2/3 → 2/4).
5. **Rien ne sort du calcul.** L'énergie et la protéine de la déclaration
   restent dans l'index et dans le verdict. Seule l'affirmation de groupe part.

## Ce que je crois qu'il NE va PAS se passer

- ⚠️ **La distribution ne « s'étale » pas** au sens de la fiche : 0 des 8 lignes
  en base ne porte de clé de groupe, et aucun écran ne l'écrit. Le lot rend le
  champ LISIBLE et le repli HONNÊTE ; il ne peut pas fabriquer une distribution.
  ⛔ Un seuil « ≥ 2 groupes distincts sur une fixture que j'écris moi-même » est
  un seuil qu'on ne peut pas rater — il ne compte pas.
- ⚠️ La lane FOYER n'appelle jamais `augmentedIndexFor`. Ce lot ne change donc
  **rien** au foyer, et je ne prétendrai pas le contraire.

## Le seuil, arrêté avant la mesure

| # | seuil | dénominateur |
|---|---|---|
| ① | apports déclarés portant `lean_protein` **= 0** | **7 apports déclarés en base** (6 `student_goals` + 1 `household_members`) |
| ② | bande `lean_protein` **identique à l'index de base** dans les 7 cas — `[110,4 ; 133,8]` | idem |
| ③ | `lean_protein` porteur de `zincSource` dans **7/7** | idem |
| ④ | un groupe DÉCLARÉ et valide est **honoré tel quel** ; un groupe déclaré et **inventé** ne devient jamais `lean_protein` | cas unitaires |
| ⑤ | compteur à deux populations : `declares_group` (ce que la déclaration porte) **et** `reaches_index` (ce qui atteint l'index) — les deux non nuls sur un cas qui déclare | cas unitaires |

⛔ **Ce que le lot doit NE PAS faire, et qui serait une régression** : retirer
l'apport de l'index. La protéine du shaker doit rester comptée — c'est la
raison d'être de FF-051, et la retirer serait la cicatrice
« ancrage : remplacer, jamais multiplier ni retirer ».
