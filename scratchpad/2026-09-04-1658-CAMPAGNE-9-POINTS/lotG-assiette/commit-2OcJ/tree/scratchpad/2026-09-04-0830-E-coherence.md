# E — la passe finale de cohérence

**Date** 2026-09-04 08:30 (horloge du chantier ; horloge du poste : 2026-09-03 22:15 CEST) ·
**Arbre** `/Users/ahmedamara/Dev/Sophia 2`, branche `ff-001-quotidien-du-coach` ·
**HEAD au départ** `ac995941` (« orchestration : le rapport final des huit chantiers »).
**Mandat** [MASTER-PROMPT §5.12](2026-09-03-1308-MASTER-PROMPT-8-CHANTIERS.md) ·
**Journal amont** [ORCHESTRATION](2026-09-03-1329-ORCHESTRATION-8-CHANTIERS.md), entrées 07:0x → 07:4x.

> Ce journal s'écrit **au fil de l'eau** (§2.3 n°25). Ce qui n'a pas été mesuré y est écrit ROUGE,
> jamais présenté comme prouvé.

---

## 0. Le poste, tel que je l'ai trouvé — et ce que ça change

**HEAD a bougé pendant ma lecture** : `5be833d4` au moment où j'ai ouvert la session, `ac995941`
quand j'ai listé les worktrees quatre minutes plus tard. L'orchestrateur a posé son rapport final
pendant que je lisais son journal. Rien de cassé — le commit ne touche que `scratchpad/` — mais il
confirme que **l'arbre est vivant sous mes pieds**, et que chaque mesure doit dire à quel sha elle
a été prise.

**Sept worktrees**, dont quatre de lanes (CUISINE, MEMBRE, SUIVI, VERIF) et un `gate-probe` détaché
sur `0909763f`, dans le scratchpad **partagé** de la session. `git diff 0909763f..ac995941 -- .
':!scratchpad'` rend **vide** : les quatre derniers commits sont tous du journal. ⇒ **`gate-probe`
est un banc « avant » exact pour tout ce qui n'est pas de la doc**, et je m'en sers plus bas sans
rien créer.

**Fichiers sales qui ne sont pas à moi**, mesurés avant de toucher quoi que ce soit :

| fichier | à qui | ce que c'est |
|---|---|---|
| `frontend/src/keel/components/MealBuilder.tsx` | lot « envie », session voisine | +interface |
| `frontend/src/keel/api/householdEnvyWiring.int.test.ts` | idem | son test |
| `frontend/src/keel/components/oneCookingSessionField.int.test.ts` | idem | son test |
| `frontend/src/keel/pages/SetupPage.tsx` | lot D14 « choisir les repas », session voisine | +113 l. |
| `frontend/src/keel/i18n/en.ts` (3 l.) · `fr.ts` (6 l.) | **le même lot « envie »** | `meals.result.empty` |
| `scratchpad/…-1308-MASTER-PROMPT-…md` | l'orchestrateur | sa note §2.2 n°14 |
| `deno.lock` | indéterminé | — |

**L'index était vide** (`git diff --cached --stat` → rien). Aucune de mes commandes n'a `git add -A`.

### ⛔ Le piège que j'ai trouvé, et l'arbitrage que j'ai rendu

Mon mandat me demande **un commit de `en.ts` et `fr.ts`**. Ces deux fichiers portaient déjà
**neuf lignes d'une session voisine** (le lot « envie » : `meals.result.empty`, la phrase qui
envoyait chercher un menu retiré le 2026-09-03). Un `git commit <chemins>` **protège l'index, pas
l'arbre** : il emporte le fichier ENTIER, hunks étrangers compris.

**Je n'ai trouvé aucune sortie sans coût**, et je nomme celle que j'ai prise plutôt que de la
taire :

- `git stash` / `checkout` / `restore` : **interdits**, et à juste titre — ils emportent le travail
  non commité des autres sessions.
- Fabriquer un blob partiel par `git update-index --cacheinfo` puis `git commit` **sans** chemin :
  ça commite **tout l'index**, et l'index redevient un objet partagé le temps de deux commandes.
  Sur un arbre à quatre sessions, c'est **pire** que le mal.
- ⇒ **Retenu : je commite les neuf lignes avec les miennes, et je le dis dans le message de commit
  et ici.** Elles sont cohérentes (les deux packs, la clé et son motif), et §2.2 n°19 range de
  toute façon `en.ts`/`fr.ts` **de mon côté** : les lanes ne les commitent pas, E fusionne. Le lot
  « envie » garde `MealBuilder.tsx` et ses deux tests ; seule sa moitié i18n part avec moi.

**Ce que ça coûte, écrit pour que personne ne le redécouvre** : la session voisine trouvera ses deux
packs **propres** au moment de commiter son lot. Ce n'est pas une perte — c'est une surprise.

---

## 1. i18n — les cinq blocs consolidés

**Fait.** Dix délimiteurs dans `en.ts`, dix dans `fr.ts`, retirés **par numéro de ligne** en UTF-8
(script Python, `encoding='utf-8'`, jamais `unicode_escape`). Là où un `— fin ──` touchait
directement le `— début ──` du bloc suivant (FOYER/CUISINE, CUISINE/MEMBRE, MEMBRE/SUIVI), une
**ligne vide** prend leur place : les blocs restent séparés à l'œil.

**Tout le reste est intact** — les clés, et surtout **les commentaires qui portent un motif** :
les retraits listés (les sept clés de `/app/meals`, les deux options vides d'objectif), les valeurs
changées en place (`setup.work_lunch.*`), les renversements datés, et la note « GARDÉE, contre la
liste de huit du mandat : `meals.loading` — deux appelants vivants ». C'est la mémoire du chantier.

`git diff` des deux fichiers, hors hunk étranger : **exactement 20 suppressions, 6 ajouts de ligne
vide**. Rien d'autre.

**`catalog.ts` : aucun délimiteur.** Mesuré (`grep -n 'chantier-0903' catalog.ts` → 7 lignes, toutes
des commentaires `⟳` de motif, aucun `── début ──`/`── fin ──`). Le fichier est **propre dans
l'arbre** et n'entre donc pas dans mon commit : il n'y a rien à y consolider. Le mandat dit « les
trois fichiers » ; la mesure dit deux.

### Les preuves

| mesure | résultat |
|---|---|
| `npx vitest run i18n/parity.int.test.ts i18n/pageSeams.int.test.ts i18n/pageFrontier.int.test.ts` | ✅ **3 fichiers, 23 tests, 23 verts** (1,01 s) |
| mojibake — `Ã`, `â€`, `ï¿½`, `�`, `Ã¢`, `Ã©`, `Ã¨` sur `en.ts`, `fr.ts`, `catalog.ts` | ✅ **0 occurrence**, et les trois décodent en UTF-8 strict |
| `npm run lint:i18n` | 🔴 **FAILED, 94 violations** — voir juste dessous |

### 🔴 `lint:i18n` est rouge — et c'est ANTÉRIEUR, mesuré et non déduit

Je ne me suis pas contenté de dire « mes lignes sont des commentaires, donc ce n'est pas moi ». J'ai
lancé le linter **des deux côtés** :

```
gate-probe (0909763f, code identique à HEAD) → i18n-lint FAILED (94 violations, 272 fichiers)
arbre principal (après mon édition)          → i18n-lint FAILED (94 violations, 272 fichiers)
diff des deux sorties → 10 lignes, TOUTES des NUMÉROS DE LIGNE dans MealBuilder.tsx et
                        SetupPage.tsx, décalés par le travail non commité de la session voisine.
```

**Même jeu de violations, même compte, aux deux bouts. Ma consolidation en a changé zéro.**

**Et le gate ne le lance pas** : `grep 'lint:i18n\|i18n-lint' scripts/agent-gate.sh` → rien. Ce
rouge ne bloque donc aucun commit ; il n'est vu que par qui tape la commande. C'est ce qui explique
qu'il ait pu monter sans que personne trébuche dessus.

**Sur la NATURE des 94, parce que le compte seul induirait en erreur** : la grande majorité sont des
**génériques TypeScript lus comme du texte JSX** — `« Promise »`, `« void | Promise »`, `« new Map »`
(le linter voit le `<` de `Promise<void>` et croit à une balise). Ce n'est pas de la dette de langue,
c'est un **défaut du linter**. Trois violations, en revanche, ressemblent à de vraies :
`NotFoundPage.tsx:38 attribut title en dur « Page not found »` et
`salesStructuredData.ts:66 locale « fr-FR » en dur`. **Je ne les répare pas** — les lanes sont
closes, et §5.12 ne m'autorise que la consolidation. Je les **nomme** en §5.

---

## 2. Les baselines — deux lignes abaissées, aucune levée

**Commit `36ec6e0f`.** `scripts/.tsc-test-red-baseline`, 29 lignes avant, **29 après** :

| fichier | avant | après | pourquoi |
|---|---|---|---|
| `src/keel/api/onboarding.int.test.ts` | 2 | **0** | le gate le signalait à chaque passage |
| `src/keel/lib/planByPersonModel.int.test.ts` | 6 | **2** | quatre erreurs réparées par un lot que je ne sais pas nommer |

⚠️ Mon mandat écrivait `onboarting.int.test.ts`. **Le fichier s'appelle `onboarding`** — une lettre,
et un pathspec faux aurait passé en silence. Mesuré sur la sortie du gate, pas recopié.

**Les deux gardent leur ligne à 0 plutôt que de disparaître**, sur le précédent de
`setupMouthsStep.int.test.ts 0` juste en dessous. Une ligne à 0 dit « ce fichier a été propre un
jour », et le gate échoue pareil dès la première erreur qui revient (un fichier hors liste qui porte
une erreur est un rouge neuf).

**Vérification que je n'ai rien levé** — parseur, pas relecture à l'œil : `git show HEAD:` du fichier
comparé au mien, ligne à ligne. **2 baisses, 0 hausse, 0 ajout, 0 retrait.**

**Effet sur le gate**, et c'est la mesure qui compte : le compteur passe de
`87 erreurs (liste: 93)` à **`87 erreurs (liste: 87)`**. **La tolérance vaut exactement le réel : il
n'y a plus un seul cran de mou.** Les deux avertissements `ABAISSE sa ligne` ont disparu.

⛔ Je n'ai touché **ni** `.vitest-red-baseline` (4 rouges pour 4 tolérés — déjà exact), **ni**
`.test-count-baseline` (c'est un plancher ; le monter est un resserrement, mais il n'est pas dans
mon mandat et il casserait la première lane qui retire un test).

---

## 3. La grille C1-C8

**Méthode** : lecture de code, SQL en lecture sur la base locale (`docker exec supabase_db_Sophia_2
psql`), et tests. **26 générations réelles du 2026-09-03** encore en base, dont les **4 de la
fenêtre sous gel** (19:14, 19:38, 19:42, 19:46 — `duration_days = 7`). Aucun run réel de ma part,
aucune session navigateur.

### C1 — la fenêtre · **PROUVÉE sauf la branche « veille », qui n'a aucune ligne**

| clause | verdict | mesure |
|---|---|---|
| chaque jour de `windowDayOrder` une fois, dans l'ordre du plan | **PROUVÉE** | `mealWindow.ts:170` — `Array.from` sur `min(7, max(1, n))` jours **consécutifs** : l'unicité et l'ordre sont structurels, pas testés. Confirmé sur les 4 runs : jours uniques `True`, **0 plat hors fenêtre**. Tests : `planDayView` 26 ✓, `planDaySeparation` 29 ✓, `presenceOutsideWindow` 6 ✓ |
| `daysToEat = duration_days − lead_days ≤ 7` | **PROUVÉE** | CHECK `student_generated_meals_eaten_days_check` en base — vrai sur **254/254 lignes**, pas seulement sur celles que j'ai regardées. Mesuré = 7 sur les 4 runs |
| avec une veille, le rang 0 est `cookOnlyDay`, porte une session et aucun repas | **NON PROUVÉE** | **`lead_days = 0` sur 254 lignes / 254.** Aucune veille n'existe en base. Couverte par test unitaire seul (`grocery_waves_test.ts` « A1 — la vague du RANG 0 (la veille) reste muette », verte au gate) |

**Fait mesuré qui vaut d'être écrit** : sur les quatre runs, le rang 0 (`thu`) porte **0 plat et 1
session de cuisine** — la *forme* du `cookOnlyDay`, obtenue **sans** `lead_days`, parce que les runs
sont partis après 19 h sur un plan qui commence le jour même (`timing.kind = same_morning`,
`reason = starts_today`).

#### ⛔ Une piste de défaut que j'ai suivie, et qui n'en est pas une — je l'écris parce que le prochain la suivra aussi

`MAX_WINDOW_DAYS = 7` et `windowDayOrder` **rabote** (`Math.min`), des deux côtés. Or la base accepte
`duration_days = 8` avec `lead_days = 1` (D1.3). J'en ai conclu qu'une fenêtre de veille perdait son
dernier jour mangé, côté serveur **et** côté écran.

**C'est faux, et le code avait déjà la réponse** : `withCookDayBefore`
(`_shared/keel/meal_plan_window.ts:605`) refuse `no_room` dès que `durationDays + 1 > MAX_WINDOW_DAYS`.
Le commentaire au-dessus nomme exactement le raisonnement que je venais de refaire — l'alphabet des
jetons, et `windowDates` qui est un `Record` par jeton où le 8ᵉ jour **écraserait** le 1ᵉʳ.

⇒ **Ce que ça laisse ouvert, et la lane A1 l'a déjà consigné ROUGE** : *un plan de sept jours mangés
n'a jamais de veille automatique*, tant que les sessions sont adressées par jeton et non par date.
Ce n'est pas un défaut neuf, c'est une dette nommée. ⇒ Et la leçon de méthode : **grep le garde-fou
avant d'annoncer le trou** — le mien était écrit, en français, dans le fichier même.

Détail annexe, vrai mais mineur : deux commentaires d'écran (`MealBuilder.tsx:1124`,
`SetupPage.tsx:6619`) affirment encore « la base : `duration_days between 1 and 7` ». La base dit
**1 à 8** depuis `20260903170000`. La contrainte a survécu à sa cause ; c'est le code qui refuse, pas
elle.

### C2 — la jointure cuisine · **PROUVÉE**

Sur les quatre runs de la fenêtre, **zéro violation sur les quatre clauses** :

| run | préparations | sessions | `uses` non résolus | consommée AVANT cuisson | prep sans session | session hors fenêtre |
|---|---|---|---|---|---|---|
| `7cd33493` | 6 | 3 (`thu`,`sat`,`mon`) | **0** | **0** | **0** | **0** |
| `3d089edb` | 4 | 1 (`thu`) | **0** | **0** | **0** | **0** |
| `ef5b0bee` | 6 | 2 (`thu`,`sun`) | **0** | **0** | **0** | **0** |
| `dfd28565` | 2 | 2 (`thu`,`sun`) | **0** | **0** | **0** | **0** |

Chaque `dish.uses[].preparation_id` résout ; chaque préparation est cuisinée à un rang **≤** celui du
plat qui la consomme ; aucune préparation n'est orpheline de session ; aucune session ne tombe hors
fenêtre. **Non prouvé** : « le nombre de sessions = celui que `deriveCookingPlan` a demandé » — la
demande (style de cuisine, `deriveCookingPlan`) n'est pas relisible dans `generated_from`, donc je ne
peux comparer le rendu qu'à lui-même. Le test `A2 — les deux lanes lisent la MÊME cuisine, et la
DÉRIVENT au même endroit` est vert au gate, mais il arme la source commune, pas le compte.

### C3 — les courses · **PROUVÉE sur trois clauses, et la quatrième est le DÉMENTI acté**

| clause | verdict | mesure |
|---|---|---|
| `buy_on` toutes datées | **PROUVÉE** | 4 runs, 174 articles, **0 non daté** |
| la première vague tombe sur le rang 0 | **PROUVÉE** | `True` sur les 4 |
| `servesCookOn` non `null` sur les vagues suivantes | **PROUVÉE** | `grocery_waves_test.ts` — « A1 — la PREMIÈRE vague ne porte pas de phrase, même tombée après le début » (qui boucle `for (const wave of waves.slice(1))` et **nomme C3**) + « la vague du RANG 0 (la veille) reste muette ». **Verts au gate.** ⚠️ `servesCookOn` n'est **pas** une colonne : il est **dérivé** par `groceryWaves.ts` à la lecture — le chercher en base ne rend rien |
| **« nombre de vagues = sessions »** | 🔴 **DÉMENTI** | ma mesure indépendante **retrouve le contre-exemple** : `dfd28565` a **2 sessions** (`thu`,`sun`) et **1 seule vague**. `vagues ≤ sessions` tient sur les 4 (3≤3, 1≤1, 2≤2, **1≤2**). Note datée portée dans le prompt maître §2.4 (commit `36ec6e0f`), sous la phrase d'origine laissée en place |

### C4 — les personnes · **DEUX clauses sur trois PROUVÉES**

| clause | verdict | mesure |
|---|---|---|
| chaque bouche présente a un plat commun ou dédié | **NON PROUVÉE** | aucun validateur de couverture n'existe (`mouths_unboxed` est un **compteur**, et le dépôt écrit lui-même que « les compteurs sont des MESURES, pas des consignes »). Scan des **1408 cellules** jour × moment : 5 laissent une bouche sans rien — mais `household_members.eating_rhythm` est `NULL` sur ces bouches, donc « présente » n'existe pas en donnée : **ni prouvable ni réfutable**, pas un démenti |
| un membre réclamé voit **sa** part, avec **ses** cases | **PROUVÉE** | run réel `9a033333…d001` : le plat `« Compote pour Cy »` (`member_id …c0d3`) est **absent** de la bande servie à Bo, qui ne porte que `KEEL_STRIP_ALL\|…\|1` et `KEEL_STRIP_SOME\|…\|1`. Écran : `myShareTickPosition.int.test.ts` — test **comportemental** (il monte le vrai `MyShareCard`), vert |
| le maître ne voit jamais la coche d'un profil réclamé | **PROUVÉE** | RLS lue sur la base vivante : `protocol_events_owner_read` = `auth.uid() = user_id` ; `meal_share_outcomes_read` n'ouvre au maître que la branche `target.user_id IS NULL`. Écriture : `keel_household_declare_share_outcome_for` **déployée** (lue par `pg_get_functiondef`, pas dans le fichier) porte `not_your_line` |

⚠️ **Deux verts de cette famille ne prouvent rien** et je les nomme : `myShare.int.test.ts` et
`member_plan_scope_test.ts:374` sont des **photographies de source** (`toContain("dishIsFor(…)")`,
`src.includes("dishIsForMouth")`). Ils épinglent une orthographe, pas un comportement. La preuve est
le SQL et le test qui monte le composant.

### C5 — les ceintures · **TROIS clauses sur quatre PROUVÉES**

| clause | verdict | mesure |
|---|---|---|
| (a) aucune calorie sans base | **PROUVÉE, bilingue** | `i18n/energyBasis.int.test.ts:153` — l'inventaire des clés qui écrivent un kcal est **CLOS**, balayé `for (const pack of [en, fr])`, égalité stricte avec `ENERGY_KEYS_WITH_A_BASIS`. On ne peut pas ajouter une phrase qui écrit un chiffre sans passer par la liste. 9 ✓ |
| (b) aucun terme de `FORBIDDEN_PORTION_TERMS` | **PROUVÉE, bilingue** | `household_portions_test.ts` — « PREUVE 2 — la ceinture est verte sur les DEUX sorties, FR et EN », « LOT 3B — les fuites que le corps rend POSSIBLES sont mordues, FR et EN », « LA NÉGATION NE RACHÈTE RIEN ICI » (qui épingle `allowNegatedMentions: false`). **Les trois verts au gate final.** Le banc est écrit à la main, **pas dérivé de la liste** : il ne se paramètre pas sur ce qu'il teste |
| (c) aucun objectif ni poids dans une vue | **PARTIELLE** | `planByPersonModel.int.test.ts:279` + ceinture **structurelle** `:307` (la vue n'accepte que `MemberPortionView`/`HouseholdDishView`, jamais le seul type qui porte `goal`). **Mais elle ne couvre que la famille `PlanByPerson`** — rien d'équivalent sur `MealBuilder`, `PlanResult`, `PlanDraftDialog`. La clause dit « une vue » ; la mesure en couvre six fichiers. *Sur « les deux langues » : la question ne se pose pas — cette garde mord sur des identifiants de code, pas sur de la copie* |
| (d) sous plancher TCA, aucun chiffre ni courbe | **PROUVÉE côté charge utile · NON PROUVÉE côté rendu** | `tracking_window_test.ts:143` — « A7 — sous plancher TCA, le rapport ne porte AUCUN chiffre ni courbe (C5) », qui **sérialise le rapport entier et refuse le moindre `typeof node === "number"`**. **Vert au gate.** Côté écran, en revanche, `trackingPage.int.test.ts:212` ne fait que deux `toContain("loadTracking(")` / `("report.floor")` : **aucun test du dépôt ne monte `StudentProgressPage` sous `floor: true`**. La clause tient parce que **le serveur ne livre aucun nombre**, pas parce que l'écran sait se taire |

### C6 — les deux surfaces · 🔴 **DÉMENTIE — le cadenas nommé ne verrouille qu'un côté**

**Le fait produit est vrai** : l'aperçu monte `PlanResult` (`PlanDraftDialog.tsx:274`) et le validé
aussi, par `MealBuilder.tsx:1800` (⚠️ **pas** par `StudentWeekPlanPage.tsx`, qui ne fait que le citer
en commentaire — la phrase du prompt maître laisse croire à un montage direct).

**Mais `setupDraftWiring.int.test.ts:176-179`, que §2.4 déclare « verrouillé par », lit UN SEUL
fichier** :

```js
const dialog = code("frontend/src/keel/components/plan/PlanDraftDialog.tsx");
expect(dialog).toContain("<PlanResult");
```

Mesure de portée : `grep -rn "<PlanResult" frontend/src --include="*.test.ts*"` → **une seule ligne
dans tout le dépôt**, celle-ci. ⇒ **Si `MealBuilder` réécrivait un rendu de plan à lui, ce test
resterait vert.** Le prompt maître dit « verrouillé » ; la mesure dit « photographié d'un côté ».
Il manque une ligne : `expect(code(".../MealBuilder.tsx")).toContain("<PlanResult")`.

### C7 — l'entonnoir · **QUATRE clauses sur cinq PROUVÉES**

| clause | verdict | mesure |
|---|---|---|
| trois étapes | **PROUVÉE** | `onboarding.ts:1130` — `STEP_ORDER = ["situate","people","request"]`, source **unique** de `steps.length` (aucune longueur en dur). `onboarding.int.test.ts:254` couvre les trois branches ✓ |
| `canGenerate` seule source du bouton | **PROUVÉE PAR LECTURE, NON PAR TEST** | `disabled={busy \|\| !verdict.ok}`, `verdict = canGenerate(...)`. Cherché et **non trouvé** : 27 autres `disabled=` (tous ailleurs), un second bouton primaire (gardé par `step.id`), `pointer-events-none` / `fieldset disabled` / `inert` / `aria-disabled` (0), une condition parente (`heldBack`/`stepBlockers` ne touchent que « Suivant »), un early-return (démonte la page entière). ⚠️ **`busy` est un second terme** — verrou de ré-entrance, pas un verdict. **Aucun test n'arme cette clause** |
| étape 2 et pop-up Foyer montent le MÊME `MouthCoreFields` | **PROUVÉE** | définition **unique** `MouthFormDialog.tsx:775` ; même symbole, **même chemin d'import** dans `SetupPage.tsx:126` et `HouseholdPage.tsx:107`. `setupMouthsStep.int.test.ts:616` compare les deux rendus avec une garde de non-vacuité (≥ 6 étiquettes) ✓ |
| plus de case « la veille » ni « durée de session » | **PROUVÉE** | `CookDayBeforeField.tsx` **n'existe plus** ; `oneCookingSessionField.int.test.ts:123/:138/:419` — et les trois **dépouillent les `//` avant de matcher**, donc pas de faux vert par commentaire ✓. Vérifié aussi à HEAD par `git show` : 0 occurrence |
| plus de `WorkLunchCard` à l'étape 3 | **PROUVÉE** | fichier supprimé au commit `2f7aa376` ; `grep -c "setup.work_lunch" SetupPage.tsx` → **0 aux deux états** ; `memberWorkLunchCard.int.test.ts:451/:464` (dont un `existsSync`) ✓ |

**Le piège de mesure a été désamorcé** : `SetupPage.tsx` est sale (+187/−1, lot D14 d'une session
voisine). Les sept hunks ne touchent **ni** `STEP_ORDER`, **ni** un `disabled=`, **ni**
`MouthCoreFields`, **ni** un motif veille/durée, **ni** `WorkLunch`. Les numéros de ligne bougent ; le
code mesuré est identique aux deux états.

### C8 — le foyer à deux comptes · **TROIS clauses sur cinq PROUVÉES**

Run réel du 2026-09-03, foyer `9a031111…c0f1` — Ada `owner`, Bo `member` réclamé :

| clause | verdict | mesure |
|---|---|---|
| le même soir, deux bandes | **PROUVÉE** | deux `outbound_messages`, même `metadata.local_date`, **5 s d'écart** (19:30:35 / 19:30:40), maître d'abord (ordre de `PULSE_AUDIENCES`) |
| la question de **courses** au maître seul | **PROUVÉE** | `KEEL_STRIP_SHOP_DONE`/`_LATER` chez Ada, **absents** chez Bo. Garde doublée : `evening_strip.ts:654` (`if (!args.masterOnly) return null;`) + les deux sites d'appel, `masterOnly` **fail-closed** |
| la question de **cuisson** au maître seul | 🔴 **NON PROUVÉE, et la garde est plus faible qu'elle n'en a l'air** | aucune question de cuisson dans ce run. Voir le défaut neuf ci-dessous |
| ③ à chacun | **DÉMENTIE telle qu'écrite — formulation périmée, pas défaut** | Ada n'a **pas** reçu ③ : sa bande ne porte que les courses. C'est le contrat **FF-061** — `openingStep` ne rend **qu'une** étape, ② puis ③ n'arrivent qu'en réponse à un tap (`stepAfter`). L'invariant décrit encore l'état **FF-058** |
| un ✗ du membre écrit **sa** ligne et jamais celle du maître | **PROUVÉE** | même plan, même `dish_index = 1`, même `local_date` : **deux lignes distinctes** dans `protocol_events` — Bo avec `disqualified_reason = food_not_eaten`, Ada **intacte**. Et l'unique `meal_share_outcomes` porte `declared_by = Bo`. Verrouillé par `INSERT with_check (auth.uid() = user_id)` et par l'**absence totale** de policy `INSERT` (écriture par RPC `security definer` seule) |

---

## 4. 🔴 Les défauts NEUFS — nommés, non réparés (les lanes sont closes)

### 🔴 D-E-1 — la question de cuisson a un TROISIÈME appelant, qui n'est pas gardé

`buildSessionQuestion` (`supabase/functions/_shared/keel/accident.ts:1711`) **n'a pas de paramètre
`masterOnly`**, contrairement à `buildShoppingStep` qui porte le sien dans sa signature. La garde vit
donc **chez les appelants**, et deux sur trois l'ont :

| appelant | gardé ? |
|---|---|
| `_shared/keel/day_review_io.ts:90` | ✅ |
| `keel-daily-pulse-v1/index.ts:440` | ✅ |
| **`_shared/chat/accident_tap.ts:379,524`** (`sessionQuestionFor`) | 🔴 **non** — `grep -c "masterOnly\|respondsForHousehold"` sur ce fichier rend **0** |

⇒ **Un membre réclamé qui tape « pas eu le temps » sur un plat se verrait poser une question de
FOYER**, et sa réponse écrirait `cooking_session_states` sous son propre `user_id`. **C'est une
lecture de code, pas un run** — à confirmer par le scénario adversarial ci-dessous. C'est très
exactement la forme de défaut que ce dépôt réapprend : *une garde portée par les appelants n'est
armée que sur les appelants qu'on a regardés.* Le remède structurel serait de la faire descendre
dans la signature, comme `buildShoppingStep`.

### 🔴 D-E-2 — `setupDraftWiring.int.test.ts` ne verrouille qu'une des deux surfaces (C6)

Détaillé en C6. Une ligne manque. Le coût est qu'un invariant du prompt maître se **croit** armé.

### 🟠 D-E-3 — les clés de durée de session survivent sans monteur, et sans garde d'absence

`setup.plan.time`, `setup.plan.time_minutes/_hours/_hint`, `plan.cooking.time_label`,
`plan.cooking.time_15/30/60`, `time_minutes`, `time_hours`, `time_required` vivent encore dans les
deux packs. **Zéro monteur** hors `i18n/`. Le bloc `day_before_*` a reçu sa garde `not.toHaveProperty`
sur les deux packs ; **le bloc durée ne l'a pas**. Asymétrie de garde, pas un champ vivant.

### 🟠 D-E-4 — `lint:i18n` est rouge à 94, hors du gate, et la moitié des violations sont fausses

Détaillé en §1. **Antérieur, mesuré des deux côtés.** Deux choses à trancher par un humain : (1) la
majorité des 94 sont des **génériques TypeScript lus comme du JSX** (`Promise`, `void | Promise`,
`new Map`) — c'est le linter qui a tort ; (2) quelques-unes semblent vraies
(`NotFoundPage.tsx:38`, `salesStructuredData.ts:66`). Tant que le linter rend 94 dont ~91 fausses,
**personne ne verra la 92ᵉ**.

### ✅ Une piste que j'ai ouverte et REFERMÉE, écrite pour qu'on ne la rouvre pas

`generated_from.food_groups` rend `{declared: 0, valid: 0, refused: 0, persisted: 0}` sur **26 runs
sur 26**, avec 21 à 98 lignes d'ingrédient chacun. Ça ressemble trait pour trait au compteur mort
que la mémoire du dépôt décrit — d'autant que `foodGroupWriteGap = valid − persisted = 0` rend un
**« aucun écart »** parfaitement rassurant.

**Ce n'est pas un défaut.** La clé `"group"` n'est demandée au modèle **que lorsqu'un régime
alimentaire est déclaré** : le bloc voyage avec `dietaryRegimePromptLine`, pas dans
`MEAL_SYSTEM_PROMPT`, et « une composition sans régime rend un prompt BYTE-IDENTIQUE à celui de v15 ».
Aucun des 26 runs n'avait de régime. `declared: 0` est donc une **mesure fidèle**, pas un silence.
*(Et le `food_group` des articles de courses vient d'un autre écrivain — le référentiel — ce qui
explique 39/39 articles groupés à côté d'un compteur à zéro.)*

---

## 5. 🔴 CE QUI RESTE ROUGE — avec le geste humain exact

### 🔴 R1 — le parcours de bout en bout au navigateur, deux comptes · **NON TENTÉ**

**Pourquoi, et ce n'est pas une excuse** : il exige une session ouverte, et **aucun agent n'entre de
mot de passe** (`AGENTS.md`, et la mémoire « ne jamais viser un compte sans mot de passe »). Je n'ai
rien tenté, rien approché, et je ne présente aucune partie de ce parcours comme prouvée.

**Le scénario, à jouer par un humain** (§5.12 n°4), sur deux comptes de fixture du tag `qa0903`,
mot de passe `1234567`, **à 320 px et 1280 px**, dans les deux langues :

1. inscription **par SQL** (jamais au navigateur : `EMAIL_DELIVERY_ENABLED=1` avec une vraie clé
   Resend envoie un vrai mail) — fixtures `docs/keel/qa-fixtures/*.sql`, jetons `''` jamais `NULL` ;
2. `/app/setup` — trois étapes ; l'étape 2 sur `MouthCoreFields` ; l'étape 3 **sans** case veille ni
   durée de session, **avec** style de cuisine + nombre de courses, équipement avant ;
3. l'aperçu — le **timing** est-il rendu ? (`same_morning` / `day_before`) ;
4. adopter → `/app/plan` — rang 0, sessions, vagues ;
5. `/app/household` — cadres, ajout (la pop-up monte le **même** `MouthCoreFields`), invitation par
   ligne, traditions ;
6. **le soir, les deux comptes** — bande du maître (① + « qui n'a pas mangé ») et bande du membre
   (③ seul) ; ⚠️ **attendre ③ chez le maître dans la MÊME bande est faux** : FF-061 ne rend qu'une
   étape, ② et ③ suivent le tap ;
7. ✗ du membre → boîte décalée → `/app/progress` des deux.

### 🔴 R2 — le scénario adversarial de D-E-1 · **NON TENTÉ**

**Le geste** : sur un foyer à deux comptes, se connecter en **membre réclamé** (pas le maître), et
taper « pas eu le temps » sur un plat qui porte une préparation. **Attendu si la garde est bonne** :
aucune question de session. **Attendu si D-E-1 est réel** : une question de cuisson de foyer, et une
ligne `cooking_session_states` sous le `user_id` du membre. Vérification :
`select user_id, cook_on from cooking_session_states order by created_at desc limit 5;`

### 🔴 R3 — C4 ① ne peut pas être tranchée sur la base actuelle

**Le geste** : renseigner `household_members.eating_rhythm` sur un foyer de fixture, relancer une
génération, puis rejouer le scan des cellules jour × moment. Tant que la colonne est `NULL`, « chaque
bouche **présente** » n'a pas de référent en donnée.

### 🔴 R4 — le rendu de `StudentProgressPage` sous plancher TCA n'a aucun test

Détaillé en C5(d). **Le geste** (un test, pas une session) : monter `StudentProgressPage` avec
`loadTracking` moqué à `{floor: true}` et asserter `expect(html).not.toMatch(/\d/)` — la forme du
walk que le test Deno fait déjà côté serveur, appliquée à l'écran.

### 🔴 R5 — le poste est instable **par construction**, et c'est un point de CONFIGURATION

Repris du journal 07:4x et confirmé par mon propre créneau : `supabase functions serve` (CLI 2.67.1)
**n'a aucune option d'exclusion** et surveille `supabase/functions/node_modules/`. Le tour de parole
**réduit le nombre d'écrivains, il ne répare rien**. **Décision humaine attendue** : déplacer ce
dossier hors de l'arbre surveillé. *Preuve que la discipline suffit en attendant* : mes deux passages
de gate ont tourné depuis un worktree, et le conteneur edge est passé de **« Up 25 minutes » à
« Up 27 minutes »** — **zéro recréation** pendant que la session voisine tenait son créneau.

---

## 6. Le gate final

`./scripts/agent-gate.sh`, lancé depuis un **worktree détaché créé au HEAD de mes commits**
(`36ec6e0f`), retiré ensuite (le disque était à 1,3 Gio ; il est remonté à 1,9 Gio).

```
EXIT = 0

deno (supabase/functions/_shared/keel/) …… 5119 passés | 0 échoué | 1 ignoré (32 s)
deno check (5 points d'entrée) ………………………… vert
vitest ……………………………………………………………………… 2249 tests, 4 rouges, 4 tolérés, 0 HORS LISTE
typecheck des tests front ………………………………… 140 fichiers, 87 erreurs (liste: 87)
eslint …………………………………………………………………… aucun fichier front modifié
agent-gate: pass
```

**Le nombre qui compte : `(liste: 87)` et non plus `(liste: 93)`.** La tolérance vaut exactement le
réel. Les deux avertissements `ABAISSE sa ligne` ont disparu.

⚠️ **HEAD a encore bougé sous mes pieds pendant que je rédigeais** (`09620159`, orchestration). Mes
deux commits sont vérifiés **ancêtres** du HEAD courant, et `git show HEAD:scripts/.tsc-test-red-baseline`
rend toujours `onboarding … 0` et `planByPersonModel … 2`.

---

## 7. Ligne finale

**VERT sur ce que j'avais à faire** — i18n consolidé (23 tests de parité verts, zéro mojibake), deux
baselines resserrées sans qu'aucune ne monte, C3 démenti écrit sous la phrase qu'il renverse, gate
exit 0 avec une tolérance qui vaut désormais le réel.
**ROUGE, nommé, non maquillé** : le parcours navigateur à deux comptes (R1) et le scénario
adversarial de la question de cuisson (R2) demandent un humain devant l'écran ; C6 est **démentie**
(le cadenas ne verrouille qu'une surface) ; C4 ① et C5(d)-au-rendu n'ont pas de mesure ; et le poste
restera instable tant que `supabase/functions/node_modules/` vivra sous l'arbre surveillé.
**Compte exact des clauses de la grille, recompté sur le tableau ci-dessus — 28 :**

| | C1 | C2 | C3 | C4 | C5 | C6 | C7 | C8 | **total** |
|---|---|---|---|---|---|---|---|---|---|
| clauses | 3 | 3 | 4 | 3 | 4 | 1 | 5 | 5 | **28** |
| prouvées | 2 | 2 | 3 | 2 | 2 | 0 | 5 | 3 | **19** |
| non prouvées / partielles | 1 | 1 | 0 | 1 | 2 | 0 | 0 | 1 | **6** |
| démenties | 0 | 0 | 1 | 0 | 0 | 1 | 0 | 1 | **3** |

Les trois démentis : **C3** « vagues = sessions » (acquis, note portée dans le prompt maître),
**C6** « verrouillé par `setupDraftWiring` » (le cadenas ne lit qu'une surface), et **C8 ③ à chacun**
(formulation FF-058 restée sur un code passé en FF-061 — périmée, pas cassée).
Une clause « prouvée » l'est par une **mesure nommée** : un test lancé, une ligne SQL, ou une lecture
de code dont je dis ce que j'ai cherché **et n'ai pas trouvé**. Aucune ne l'est par déduction.
