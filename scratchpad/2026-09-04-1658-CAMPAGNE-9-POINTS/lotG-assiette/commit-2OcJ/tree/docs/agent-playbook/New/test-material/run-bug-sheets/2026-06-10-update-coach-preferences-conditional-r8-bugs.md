# Bug Sheet - 2026-06-10 update_coach_preferences conditional r8

Cadre: Supabase local, `/functions/v1/test-send-message`, `force_full_ai=true`, `disable_debounce=true`, connexion temporaire `updatecoachpref_updatecoachpref_r8`, cleanup ciblé effectué. Aucun fallback déterministe.

| Bug id | Tours | Famille | Owner | Source amont | Symptôme | Preuve | Correction recommandée | Statut | Notes / tests requis |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `UCP-R8-B01` | T2 | `BF-LEDGER-02` | `effect_ledger_adapter` / intégration ledger `update_coach_preferences` | Contrat ledger encore branché sur legacy `platform_handoff` | Le commit DB réussit mais EffectLedger affiche une entrée `platform_handoff.update_coach_preferences` bloquée | T2: `write_committed=true`, `executed_tools=["update_coach_preferences"]`, DB `coach.tone=direct`, `coach.question_tendency=low`; mais `effect_ledger.entries[0].status=blocked`, `reason_code=missing_platform_handoff_contract`, `counts.committed=0` | Émettre une entrée ledger commit `update_coach_preferences` / `coach_preferences.update` sur chemin local write nominal, et ne plus produire l'entrée platform handoff bloquée | verified | Fix: `effect_ledger_adapter` exclut `update_coach_preferences` / `local_write_flow` du fallback legacy platform handoff et mappe les `committed_effects` vers `coach_preferences.update`; tests ciblés passés: `effect_ledger_adapter_test.ts`, test historique `run_product_help_guard.test.ts --filter "effect ledger maps update_coach_preferences executor commit"`, `update_coach_preferences/tests.ts`. Vérifié par run réel `2026-06-11-update-coach-preferences-conditional-r9`: T2 `effect_ledger.counts.committed=1`, committed entry `coach_preferences.update`, aucune entrée `platform_handoff` bloquée, DB `coach.tone=direct` et `coach.question_tendency=low`. |
