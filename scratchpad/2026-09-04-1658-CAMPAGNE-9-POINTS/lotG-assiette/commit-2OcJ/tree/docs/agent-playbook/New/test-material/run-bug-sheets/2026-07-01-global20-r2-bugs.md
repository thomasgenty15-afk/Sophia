# Bug Sheet — global20-20260701-r2

- Run report: `docs/agent-playbook/New/test-material/qa-run-reports/2026-07-01-global20-r2.md`
- Persona / connexion: `qa-skill` / `conn_global20-20260701-r2` (user `782f26f1-65a2-4298-9a93-6735bf31cc06`)
- Cadre: IA réelle locale, `force_full_ai=true`. Verdict global: **red**.
- Rappel: correction du code interdite pendant le run (guideline) — les lignes ci-dessous sont ouvertes pour traitement post-run.

## Résumé validé (non-bug)

- **BF-ROUTE-02 (r1) — corrigé/vérifié en run**: en T5, l'intention « préférence durable » exprimée pendant un flow coaching actif **exit** désormais le flow et route vers `feature_opportunity` (r1: capturée par le coaching et ignorée). Preuve: T5 `response_owner=feature_opportunity`, `route_reason=feature_opportunity_signal`. Owner: `coaching_recommendation` (reducer `shouldPreserveDispatcherExit`/`isCoherentDispatcherExit`). Statut: `verified` (rerun QA).

## Bugs

### R2-B01 — Rappel one-shot non créé + déni de capacité
- Tours: T6, T7
- Famille: **BF-EFFECT-02** (Effet attendu absent) ; secondaire BF-EFFECT-04 (executor fragile) / renderer.
- Domaine owner: pipeline direct effects (admission/executor `create_one_shot_reminder`) + contrat rappel visible (`normal_reply`).
- Source amont: route→executor. Le frame détecte l'effet et `route_decision.direct_effects_to_run=["create_one_shot_reminder"]`, mais l'EffectLedger reste vide (`requested=0`) et `scheduled_checkins=0`. Le message visible nie la capacité (« je ne le recrée pas », « je ne peux pas te confirmer ce rappel ici »), ce qui suggère aussi une fuite de la consigne de limitation rappel vers le chemin de **création**. À rapprocher de l'échec connu `one_shot_reminder_insert_failed` (`operation_runtime_pipeline_test`).
- Symptôme visible: Sophia refuse de créer un rappel qu'elle sait normalement créer ; aucun rappel en DB.
- Preuve système: T6 frame `create_one_shot_reminder` explicit high, `direct_effects_to_run=[...]`, ledger `requested=0/committed=0/entries=[]`, `trace_error=null`, `scheduled_checkins=0` (vérifié via PostgREST après T6 et en fin de run).
- Correction attendue: garantir la chaîne `direct_effects_to_run` → admission ledger (`requested>=1`) → commit `scheduled_checkins` ; confiner la consigne « pas d'annulation/modif depuis le chat » aux rappels **existants** dans l'EffectLedger (jamais sur une création).
- Statut: `open`
- Fix reference: —
- Tests requis: (positif) rappel explicite ⇒ `scheduled_checkins` +1 + message de confirmation ; (anti-faux-positif) demande de création sous consigne de limitation ⇒ création quand même ; (paraphrase) formulations variées de rappel ⇒ même effet ; (intégration) `operation_runtime_pipeline_test` rappel local vert.

### R2-B02 — Préférence mémoire non persistée (fausse promesse)
- Tours: T18 (contexte T5)
- Famille: **BF-MEMORY-01** (Promesse mémoire non persistée).
- Domaine owner: memory runtime (planner → writer) + `feature_opportunity` visible.
- Source amont: le planner décide `memory_mode=light` (`response_intent=store_user_preference_for_future_use`) mais aucune écriture n'aboutit (`memory_items=0`, `user_topic_memories=0`). Le visible promet « je peux m'appuyer là-dessus dans mes prochaines réponses ».
- Symptôme visible: Sophia dit garder/utiliser une préférence, rien n'est stocké.
- Preuve système: T18 `memory_mode=light` ; `memory_items=0` et `user_topic_memories=0` juste après le tour et en fin de run.
- Correction attendue: soit persister réellement la préférence (writer aligné sur le plan `light`), soit ne rien promettre et se limiter au pointer produit « Preferences coach ». Aligner claim visible et effet durable.
- Statut: `open`
- Fix reference: —
- Tests requis: (positif) « retiens X sur moi » ⇒ écriture mémoire +1 ; (cohérence) si pas d'écriture ⇒ pas de promesse d'usage futur ; (réserve) exclure/vérifier la compaction topic asynchrone.

### R2-B03 — Préférence de coaching non appliquée au runtime
- Tours: T15, T16 (préférence posée T5)
- Famille: **BF-PREF-01** (Préférence non appliquée runtime).
- Domaine owner: preference runtime policy + renderer `coaching_recommendation`.
- Source amont: même après avoir exprimé « direct, sans jargon » (T5) puis répété la contrainte (T16), le rendu coaching réutilise le vocabulaire carte/technique et propose parfois moins concret. Aucune préférence stockée n'est lue au rendu (lié à R2-B02).
- Symptôme visible: le user doit répéter sa contrainte sans effet.
- Preuve système: T15/T16 `response_owner=coaching_recommendation`, jargon présent malgré demandes explicites.
- Correction attendue: appliquer les préférences de coaching (une fois persistées) au rendu ; rendre la nomenclature carte/technique optionnelle.
- Statut: `open`
- Fix reference: —
- Tests requis: préférence `tone=direct/no_jargon` ⇒ rendu sans « carte/technique ».

### R2-B04 — Jargon carte/technique imposé malgré contrainte explicite
- Tours: T4, T15, T16
- Famille: **BF-INTAKE-03** (Contrainte explicite perdue).
- Domaine owner: renderer/visible `coaching_recommendation`.
- Source amont: la politique de nomenclature verbalise « carte d'attaque / technique X » même quand le tour contient « sans jargon ni nom de méthode ». Contraste net: en flow `feature_opportunity`/`normal_reply` (T19), le conseil est concret et sans jargon.
- Symptôme visible: vocabulaire produit interne imposé alors que le user demande du langage normal.
- Preuve système: T4/T15/T16 textes visibles avec « carte/technique » ; T19 sans jargon.
- Correction attendue: découpler le levier interne (technique) du rendu visible ; suppression de la mention carte/technique sous contrainte de style.
- Statut: `open`
- Fix reference: —
- Tests requis: prompt « sans nom de technique » ⇒ rendu sans nomenclature carte/technique ; reco reste concrète.

### R2-B05 — Sur-clarification en ouverture de coaching
- Tours: T1→T3 (culmine T3)
- Famille: **BF-INTAKE-01** (Slot fourni mais redemandé).
- Domaine owner: `coaching_recommendation` (intake/dispatcher local).
- Source amont: 3 questions de narrowing consécutives alors que le signal coaching est `high` et l'info déjà suffisante dès T3.
- Symptôme visible: sensation d'interrogatoire avant toute aide.
- Preuve système: T1/T2/T3 tous coaching, aucune reco avant T4.
- Correction attendue: seuil de convergence — après 1–2 slots clairs sur signal `high`, produire une reco.
- Statut: `open`
- Fix reference: —
- Tests requis: signal coaching `high` + 1 slot clair ⇒ ≤1 clarification avant reco.

### R2-B06 — Release safety légèrement collant
- Tours: T12, T13
- Famille: **BF-SAFETY-01** (Désescalade/priorité safety) — sévérité faible.
- Domaine owner: `safety` (reducer local de sortie de flow).
- Source amont: après désescalade robuste (T10–T13) + facteurs protecteurs, une question neutre reste bloquée ; même quand le skill déclare « stabilisé » (T13), le flow ne rend pas la main au même tour. Release effectif seulement à T14.
- Symptôme visible: question pratique différée un tour de trop.
- Preuve système: T12/T13 `route_reason=active_safety_crisis`, `blocked_paths` complet ; `conversation_risk.should_exit_flows=false` ; T14 release OK.
- Correction attendue: permettre un hand-back au tour suivant quand désescalade robuste + sujet neutre, sans exiger un nouveau signal fort. Comportement conservateur globalement défendable.
- Statut: `open` (sévérité faible — à arbitrer côté doctrine safety, risque de release trop rapide)
- Fix reference: —
- Tests requis: désescalade robuste + facteurs protecteurs + sujet neutre ⇒ release au tour suivant ; ne pas release sur simple « ça va » isolé.
