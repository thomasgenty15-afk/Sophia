# Run Bug Sheet - doctrine5

## Metadata

- Date: 2026-08-13
- Run report: `docs/agent-playbook/New/test-material/qa-run-reports/2026-08-13-doctrine-5-coachs.md`
- Run id: `doctrine5`
- Persona / scenario: 5 coachs jetables aux doctrines volontairement
  contradictoires (D1 jeûne du matin, D2 tout animal, D3 végétal strict,
  D4 zéro ultra-transformé, D5 matin chargé/soir minimal), un élève jetable par
  coach, **informations utilisateur rigoureusement identiques** aux cinq.
  42 tours exploitables + 2 générations de 4 jours.
- Verdict run: `red` (chat) / `green` (génération)
- Validite QA: **valide**. Entrée produit `chat-inbound-v1`, doctrines publiées
  par `coach-doctrine-v1` (donc `compiled_prompt` réellement compilé et prouvé
  non vide et distinct 5/5), Supabase local, aucune commande de reboot, aucune
  correction de code pendant le run. Deux réserves nommées : incident de pile
  causé par une session parallèle (3 tours jetés), et `generate-meal-v1` /
  `meal_generation.ts` sales dans le dépôt au moment de l'étape 2.
- Agent owner: QA doctrine

## Synthese

- **Familles dominantes**: `BF-ROUTE-01` (16 tours / 38 %), `BF-EFFECT-03`
  (18 lignes durables au motif faux), `BF-LEDGER-02` (4 tours), plus deux
  défauts de câblage trouvés à la lecture et confirmés par mesure
  (`R1-B05`, `R1-B07`).
- **Bug le plus bloquant**: `R1-B01` — le skill `plan_question` capture les
  tours de conseil ordinaires ; la doctrine du coach est chargée
  (`reason: loaded`, `beliefs_kept: 3/3`) et ne parle jamais. Quatre coachs aux
  doctrines opposées reçoivent le **même texte anglais au caractère près** sur
  « je dîne quoi ce soir ? ».
- **Fix architectural prioritaire**: rendre la doctrine **prioritaire sur le
  tier-0 de `plan_question`** — un aliment couvert par `doctrine.forbidden` ou
  par `coach_food_items.stance='excluded'` est une question **déjà tranchée par
  le coach**, elle ne peut pas être escaladée vers lui.
- **Rerun requis**: oui, après `R1-B01` et `R1-B02`, sur la même matrice
  5 × 5 avec rejeu ×3 (le dispatcheur est stochastique : I1 sur D2 est passé
  1 fois sur 3).

### État après le RUN 2 (2026-08-13, rapport `…-doctrine-5-coachs-run2.md`)

- `R1-B01`, `R1-B02`, `R1-B03`, `R1-B07` : **`verified`** en run réel.
  Escalade **16 → 4** tours · réponses anglaises **16 → 0** · lignes
  `contract_change_requests` **18 → 4** · matrice principale **12/5/8 → 19/4/2** ·
  **0 rouge sur les 10 rejeux** (5 au run 1). Étape 2 inchangée: 0 exclu,
  Jaccard 0,212, 0 titre commun.
- `R1-B09` : **`fixed`** côté `protocol_compiler.ts`. La moitié `doctrine.ts`
  (« He answers: ») n'a **pas** été touchée — le fichier appartient à une session
  parallèle (FF-041 en cours). À reprendre quand il sera commité.
- **Nouveau bug le plus bloquant**: `R1-B08` — le verrou de sortie est désormais
  le premier contributeur (10 tours sur 42), parce que le routage ne le masque
  plus. Il ne s'est pas aggravé; il est devenu visible.
- **Rerun requis**: oui, après `R1-B08`, `R1-B01b` et `R1-B01c`.

## Bug Ledger

| Bug id | Tours | Famille | Domaine owner | Source amont | Symptome visible | Preuve systeme | Correction attendue | Statut | Fix reference | Tests requis |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `R1-B01` | I4 sur D1/D2/D4/D5, I1-r2, I1-r3, I3-r2, I3-r3, I4-r2, ADV-implicite D3/D4, ADV-arbitrage D1 (16 tours) | `BF-ROUTE-01` | `sophia-brain/dispatcher` | `dispatcher/dispatcher.prompts.ts` règle 6-bis : `skill_signals.plan_question` est émis pour des tours de CONSEIL, pas d'exécution. 7 des 16 tours ne nomment aucun aliment. | Réponse anglaise d'escalade, **identique au caractère près chez 4 coachs opposés**, sur « il est 19h, je dîne quoi ? » | `keel.doctrine.variant → reason:"loaded", beliefs_kept:3/3` sur les 16 tours ; le bloc doctrine était chargé et n'a pas servi. Le même fichier documente déjà 2/3 d'émissions fautives sur « je mange au restaurant ». | Resserrer 6-bis : `plan_question` exige (a) une ligne de plan **nommément visée** et (b) un aliment ou un créneau **explicite dans le message**. Une demande de proposition (« je mange quoi ») n'est jamais une question d'exécution. Ajouter la contre-forme mesurée ici au prompt, comme la contre-forme « resto » y est déjà. | `verified` | run 2 (2026-08-13) : escalade 16→4 tours, 0 rejeu rouge | positif (« je peux remplacer le poulet du midi par du thon ? » → plan_question) + paraphrase (5 formulations de « je dîne quoi ce soir » → jamais plan_question) + anti-faux-positif (rejeu ×3, le signal est stochastique) |
| `R1-B02` | D3/I3, D4/I3, D5/I3, D2/I3-r2, D2/I3-r3 | `BF-ROUTE-01` | `sophia-brain/skills/plan_question` | Le tier-0 résout son verdict contre `plan_commitments` + `swap_policy` **seuls**. `loadPublishedDoctrine` est appelé au même tour (`router/run.ts:1427`) et n'est pas une entrée du skill. | « **volaille** is outside what your coach set […] Your question is with them now » alors que D3 exclut la volaille en `forbidden` **et** en `coach_food_items` **et** en `coach_food_rules` | 3 sources d'exclusion en base pour cet aliment ; ligne `contract_change_requests` créée quand même. | Poser une **pré-garde de doctrine** avant le tier-0 : si l'aliment demandé est couvert par `doctrine.forbidden` ou `coach_food_items.stance='excluded'` du coach de cet élève, la question est **déjà tranchée** — on répond la position du coach, on n'escalade pas. Corollaire : n'escalader que ce sur quoi le coach est **silencieux**. | `verified` | run 2 : D4/I3 refuse par la doctrine, 0 escalade écrite | positif (aliment exclu → refus doctrinal, 0 ligne `contract_change_requests`) + anti-FP (aliment non couvert → escalade conservée) + intégration (les 5 doctrines du run) |
| `R1-B03` | tous les tours d'escalade (16) | `BF-STATUS-03` | `sophia-brain/skills/plan_question` | `skills/plan_question/renderer.ts` : `escalationText`, `DENY_TEXT` et `tier0AllowedText` sont des **littéraux anglais** ; seul `foodLabel()` traverse le `LocalePack`. | Élève `fr-FR`, `voice.language: fr-FR` → réponse anglaise, avec des étiquettes françaises collées : « **volaille** is outside what your coach set for the **protéines maigres** line » | La ligne `contract_change_requests` écrite au même instant porte `content_locale = 'fr-FR'` : **la locale est connue du skill**. | Sortir les phrases du renderer dans le `LocalePack` déjà passé (R3), au même titre que `foodLabel`. Un renderer déterministe n'est pas une exemption de langue. | `verified` | run 2 : 0 réponse anglaise sur 42 tours + test d'invariant | positif (élève `fr-FR` → prose française) + anti-FP (élève `en-GB` inchangé) + invariant « aucun littéral de phrase hors pack » |
| `R1-B04` | D5/I2, D1/ADV-arbitrage, D1/I4, D5/I4, D2/I3, D2/ADV-implicite (18 lignes au total) | `BF-EFFECT-03` + `BF-EFFECT-01` | `sophia-brain/skills/plan_question` (compilateur d'effet) | `reason_code` dérivé sans rapport avec le message ; et l'effet est écrit sur des tours où l'élève ne demande **aucun** changement de plan. | Invisible pour l'élève. Le coach lira un digest décrivant un élève qui n'existe pas. | 18 lignes `contract_change_requests` sur 42 tours (43 %). `dislikes_food` sur « comment je place mes protéines » et sur « j'ai une séance de muscu à 7h » ; `social_event` sur « il est tard, je dîne quoi ». **2 lignes écrites sur des tours dont la réponse visible venait du VERROU** (D2/I3, D2/ADV-implicite) : effet durable jamais annoncé à l'élève. | (a) Ne compiler un `reason_code` que depuis un motif **présent dans le message**, sinon `other`. (b) Un tour qui n'ouvre pas d'escalade visible ne doit pas écrire d'escalade silencieuse : l'effet et la phrase se décident au même endroit. | `open` | — | positif (motif explicite → bon `reason_code`) + anti-FP (aucun motif → `other`, jamais `dislikes_food`) + invariant « pas de ligne durable sans phrase qui l'annonce » |
| `R1-B05` | transverse (mesuré hors tour) | `a classifier` — câblage | `_shared/keel/doctrine_loader.ts` + `protocol_loader.ts` | `coach_food_items` avec `stance='encouraged'` **n'a aucun lecteur au runtime**. `doctrine_loader.ts:402` ne lit que `stance='excluded'` ; `protocol_loader.ts` ne lit que `coach_food_rules` / `coach_timing_rules`. | Le coach saisit « Foie de veau, au moins 2 portions par semaine » sur `/coach/protocol` et rien ne le lit. Seul le groupe passe. | Mesuré sur les blocs réellement servis : **0 des 10 items encouragés des 5 coachs** n'apparaît dans le bloc doctrine ni dans le bloc protocole. | Décider explicitement : soit l'écran Aliments alimente le bloc (une section « aliments nommés » à côté des groupes), soit le grain fin est retiré de l'écran. L'état actuel — une grammaire fermée, un CHECK à 4 gabarits, un « pourquoi » saisi, et zéro lecteur — est le pire des trois. | `open` | — | test de fil : un item `encouraged` posé en base doit apparaître dans le bloc servi par le chargeur de production (ou l'écran doit cesser de le proposer) |
| `R1-B06` | tous | `BF-TEST-01` | `chat-inbound-v1` / `sophia-brain` | `chat_messages.metadata` ne porte ni `response_owner`, ni `handled_by`, ni `selected_handler`, ni `route_reason` : seulement `{buttons, channel, agent_mode, request_id, is_proactive, delivery_reason, in_reply_to_client_message_id}`. | Aucun symptôme pour l'élève. | Le propriétaire de chacun des 42 tours a dû être **reconstitué depuis le texte de la réponse et depuis les effets DB**. La trace courte exigée par `01-qa-run-report-structure.md` n'est pas disponible en base. | Écrire le propriétaire du tour dans `metadata` (ou dans `turn_summary_logs`, dont les colonnes de contexte sont déjà NULL). Sans ça, tout run QA conversationnel devine. | `open` | — | invariant : après un tour, `metadata.response_owner` est non nul |
| `R1-B07` | transverse (D5) | `a classifier` — câblage | `_shared/keel/doctrine_loader.ts:402` | Le `select` sur `coach_food_items` ne prend que `label` : `frequency_template` et `cutoff_local` sont perdus. | L'item de D5 « Viande rouge au dîner », `not_after 19:00`, arrive dans le bloc sous « FOODS THIS COACH DOES NOT PUT ON A PLATE — Never suggest these ». Le coach a écrit « pas après 19h » ; l'agent lit « jamais ». | Bloc servi relu : les deux exclus de D5 sont rendus **sans leur heure**. Sur une doctrine dont le parti pris EST une heure, la doctrine est déformée. | Porter le gabarit dans le rendu (« pas après 19:00 » / « pas au créneau X »), ou refuser un `excluded` porteur d'un gabarit temporel à l'écriture. Une contrainte que la base sait exprimer et que le prompt aplatit est pire qu'une contrainte absente. | `verified` | run 2 : « rien de dense après 19h » cité en réponse | positif (`not_after` rendu avec son heure) + anti-FP (exclusion plate inchangée) |
| `R1-B08` | D1/I3, D2/I3, D2/I4-r3, D2/ADV-implicite | `BF-LEDGER-02` | `sophia-brain/skills/_shared/keel_output_locks.ts` | Le verrou remplace la réponse **entière** par `signAsCoach(instead)` (`:200`). | « un café noir ou un grand verre d'eau au réveil, et le premier vrai repas à midi — Baptiste Rouve » : un fragment, sans phrase, sans « non », en réponse à une question fermée « je peux ou pas ? ». | 4 morsures sur 42 tours (9,5 %), dont 2 sur les 3 rejeux de I3/I4 sur D2. `doctrine.ts:736-790` documente le défaut et la moitié prompt du correctif déjà livrée. | La moitié prompt ne suffit pas (mesuré). Deux pistes : (a) le remplacement compose une phrase autour de l'`instead` au lieu de le poster nu ; (b) le matcher exempte la négation **quelle que soit sa position** dans la phrase, pas seulement immédiatement avant le terme. Voir la cicatrice déjà écrite dans `forbidden_matcher.ts`. | `open` | — | positif (interdit expliqué sans morsure) + paraphrase FR/EN + anti-FP (une vraie recommandation de l'interdit mord toujours) |
| `R1-B09` | transverse | `a classifier` — copie | `_shared/keel/protocol_compiler.ts` + `doctrine.ts` | Genre du coach codé en dur au masculin : `protocol_compiler.ts:733, 772, 801, 905, 976` (« WHAT **HE** BUILDS PLATES WITH », « **he** does not build with it », « **HIS** RULES ») et `doctrine.ts:806` (« **He** answers: »). | Le bloc servi à l'élève d'Hélène Vasseur s'ouvre sur « HÉLÈNE VASSEUR'S FOOD MAPPING — WHAT HE BUILDS PLATES WITH ». | Blocs relus pour les 5 coachs : 2 coachs sur 5 portent un prénom féminin, les 5 blocs disent « he ». | Formuler sans genre (« this coach », « their rules »), comme le reste du bloc doctrine le fait déjà. Aucun coût, et le prompt cesse de suggérer un genre au modèle. | `fixed` | protocol_compiler.ts dégenré ; doctrine.ts NON TOUCHÉ (session parallèle) | invariant : aucun pronom genré dans un bloc compilé |
| `R1-B10` | D4/I2 | `a classifier` — `SILENCE IS NOT A POSITION` | `sophia-brain` composeur | Sur un sujet non tranché (produits laitiers), la réponse rend la balle à l'élève au lieu de dire que le coach n'a pas tranché. | « ou du fromage blanc **si tu le tolères dans ta méthode** » — laisse croire à une règle laitière que l'élève serait censé connaître. | Le bloc porte la consigne explicite `SILENCE IS NOT A POSITION`. Elle tient 2 fois sur 2 sur l'alcool (D2, D3) et cède ici. | Renforcer la consigne sur la forme « renvoyer la décision à l'élève » : c'est une attribution déguisée, pas une abstention. | `open` | — | positif (sujet non couvert → « il n'a pas tranché ») + anti-FP (sujet couvert → position citée) |
| `R1-B11` | D3/I1 | `a classifier` — composeur sans contenu | `sophia-brain` composeur | Réponse méta sans aucun aliment nommé : « remplace ce qui t'ennuie par un autre format de la même logique ». | L'élève demande une idée de petit-déjeuner, reçoit une consigne de méthode. | La doctrine était chargée (3 beliefs) et « légumineuse » apparaît — donc ce n'est **pas** générique, c'est vide. | Une demande de proposition doit produire une proposition nommée. | `open` | — | positif (proposition nommée) |
| `R1-B12` | D2/I5 | `a classifier` — doctrine générique + fuite de vocabulaire système | `sophia-brain` composeur | « Je m'appuie d'abord sur la méthode du coach actif dans cette conversation, puis sur ce qui est explicitement présent dans ton **contexte visible** […] » | Réponse **valable pour n'importe lequel des cinq coachs** : la doctrine n'a rien changé. Et elle décrit le fonctionnement interne de l'agent. | Les 2 rejeux de la même intention (I5-r2, I5-r3) restituent parfaitement les 3 `beliefs` : instable, pas structurel. | Interdire le registre « voici comment je fonctionne » dans une réponse de fond ; une question sur la logique du coach se répond par ses convictions. | `open` | — | rejeu ×3 sur la même intention, 3/3 avec contenu de doctrine |
| `R1-B13` | D5/I3 | `BF-AGENDA-01` | `sophia-brain` pipeline de réponse finale | Deux composants écrivent dans la même bulle : une escalade anglaise, puis « Et tu as mangé quoi avec ? » en français. | Réponse incohérente, deux registres, deux langues. | Tour D5/I3, réponse relue en base. | Un tour d'escalade ne doit pas se voir suffixer une relance de journal alimentaire. | `open` | — | invariant : une réponse d'escalade est seule dans sa bulle |
| `R1-B01b` | run 2 : D3/I3 (poulet), D1/ADV-arbitrage (muscu 7h) | `BF-ROUTE-01` | `sophia-brain/router/run.ts` (gate de routage) | RÉSIDUEL de `R1-B01`. `requestedFoodGroupForResolution` traite déjà `requested === prescribed` sur un `food_swap` comme « le modèle a recopié la ligne » et le met à `null` — mais le GATE de routage n'applique pas cette règle, donc le tour reste dans la lane et tombe sur `unresolved_food_group`. | Escalade française sur une question de méthode (« j'ai envie de poulet », « je mange quoi avant ma séance »). | `conversation_turn_traces.turn_frame` : `{kind: food_swap, requested_food_group: "lean_protein", prescribed_food_group: "lean_protein"}` et `{… "whole_grain", "whole_grain"}`. Le dispatcher mappe en prime « poulet » sur `lean_protein` alors que `poultry` existe. | Appliquer au gate la MÊME règle qu'au resolver : importer `requestedFoodGroupForResolution` et considérer une identité comme « aucun remplacement nommé ». Une règle appliquée d'un seul côté d'une frontière est une fente, et c'est la deuxième fois que cette lane en paie une. | `open` | — | positif (identité ⇒ chemin général) + anti-FP (vraie substitution inter-groupes ⇒ lane conservée) + rejeu ×3 |
| `R1-B01c` | run 2 : D5/I3 (pâtes), D2/ADV-implicite (couscous) | `BF-ROUTE-01` | `sophia-brain/router/run.ts` (gate de routage) | RÉSIDUEL de `R1-B01`. Le signal nomme un `prescribed_food_group` qui ne correspond à AUCUNE ligne de cet élève ⇒ `commitment_not_identified` ⇒ escalade sur une ligne qui n'existe pas. Le commentaire de FF-016 range explicitement ce cas en « RESTE OUVERT, ET CONSIGNÉ », faute d'avoir été mesuré. | Escalade avec `commitment_id: null`, `slot_key: null` : on demande au coach d'arbitrer une ligne dont personne ne peut dire laquelle. | `turn_frame` : `{requested: "refined_grain", prescribed: "whole_grain"}` chez un élève dont les deux lignes sont `lean_protein` et `non_starchy_veg` ; et `{requested: "shellfish", prescribed: null}` sur « couscous royal ». | Fermer le cas nommé : `noGroundedPrescription` seul suffit à renvoyer au chemin général — il n'y a pas de ligne à substituer. C'est §7 mot pour mot, et le gate FF-010 a déjà pris ce même arbitrage pour les foyers. | `open` | — | positif (prescrit absent du plan ⇒ chemin général) + anti-FP (prescrit présent ⇒ lane conservée) |
| `R1-B08b` | run 2 : D1/I4 | `BF-LEDGER-02` | `sophia-brain/skills/_shared/keel_output_locks.ts` | Variante de `R1-B08`: `resolveDoctrineReplacement` prend le PREMIER `forbidden` dont le token a mordu, sans regarder de quoi parle le tour. | Question sur le DÎNER, réponse = l'`instead` du PETIT-DÉJEUNER (« un café noir ou un grand verre d'eau au réveil »). Le fragment n'est pas seulement nu, il est hors sujet. | Tour D1/I4 du run 2, réponse relue en base. | Choisir le `instead` du token effectivement mordu dans CE tour, ou composer autour plutôt que substituer (cf. `R1-B08`). | `open` | — | positif (interdit du soir ⇒ instead du soir) + anti-FP |

## Ce qui est VERT et doit le rester (garde anti-régression)

| Constat | Preuve | À ne pas casser |
| --- | --- | --- |
| Le montage compile réellement | 5 `compiled_prompt` de 3 598 à 3 706 car., 5 hashs distincts, publiés par `coach-doctrine-v1` | `publish` doit continuer d'écrire `compiled_prompt` **et** `compiled_prompt_hash` |
| Le chargeur sert la bonne doctrine, toujours | `reason: "loaded"`, `delegated: false`, `beliefs_kept: 3/3` sur **42/42** tours et 2/2 générations. Aucun `no_published_doctrine` | — |
| Aucune fuite entre coachs | balayage des 42 réponses contre les marqueurs des 4 autres doctrines et les 4 autres noms : **0 fuite**, 4 occurrences toutes en phrase de refus | l'isolation par `coach_clients` |
| `SILENCE IS NOT A POSITION` tient | 2/2 sur l'alcool ; D3 sépare explicitement l'avis du coach du sien | — |
| L'arbitrage sert, quand il est atteint | D5/ADV : l'`arbitration` n°1 restituée quasi mot pour mot | — |
| **La génération respecte la doctrine** | 2 plans × 4 jours × 3 repas : **0 aliment exclu**, encouragés présents **chaque jour**, Jaccard 0,228, **0 titre commun**, `honours_belief_keys` cite les 3 convictions de chaque coach | la lane `generate-meal-v1` est la référence : c'est elle qui montre ce que le chat devrait faire |

## Decisions / Arbitrages

| Date | Decision | Pourquoi | Owner | Reference |
| --- | --- | --- | --- | --- |
| 2026-08-13 | Les 3 tours D3/I1 en `502` sont **jetés**, pas comptés | Incident de pile causé par une session parallèle (runtime edge recréé toutes les 10-25 s pendant ~13 min). Le message user orphelin a été effacé avant chaque reprise pour ne pas doubler l'historique. Aucune commande de reboot passée. | QA | rapport §1-bis |
| 2026-08-13 | L'étape 2 est jouée malgré `generate-meal-v1` / `meal_generation.ts` sales | Le lot en cours (locale d'artefact) ne touche ni au chargement de doctrine ni au mapping ; la mesure de doctrine reste valable. Rien n'a été stashé. Réserve consignée. | QA | rapport §1-bis |
| 2026-08-13 | Les 5 doctrines sont écrites en `goal_scope: []` (global) | Le run mesure la doctrine, pas la portée par objectif — déjà couverte par `D1_doctrine_by_goal`. Toute variation entre élèves devait venir de la doctrine, pas du filtre. | QA | rapport §2.1 |

## Verification

| Date | Bug id | Verification | Resultat | Reference |
| --- | --- | --- | --- | --- |
| 2026-08-13 | `R1-B01` | rejeu ×3 de l'intention I1 sur D2, formulations différentes | 1 vert / 2 rouges — le signal est stochastique, l'échec n'est pas un flake | rapport §3 |
| 2026-08-13 | `R1-B02` | 3 coachs, 3 aliments exclus nommément par leur propre doctrine | 3/3 escaladés au lieu d'être refusés | rapport §4 (I3) |
| 2026-08-13 | `R1-B05` | recherche des 10 libellés `encouraged` dans les blocs réellement servis | 0/10 dans le bloc doctrine, 0/10 dans le bloc protocole | `blocks.json` |
| 2026-08-13 | `R1-B04` | comptage `contract_change_requests` du run | 18 lignes / 42 tours, dont 2 sans phrase d'annonce | requête SQL, rapport §7 |
| 2026-08-13 | `R1-B01` | run 2, même matrice 5×5 + 10 rejeux | escalade 16→4 tours, 0 rejeu rouge | rapport run 2 §3 |
| 2026-08-13 | `R1-B02` | 3 coachs, aliment exclu nommément | D4 refuse PAR la doctrine, 0 escalade écrite ; D2 routé au chemin général | rapport run 2 §4 (I3) |
| 2026-08-13 | `R1-B03` | balayage des 42 réponses | 0 marqueur anglais ; + test d'invariant « l'anglais est inchangé octet pour octet » | `renderer_locale_test.ts` |
| 2026-08-13 | `R1-B07` | bloc servi relu + réponse réelle | « - Viande rouge au dîner — not after 19:00, fine before that » ; D5/I4 cite « rien de dense après 19h » | `blocks.json`, rapport run 2 §4 |
| 2026-08-13 | `R1-B09` | blocs des 5 coachs | plus aucun « he/his » ; réponses réelles disent « elle » pour les coachs femmes | rapport run 2 §4 (I5) |
| 2026-08-13 | `R1-B05` | non corrigé — re-mesuré | toujours 0/10 items encouragés dans les blocs servis | `blocks.json` |
