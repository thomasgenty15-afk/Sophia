# Rose — Global 15 — R2 — Feuille De Suivi Bugs

Rapport associé: `docs/agent-playbook/New/test-material/qa-run-reports/2026-07-02-rose-global15-r2.md`

- Familles dominantes: `BF-EFFECT-03` (payload/effet durable faux, 2 occurrences + 1 conséquence en recap), `BF-AGENDA-01` (multi-intention incomplète, 1 occurrence), `BF-ROUTE-01` (owner discutable, 1 occurrence mineure).
- Aucun tour `red` dans ce run. Le bug système le plus important (`BF-EFFECT-03`) est une régression connue déjà ouverte ailleurs dans le registre, pas une découverte nouvelle — ce run en apporte une deuxième confirmation indépendante avec des items différents.

| Bug id | Tours | Famille | Domaine owner | Source amont | Symptome visible | Preuve systeme | Correction attendue | Statut | Fix reference | Tests requis |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `rose-global15-r2-B01` | T1 | `BF-EFFECT-03` | `sophia-brain/tools/always_on/track_progress_plan_item/db.ts` (`logPlanItemProgressV2`) | Insert `user_plan_item_entries` sans update correspondant de `user_plan_items.current_reps` | Aucun côté user dans ce tour (Sophia félicite sans afficher de compteur) | `user_plan_item_entries` contient 1 ligne `checkin/completed` pour l'item `3db6278b-...`, mais `current_reps=0` (target_reps=3) immédiatement après | Le writer doit incrémenter `current_reps` après l'insert (miroir du contrat frontend `useDashboardV2Logic.ts:576-650`) ou une projection doit le recalculer à la lecture | `fix_applied` (rerun requis) | Miroir du contrat dashboard dans le writer : `planItemPatchForCompletedEntry` (`track_progress_plan_item/db.ts`) — incrément `current_reps` + transition de statut (habits → in_maintenance à la cible, missions → completed, boolean immédiat) appliqués après l'insert quand `status=completed` ; missed/partial n'incrémentent jamais ; patch non-bloquant (l'entry reste la vérité si le patch échoue). Tests `db_counter_test.ts`. Résout aussi `R2-W01`. | Positif: commit progression -> entry + `current_reps` cohérents. Anti-faux-positif: statut/question -> pas d'entry, pas de compteur touché. Intégration: assertion DB après appel `test-send-message` réel. |
| `rose-global15-r2-B02` | T2 | `BF-EFFECT-03` (même bug, item différent) | idem B01 | idem B01 | Aucun côté user | `user_plan_item_entries` 1 ligne `progress/completed` pour `d62d828a-...`, `current_reps=0` (target_reps=2) après le tour | idem B01 | `fix_applied` (même fix que B01, rerun requis) | cf. B01 (`planItemPatchForCompletedEntry`) — testé sur habit ET mission. | idem B01, avec un deuxième item pour prouver que le fix couvre tous les items, pas un cas particulier |
| `rose-global15-r2-B03` | T11 | `BF-STATUS-01` (conséquence de B01/B02) | `sophia-brain/router/plan_snapshot_runtime.ts` (projection de statut utilisée pour le recap) | `current_reps` jamais mis à jour en amont (B01/B02) -> rien de concret à projeter | Recap "où j'en suis sur mon plan" ne mentionne aucune des 2 progressions committées dans le même run | Aucun `logged_progress_id`/compteur cité dans la réponse malgré 2 entries réelles en DB au moment du tour | Corriger B01/B02 devrait résoudre ce symptôme automatiquement ; si le recap doit rester robuste indépendamment, le lire depuis `user_plan_item_entries` en direct (comme `guard_v2_plan_item_activation()` le fait déjà pour l'activation) plutôt que depuis `current_reps` seul | `fix_applied` indirect (rerun requis) | Résolution attendue via B01/B02 : `current_reps` désormais maintenu, la projection du recap a de la matière. Si le rerun montre encore un recap vague, fix résiduel = lecture directe de `user_plan_item_entries` par la projection. | Recap positif (progrès réellement committé cette semaine) vs recap sans progrès (ne doit rien inventer) |
| `rose-global15-r2-B04` | T10 | `BF-AGENDA-01` | `TurnAgenda` / pipeline de réponse finale (arbitrage multi-intention) | Message à deux intentions (annulation implicite + question produit) ; seule la deuxième est traitée, la première n'est ni confirmée ni refusée | Sophia répond uniquement sur la langue de l'appli, aucune mention du rappel dont l'utilisateur venait de demander l'annulation | `scheduled_checkins` `3ee6b5ab-...` reste `status=pending` après le tour (comportement correct côté effet, puisqu'aucune capacité de cancel n'existe), mais la réponse ne l'accuse pas | Garantir qu'une intention secondaire explicite reçoive au moins un accusé de réception honnête, même non actionnable | `fix_applied` (rerun requis) | Bloc canonique one-shot (`one_shot_reminder_prompt_contract.ts`) : "laisser tomber" ajouté aux formes d'annulation + « même si le message contient d'autres demandes, accuse d'abord cette demande en une phrase avant de répondre au reste ; ne l'ignore jamais en silence ». Test « cancel requests must be acknowledged first even in multi-intent messages ». | Positif: message à 2 intentions (effet + product_help) -> les 2 sont accusées dans la réponse. Anti-faux-positif: message à intention unique -> pas de sur-accusé parasite. |
| `rose-global15-r2-B05` | T4 | `BF-ROUTE-01` (mineur) | Route policy / arbitrage `feature_opportunity` | Réponse à la précision d'un déclencheur (item de clarification actif `Cibler le joint réflexe`) routée vers `feature_opportunity` plutôt que vers la progression de la clarification existante | Contenu reste cohérent et utile (faible impact), mais le mécanisme de plan existant n'est pas explicitement fait progresser | `route_decision.selected_handler=feature_opportunity`, aucun lien explicite avec l'item `bd2ef2a9-...` (`Cibler le joint réflexe`) dans la trace | Vérifier l'existence d'un item de plan actif correspondant avant de proposer une nouvelle `feature_opportunity` sur le même sujet | `fix_applied` (rerun requis) | Doctrine frontière feature_opportunity (`dispatcher.prompts.ts`) : item actif du plan couvrant le sujet ⇒ pas d'initiatives, la réponse fait progresser l'item existant ; + blocage de démarrage sur action active ⇒ coaching_recommendation prime. Test « keeps initiatives away from active plan items and launch blockers ». | Positif: précision d'un déclencheur avec item de clarification actif correspondant -> progression de l'item plutôt que nouvelle feature. Anti-faux-positif: sujet sans item de plan correspondant -> `feature_opportunity` reste valide. |

## Suivi Des Décisions Architecturales

| Date | Décision | Statut | Référence |
| --- | --- | --- | --- |
| 2026-07-02 | Confirmation indépendante du bug `R2-W01`/`BF-EFFECT-03` (compteur `current_reps` jamais incrémenté par le chemin conversationnel) sur un deuxième run et deux items différents de ceux du run d'origine. Priorité de fix inchangée : la plus haute, car elle explique aussi le symptôme de recap `BF-STATUS-01` de ce run. | Active | rose-global15-r2 |
