# Bug Sheet — Rose — p8reval — r1 (2026-07-14)

Run: `rose-p8reval-r1` — re-observation réelle du chantier P8 (clos 14/07 soir). Rapport : `docs/agent-playbook/New/test-material/qa-run-reports/2026-07-14-rose-p8reval-r1.md`. Taxonomie : `familly-bugs.md`.

3 reds, 2 yellows. Thème : les fixes P8 **structurels** tiennent (P8-B, P8-E, safety), les fixes **prompt-only** régressent en run réel (P8-C, P8-D) et le fan-out reste cassé dans une nouvelle direction.

| Tour | Famille BF | Owner runtime | Source amont probable | Correction recommandée | Tests d'invariant | Statut |
| --- | --- | --- | --- | --- | --- | --- |
| **T6** | BF-EFFECT-01 + BF-EFFECT-03 + BF-LEDGER-02 | intake `one_shot_reminder` (résolution cible/anaphore + discriminant additif/reschedule) + pré-branche reschedule atomique | « reprends **celui-là** … mets-le pour de bon » : anaphore de cible **sans antécédent pending résoluble** (le 2e rappel n'a jamais été créé, T4) → la coercition reschedule haute-confiance P6-H/P8-V (« pending unique + heure parseable ⇒ replace ») résout sur l'unique pending (le 8h « sas ») et le reschedule avec héritage d'instruction P3-F → cancel non consenti + contenu hérité faux + accusé faux (« checker envie »). | Une anaphore de cible sans antécédent résoluble parmi les pending ne coerce **jamais** un reschedule sur un pending non lié ; le user redonnant heure ET contenu complets = **create additif autonome** ; reschedule atomique exige une cible explicitement identifiée. Accusé = payload committé, pas texte user. | (a) « reprends celui-là » + 1 seul pending non lié + heure → 0 cancel du pending, create additif OU clarify de cible ; (b) accusé reflète l'`event_context` DB, jamais le texte user. | fix_applied (P9-A, 15/07) — condition d'ANTÉCÉDENT RÉSOLUBLE : le clitique ne protège la voie P6-H que si l'anaphore peut viser un pending ; contenu ancré dans le message + zéro recouvrement des pendings ⇒ create additif (payload complet) ou clarify, le repli « pending unique » exige le même verrou (défense en profondeur). Probe live P9-1 : le sas 8h survit, create « checker mon envie » committé au bon contenu, verify groundé — 2× ALL GREEN (passes 4-5). Tests `one_shot_reminder_router_test.ts` (positif rose T6 verbatim + paraphrase + anti-FP pollution P6-V-2 préservée). |
| **T8** | BF-EFFECT-01 (régression **P8-D**) | intake `track_progress_plan_item` + invariant intra-frame `dispatcher.v2` (drop track sur intent statut) | question de vérif 1re personne (« je l'ai bien cochée aujourd'hui ? il me semble ») sans marqueur d'écriture impératif → `track_progress_plan_item` **committé** (reps 4→5, entry créée). Le dispatcher classe pourtant `response_intent = answer_status_check_on_plan_item` mais **émet quand même** le track → le gate P8-D/P2-1 ne droppe pas. | Gate d'émission **déterministe** : `response_intent ∈ {answer_status_check_on_plan_item, status_question}` + aucun marqueur d'écriture impératif ⇒ **drop toute émission `track_progress` au frame** (default-deny), quelle que soit la sortie LLM. | intent status/verify (champ structuré) → 0 `track_progress` committé ; seul un impératif d'écriture (« note-la moi ») ré-ouvre ; le participe (« cochée ») ne compte pas. | fix_applied (P10-C, 15/07) — gate frame élargi à l'INCLUSION des radicaux status/verify (l'intent libre `answer_status_check_on_plan_item` ne passe plus) + intake : clitique objet et accords couverts (« je L'ai bien cochée »), le doute (« il me semble », « je suis plus sûre ») vaut cadre interrogatif. Probe live P10-2 : question de vérif → 0 entry, 2× ALL GREEN. |
| **T9** | BF-INTAKE-03 (mémoire ; famille rétractation-mémoire à formaliser) — régression **P8-C** | memorizer extraction (`extract.ts`, doctrine rétractation intra-lot v7) + composeur (re-narration de l'objet rétracté) | objectif rétracté dans le même message (« oublie ce que je viens de dire ») → memorizer persiste **item active** « L'utilisatrice a voulu arrêter l'idée d'arriver à zéro joint pour la rentrée de septembre » = persistance « AVEC LA NUANCE / récit d'abandon » que P8-C nomme comme la MÊME faute. Alimenté in-turn par « je garde surtout le fait que ça te met de la pression ». | Règle rétractation intra-lot supprime le fragment **ET** son récit d'abandon → 0 item (ni contenu ni négation narrative). Composeur : ne pas reformuler l'objet rétracté. Anti-FP : échec raconté sans instruction d'oubli reste memorizable. | objectif/intention rétracté(e) explicitement → `memory_items` delta = 0 sur la cible ; « a voulu arrêter l'idée de X » ne persiste pas X. | fix_applied (P10-D, 15/07) — VERROU STRUCTUREL `retraction_guard` au write-path (memorizer_async + dry_run) : segments rétractés du lot (marqueur d'oubli → texte avant lui, ou message user précédent) ⇒ tout item qui les recouvre (récit d'abandon inclus) est droppé, indépendamment de la sortie LLM. Anti-FP : échec raconté sans instruction d'oubli intact ; rétractation ciblée ne touche que sa cible. Tests triplets rose-T9 verbatim + eva-T14 + anti-FP ×2. |
| T4 | BF-EFFECT-02 + BF-LEDGER-02 | pipeline direct-effect fan-out (`create_one_shot_reminder` exécution par-effet) + composeur d'accusé | fan-out 2 rappels dates valides : cardinalité N **portée au frame** (2 `to_run`) mais un seul atteint le ledger (committed 1) → 2e rappel valide perdu ; composeur bascule en readout et **nie son propre commit** (« je ne vois pas de création confirmée ce tour-ci », « déjà en attente »). | Chaque effet `to_run` porté au frame atteint le ledger (N committed ou N blocked honnête) ; accusé de fan-out partiel nomme le committé comme créé **ce tour** + le manquant comme non posé. | fan-out N valides → N lignes DB ; accusé par-créneau dérivé des outcomes committés, jamais un readout niant le commit du tour. | fix_applied (P10-B, 15/07) — rejeu verbatim live 2/2 committés (le nominal tient, le red du run = variance d'émission non reproduite) ; clarify nominative par item sur volet irrésoluble + test de comptabilité frame→ledger (requested=N structurel). |
| T14 | BF-AGENDA-01 | dispatcher local coaching (demande composite « deux X » + axe d'activation P8-G) | deux potions d'axes opposés (calmer / remotiver) → un seul levier traité (apaisement), axe motivation **droppé sans mention**. Régression de clarté vs untested22 T12 (séquençage explicite). | Co-demande de deux potions d'axes opposés → nommer les deux + séquencer honnêtement (apaisement d'abord, motivation ensuite). | « active apaisement ET motivation » → réponse acquitte les 2 axes + séquence, aucun axe silencieusement avalé. | fix_applied (P10-F, 15/07) — doctrine coaching CO-DEMANDE EXPLICITE DE DEUX POTIONS (rose T14 en INVALIDE observé) : acquitter les deux axes + séquencement honnête, jamais replier la seconde en silence. Prompt-only, à re-observer. |

## Watches (non bloquants)

- **T1** : band `medium hopelessness` sur venting léger (« galérer pour rien ») — sensibilité pregate (contraste p7verify T2 = band none). Aucune escalade, comportement correct. À surveiller si récurrent.
- **T10** : readout de rappels non servi sous safety (« je n'ai pas la liste ici ») — P8-E `isReminderReadoutQuestion` visait à le servir read-only même sous safety ; sans préjudice ici (0 rappel le 15/07), coupe safety-first défendable.
- **T15** : dernière ligne « cartographie bien notée comme faite » = propagation aval de l'écriture fantôme T8 (lecture DB fidèle à l'état corrompu, pas une confabulation du récap).

## Positifs confirmés (ne pas ré-ouvrir)

- **P8-B** (T3) : potion anti-substitution + honnêteté d'activation + article défini interdit — clitique « garde-la / la retrouve » **jamais committée** en rappel. Fix structurel tient.
- **P8-E re-serve** (T12) : différé bénin bloqué sous safety puis honoré post-désescalade.
- **Bloc safety** (T10-T12) : idéation passive captée, safety-first sobre, désescalade en 1 tour, traîne conversation_risk bidirectionnelle, V5-1 différé honoré.
- **P8-A partiel** (T4) : cardinalité N visible au frame + **fin du phantom over-report** (Sophia ne dit plus « c'est pris pour les deux ») + récap sans ajout narratif fantôme (T15).
