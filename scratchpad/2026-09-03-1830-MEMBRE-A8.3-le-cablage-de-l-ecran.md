# MEMBRE · A8.3 — le câblage de l'écran

**Worktree** `/Users/ahmedamara/Dev/Sophia-2-chantiers/MEMBRE` · branche
`chantier-0903/MEMBRE`. Suit A8.0/A8.1/A8.2 (`31148a5a`, `7c403607`, `5650ed9d`,
`2e330f85`, `e25d8578`, `26ad59b9`).

---

## 0. Synchronisation

Deux fusions de `ff-001-quotidien-du-coach` dans le worktree, sans conflit :

| # | tête reprise | ce qu'elle apporte |
|---|---|---|
| 1 | `e43eab06` | A6, A1, A2, les réparations du gate |
| 2 | `089f7fdf` | `4cacecc0` — les **sept épinglages v25** de CUISINE sont corrigés |

⚠️ `git fetch` est **bloqué par le hook** de ce poste (`_confirm_risky_cmd`). Il
n'était pas nécessaire : un worktree partage le `.git` du dépôt principal, donc
`ff-001-quotidien-du-coach` y est la **même référence locale**. Écrit ici pour
que la prochaine session ne perde pas le même quart d'heure.

Vérifié avant d'écrire : `20260903172000` **n'est pas appliquée** (tête du
registre `20260903150000` ; `20260903170000` d'A1 non appliquée non plus, et les
deux sont dans l'ordre). `information_schema` compte **0** ligne pour
`meal_share_outcomes`.

---

## 1. 🔴 Le rouge repris de l'orchestrateur — A8.1 tenait l'arbre principal rouge

**Commit `f8ddedf8`.** Vingt-trois erreurs de type dans trois fichiers de test,
toutes de la lignée A8.1 : `HouseholdDishView.dishIndex` y est devenu **requis**,
et les fixtures qui construisent des plats à la main ne le portaient pas. Même
défaut que celui que CUISINE vient de payer sur A1 — *un champ requis ajouté à un
type partagé casse des fichiers que le mandat ne nomme pas.*

⛔ **Le champ reste requis, et c'est le geste juste.** Ces listes sont filtrées
(`dishIsFor`), regroupées par jour puis triées par moment : le rang d'affichage
n'est **pas** la position dans le `dishes[]` stocké. Le rendre optionnel pour
faire taire un test rouvrirait exactement le trou qu'A8.1 a fermé — la coche d'un
membre posée sur le plat suivant, dans une ligne datée, sous le titre de
quelqu'un d'autre. Chaque fixture réparée porte la phrase qui le dit.

`householdFlatLists` portait en plus un montage de `MyShareCard` **sans les trois
props d'A8.1** (`userId`, `householdMealId`, `planStartsOn`) : écrites `null`, pas
omises — ce fichier mesure la liste des plats, pas les coches.

| fichier | avant | après | toléré |
|---|---|---|---|
| `planByPersonModel.int.test.ts` | 13 | **2** | 6 |
| `dishListByDay.int.test.ts` | 8 | **0** | absent |
| `householdFlatLists.int.test.ts` | 2 | **0** | absent |

Les **2 restantes** sont `eatingSlots` manquant sur `MemberPortionView` :
`git log -S` date le champ des commits `488277b8` (2026-08-13) et `8ef6cf09`
(2026-08-14) — **antérieures au chantier**, donc deux des six déjà tolérées. Pas
de worktree ouvert pour le prouver (disque plein) : `git show -s` a suffi.

`scripts/.tsc-test-red-baseline` **n'est pas touché**. Vérifié dans
`agent-gate.sh` : un compte qui DESCEND ne fait qu'un `info`, seul un compte qui
MONTE ou un fichier hors liste fait échouer. `vitest` sur les trois fichiers :
**49 verts**.

---

## 2. Ce que j'ai trouvé dans l'état repris

- `meal_share_outcome.ts` (le modèle pur, 16 épreuves) est **complet et juste** :
  je ne l'ai pas touché. `boxOptionsFor`, `whoDidNotEatOptions`,
  `resolveShareOutcomes`, `boxStillWaiting` sont tous appelés par le câblage.
- `leftover` est bien dans `REALIGNMENT_ACTIONS`, avec `shareOf` requis.
- 🔴 **`shift_dish` n'a AUCUN exécuteur dans ce dépôt** (vérifié le 2026-09-03 :
  la seule occurrence hors tests et hors commentaires est le `handledAs` de
  `accident_tap.ts:418`). Le mandat écrit « tout le foyer ⇒ l'action `shift_dish`
  existante » ; l'action existe **dans l'espace**, et rien ne l'exécute. Voir §4.
- `buildRealignmentSpace` est appelé deux fois en production et **son résultat ne
  devient jamais des boutons** : il ne sert qu'à choisir un `handledAs`.

---

## 3. Les surfaces neuves (en cours d'écriture — ce journal se complète)

*(à compléter au fil des commits)*
