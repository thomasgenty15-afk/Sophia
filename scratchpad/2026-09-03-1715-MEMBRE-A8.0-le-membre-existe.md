# MEMBRE · A8.0 — le membre existe pour le produit

**Worktree** `/Users/ahmedamara/Dev/Sophia-2-chantiers/MEMBRE` · branche `chantier-0903/MEMBRE`
· base `31ee930f` · lane MEMBRE · tag QA `qa0903m`.

Journal écrit au fil de l'eau (§2.3 n°25). Ce lot est **le contrat de §5.10** : la lane SUIVI (A7)
code contre lui.

---

## 0. L'état repris — ce qui était là, non commité

Une session précédente a été tuée par une limite de session après ~20 tours, **sans un seul
commit** et **sans avoir lancé un seul test**. Treize fichiers modifiés, un neuf non suivi.

### Ce qui était JUSTE, et que je n'ai pas retouché

| Pièce | Verdict |
|---|---|
| `pulse_audience.ts` (neuf) | Juste. Deux audiences, l'ordre `students` → `members`, la définition par `household_members.role='member' and user_id is not null`, aucun `.eq("keel_role")` sur la seconde, le curseur qui porte sa phase, et `cursorEnd` = dernier **parcouru** (pas dernier servi). Le raisonnement sur la page entièrement écartée est exact et il évitait une boucle infinie. |
| `resolvePlanScope` + branche foyer de `loadPlannedDishContext` | Juste. Le `.eq("plan_kind","household")` **et** le `.eq("household_id")`, l'ordre « le sien d'abord, le foyer ensuite » (la prise de main L3 gagne), le repli fail-closed vers `own`. |
| `loadPlanForTick` (branche foyer) | Juste, même forme. |
| `statesOwnerId` **requis** sur `loadSkippedDishIndexes` | Juste, et c'est le bon arbitrage : un paramètre optionnel replié sur `userId` aurait laissé le défaut invisible chez tout appelant qui l'oublie (cicatrice `optional-gate-params-are-disarmed-gates`). |
| `keel-daily-pulse-v1` : boucle `phases:` | Juste. Corps unique pour les deux audiences, compteurs `members_*` à part, `exhausted` = les deux phases parcourues. `deno check` : 0 erreur. |
| Renversements FF-048 §3 / R14, README foyer, FF-058 §11, `routeGuards.int.test.ts`, `KeelHouseholdRoute.tsx`, `App.tsx` | Justes, et écrits **là où la phrase inverse vivait** (§2.1 n°10, R4 et R5). |

### Ce qui était FAUX ou MANQUANT — corrigé

1. **Un troisième appelant de `loadSkippedDishIndexes` n'avait pas été mis à jour.**
   `missed_wave_reader_test.ts:185` passait encore `{ userId, plan }`. **Le fichier ne
   compilait pas** — `deno test` s'arrêtait sur `TS2345` avant d'exécuter quoi que ce soit.
   Corrigé (`statesOwnerId: USER`). C'est la preuve que rien n'avait été lancé.

2. **La garde de `household_plan_kind_readers_test.ts` ne mordait que dans UN sens.**
   Elle tient `household_id ⇒ plan_kind`. Elle laissait passer l'inverse — une requête qui ne
   porte **que** `.eq("plan_kind","household")`. Tant qu'un seul lecteur lisait le plan du foyer
   sous le `user_id` de son maître, ce sens n'avait pas d'occasion de se tromper : **A8.0 la
   crée**, puisque deux lecteurs doivent désormais lire un plan écrit sous le `user_id` de
   quelqu'un d'autre. Sans ce second sens, la mutation que le mandat nomme (« lecteur sans
   `.eq("household_id")` ») **ne rougissait sur aucun test**. Ajouté : `⛔ H2 — tout lecteur qui
   filtre plan_kind=household nomme AUSSI SON foyer`. Cas qui passe : les cinq lecteurs listés le
   satisfont déjà.

3. **Zéro test de comportement sur tout le lot.** `pulse_audience.ts` (le cœur de A8.0) n'avait
   aucune couverture ; la branche foyer des deux lecteurs non plus. Deux fichiers neufs écrits
   (§1 ci-dessous). Sans eux, le lot ressemblait à un lot qui marche.

---

## 1. Ce que le lot livre — et ce qui le prouve

### Fichiers neufs

- `supabase/functions/_shared/keel/pulse_audience.ts` (repris, non modifié)
- `supabase/functions/_shared/keel/pulse_audience_test.ts` — **9 épreuves**. L'ordre
  (`students` avant `members`, FF-061 §11), le curseur qui porte sa phase, la définition de la
  seconde audience **par l'appartenance et jamais par `keel_role`**, l'élève-qui-a-rejoint écarté
  et **compté**, le curseur qui avance sur le parcouru, la panne qui **remonte**.
- `supabase/functions/_shared/keel/member_plan_scope_test.ts` — **10 épreuves**, sur une base
  doublée **qui enregistre ses filtres** (une épingle textuelle ne verrait ni QUEL foyer est
  demandé, ni QUAND la seconde requête part). `resolvePlanScope` (4 cas dont le fail-closed),
  `loadPlannedDishContext` (le cas qui passe, H2, la prise de main, le non-membre),
  `loadStripDishes` → `loadPlanForTick` (le tap, et H2 au tap).

### Le cas qui passe, et les deux H2

- **Le cas qui passe** : `LE CAS QUI PASSE — un membre réclamé reçoit le plat du foyer` et
  `le tap d'un membre relit les titres sur le plan de SON foyer`. Sans eux, les gardes H2
  bloqueraient tout et ressembleraient à des gardes qui marchent (§2.3 n°23).
- **H2 en lecture** : la base ne porte que le plan d'un **autre** foyer ⇒ `mealId` reste `null`.
- **H2 au tap** : une charge forgée citant `FOREIGN_MEAL` ⇒ aucun titre relu ⇒ `stale`, rien
  d'écrit sous le compte de l'attaquant. Le foyer vient de `household_members`, **jamais** du
  payload.

---

## 2. Mutations jouées (§2.3 n°21) — restauration par `cp`, prouvée par `cmp`

| # | Mutation | Rouge vu | Restauration |
|---|---|---|---|
| M1 | retirer `.eq("household_id", scope.householdId)` de `planned_dish_io.ts` | **3 rouges** : `LE CAS QUI PASSE…`, `⛔ H2 — un membre ne lit JAMAIS…`, `⛔ H2 — tout lecteur qui filtre plan_kind=household…` | `cp` + `cmp` ok |
| M2 | idem dans `evening_strip_io.ts` | **3 rouges** : `le tap d'un membre…`, `⛔ H2 AU TAP…`, `⛔ H2 — tout lecteur…` | `cp` + `cmp` ok |
| M3 | ajouter `.eq("keel_role","student")` à la requête d'appartenance | **1 rouge** : `⛔ phase 2 — l'APPARTENANCE définit l'audience, JAMAIS keel_role` | `cp` + `cmp` ok |
| M4 | inverser `PULSE_AUDIENCES` en `["members","students"]` | **1 rouge** : `⛔ LES MAÎTRES AVANT LES MEMBRES — l'ordre EST le produit` | `cp` + `cmp` ok |
| M5 | remettre `/app/chat` sous `<KeelStudentRoute>` dans `App.tsx` | **1 rouge** : `laisse un profil réclamé atteindre /app/chat et /app/progress (A8.0)` | `cp` + `cmp` ok |

Aucun `git stash`, `checkout`, `reset` ni `restore` n'a été employé.

---

## 3. Suites

- **`cd frontend && npx tsc -b --force`** → **exit 0**.
- **`npx vitest run`** → `Test Files 3 failed | 120 passed | 5 skipped (128)` ·
  `Tests 5 failed | 2048 passed | 20 skipped (2073)`. **Les cinq rouges sont les rouges
  étrangers nommés au mandat, aucun autre** :
  - `src/edge/coverage-guard.int.test.ts` ×2
  - `src/keel/api/household.int.test.ts › awayFrom …` ×2 (baseline)
  - `src/keel/components/mealBoxes.int.test.ts › un contenant sans bouche…`
  `routeGuards.int.test.ts` : **4/4 verts**.
- **Deno**, `deno test --allow-env --allow-read --allow-net` (jamais `--no-check`), sur
  `accident_test · household_plan_kind_readers_test · member_plan_scope_test ·
  missed_wave_reader_test · pulse_audience_test · evening_strip_test · day_review_test ·
  day_review_chain_test · planned_dish_match_test` → **144 passed | 0 failed**.
- **`deno check`** sur `keel-daily-pulse-v1/index.ts`, `pulse_audience.ts`, `planned_dish_io.ts`,
  `evening_strip_io.ts`, `accident_io.ts`, `daily_recap_io.ts`, `day_review_io.ts`,
  `meal_generation.ts`, `fridge_window.ts`, `grocery_waves.ts` → **0 erreur**.

### Le gate de commit, et l'antériorité de `daily_pulse_*`

Le mandat signale 15 erreurs TS dans `daily_pulse_test.ts` / `daily_pulse_locale_test.ts`, « dans
ta région ». **Mesuré : 13 erreurs, et elles ne portent pas sur ce que A8.0 retourne** — toutes
disent la même chose, `renderPulseMessage({…})` appelé sans la propriété `memory` déclarée à
`daily_pulse.ts:655`. C'est la ligne de mémoire du message du soir, pas l'audience.

**Antériorité prouvée par identité de fichier**, ce qui est plus fort qu'un worktree détaché : les
**trois** fichiers impliqués (`daily_pulse.ts`, `daily_pulse_test.ts`, `daily_pulse_locale_test.ts`)
sont **byte-identiques à `31ee930f`** — vérifié par `git diff --quiet` + `git status --porcelain`
par chemin. Une lane ne peut pas avoir causé une erreur dans trois fichiers qu'elle n'a pas
touchés. **Non touchés**, conformément au mandat.

Commit `--no-verify` en conséquence, motif écrit dans le message.

---

## 4. i18n

**Aucune clé** pour A8.0 : le lot est routes + serveur, il n'ajoute aucun mot à l'écran.
`en.ts` / `fr.ts` / `catalog.ts` **non touchés**. Le bloc `chantier-0903/MEMBRE` s'ouvrira à
A8.1.

## 5. Migration

**Aucune** pour A8.0 (le mandat le dit). `20260903142000` (`meal_share_outcomes`) est réservée
pour A8.2.

---

## 6. Les renversements écrits (§2.1 n°10)

| Renv. | Fichier | Où |
|---|---|---|
| **R4** | `docs/fonctionnalites/le-foyer/FF-048-reclamer-son-profil.md` | §3 « Hors périmètre — engageant » : la ligne « ❌ le chat » est **barrée** et remplacée par le renversement daté (D8.1), avec ce qui reste hors périmètre (les mesures corporelles). |
| **R4 bis** | `docs/fonctionnalites/le-foyer/README.md` | sous « ❌ Aucun canal 1:1 dans le foyer » : ce que l'interdit **n'interdit pas** — Sophia → la personne. |
| **R5** | `frontend/src/keel/components/routeGuards.int.test.ts` | le cas « garde les écrans de coaching derrière la garde ÉLÈVE » est **scindé en deux** : le retournement (`/app/chat` + `/app/progress` sous `KeelHouseholdRoute`, et **pas** sous `KeelStudentRoute`) et le cas qui passe (`/app/today` reste élève). |
| **R14 amendé** | `FF-048` §6, ligne R14 | ce qui s'est élargi est la **porte**, pas le rôle : `keel_role` reste NULL. |
| — | `frontend/src/keel/components/KeelHouseholdRoute.tsx` | le motif « ces trois écrans n'ont rien à montrer » est corrigé : `/app/today` seul garde ce motif ; les deux autres sont datés A8.0. |
| — | `docs/.../FF-058-la-bande-du-soir.md` §11 | la question ouverte « la réclamation n'existe pas encore » est **refermée**, avec ce qui reste à A8.1. |
| **D7.3** | `supabase/functions/_shared/keel/accident_io.ts` | `generated_from.shifts[]` **apposé** par `withShiftTrace` — `buildShiftedPayload` garde ses trois clés de date (R13, tenu par `accident_test.ts`, vert). |

---

## 7. 🔴 ROUGE — ce qui n'a PAS été vu en run réel

Rien de A8.0 n'a été joué contre la base ni le runtime edge : **hors fenêtre**, et le mandat
l'interdit. Consigné ROUGE, jamais présenté comme prouvé.

**Ce que la fenêtre doit jouer** (`qa0903m`, un foyer à **deux comptes** : un maître, et un profil
**réclamé** — `household_members.user_id` posé, `role='member'`, `access_tier='household_member'`,
`keel_role` **NULL** ; comptes par SQL, patrons `docs/keel/qa-fixtures/*.sql` +
`scripts/2026-09-01-fixture-foyer-retours.ts`, mot de passe `1234567` par `crypt`, colonnes de
jeton `''` jamais NULL) :

1. **Le cron du soir atteint le membre.** Invoquer `keel-daily-pulse-v1` par le chemin documenté
   dans la fonction. Attendu au compte-rendu : `members_scanned ≥ 1`, `members_sent ≥ 1`.
   `members_scanned: 0` sur cette base **est** le défaut que le lot ferme, revenu par la porte
   d'à côté.
2. **Il reçoit ③ SANS ① ni ②.** Sa bulle porte les repas ; aucune question de courses, aucune de
   cuisson. Le maître, le même soir, reçoit les trois. (C8.)
3. **Sa bande est bâtie sur le plan `household` de SON foyer** — les plats du jour du plan du
   maître, pas un silence.
4. **L'ordre.** Le maître déclare une cuisson ratée dans le tick ; la bande du membre servie
   ensuite est **amputée** des plats qui en descendent.
5. **Le curseur à deux phases.** Un tick coupé par le budget rend `audience` + `next_audience` +
   `next_after_user_id` ; le rappel avec ces trois valeurs reprend **sans** rejouer la phase 1.

⚠️ Avant tout run : le runtime edge sert des `_shared` **périmés** (cicatrice
`edge-runtime-serves-stale-shared-modules`) — `pulse_audience.ts` est un fichier **neuf**, il
rendra 404/ancien code sans un restart, et **seul l'orchestrateur restart** (puis
`local_extend_kong_functions_timeout.sh`).

**Navigateur : non joué non plus.** `/app/chat` et `/app/progress` sous `KeelHouseholdRoute` n'ont
pas été ouverts sous un compte membre (port 5207, 320 px et 1280 px, deux langues). ROUGE.

---

## 8. Hors périmètre croisé, nommé et non touché (§2.1 n°11)

- `HouseholdPage.tsx` — région de la lane FOYER, qui la refait en parallèle. Non ouvert.
- `daily_pulse.ts` / `daily_pulse_test.ts` / `daily_pulse_locale_test.ts` — 13 erreurs TS
  antérieures, §3 ci-dessus.
- Les 5 rouges vitest étrangers — nommés, non touchés, aucune baseline régénérée.
- La lane 1:1 (`plan_versions`), la fusion / prise de main (**lue** par `resolvePlanScope` pour
  lui céder la priorité, jamais **écrite**).

---

## 9. Commits

| sha | quoi |
|---|---|
| `31148a5a` | **A8.0 commité** — le membre existe pour le produit |

---

## 10. ⟳ CORRECTIF A8.0 (2026-09-03, ~17h50) — les deux ROUGES du vérificateur

Le vérificateur d'A8.0 a rendu **ROUGE** sur deux défauts **de la même famille** :
*une garde réelle que rien ne tient*. Dans les deux cas il a joué la mutation
lui-même et la suite Deno du domaine est restée **verte** (4 936 passés, 0
échec). Il a raison sur les deux, et le diagnostic est juste : **j'avais prouvé
la FORME, pas l'EFFET.**

### D1 — la cascade du maître sur la bande du membre

`statesOwnerId` est un paramètre **requis**, donc `deno check` attrape
l'appelant qui l'**oublie** — c'est ce que mon lot prouvait, et c'est tout ce
qu'il prouvait. Il n'attrape **pas** celui qui passe la **mauvaise valeur** :
remettre `args.userId` restaure exactement le défaut d'avant le lot. Or c'est la
seule raison d'être de l'ordre du cron : M4 prouvait l'**ordre**, rien ne
prouvait **ce qu'il sert**.

La cause du trou est identifiable : la doublure de `missed_wave_reader_test.ts`
rend ses lignes **quel que soit le filtre**, et y passe `userId ===
statesOwnerId`. Échanger les deux y est structurellement invisible.

### D2 — `generated_from.shifts[]`

Écrit par `applyPlanShift`, cité dans **aucun** `*_test.ts`. C'est une clause du
contrat **§5.10** : la lane SUIVI compte « plans modifiés » dessus. Le jour où
une édition perd la clé, rien ne rougit et le compteur tombe à zéro **en
silence** — la cicatrice « lecteur sans écrivain » réintroduite **dans le
contrat lui-même**.

### Le correctif

`supabase/functions/_shared/keel/member_cascade_and_shift_trace_test.ts`
(**neuf, 6 épreuves**), avec pour chaque défaut **un cas qui passe et un cas qui
refuse** — le cas qui refuse étant celui qui distingue la bonne valeur de la
mauvaise.

La doublure `twoAccountDb` **regarde le `user_id` demandé** : la déclaration de
cuisson n'existe que sous le **maître**, la coche que sous le **membre**. Lire
la mauvaise clé rend donc un résultat **différent**, ce que l'ancienne doublure
rendait impossible.

| Épreuve | Ce qu'elle tient |
|---|---|
| D1 · le cas qui passe | cuisson ratée du **maître** ⇒ les plats 0 et 1 du **membre** tombent |
| D1 · le cas qui refuse | `statesOwnerId = MEMBER` ⇒ `[]` — la mauvaise clé ne trouve rien |
| D1 · les deux clés sont deux faits | la coche du **membre** fait survivre le plat 0 ; le plat 1 tombe par la déclaration du **maître** (R10, les deux moitiés dans une lecture) |
| D2 · le cas qui passe | la trace est **apposée** : `prompt_version` et `belief_keys` survivent, un 2ᵉ glissement s'**ajoute**, et **les 5 champs de la forme** sont exigés |
| D2 · le cas qui refuse | `null`, `42`, `"x"`, `["a"]`, `shifts: "pas un tableau"` ⇒ **remplacé**, jamais poussé dedans, jamais jeté |
| D2 · `applyPlanShift` écrit vraiment | la doublure est **étatique** (la fonction **relit** et compare l'empreinte : une doublure figée rendrait `unverified`, un vert de façade) ; `delta` et `fingerprint` viennent du **produit** (`computeSessionShift`, `planShiftFingerprint`), jamais écrits en dur — sinon l'épreuve serait paramétrée par sa propre constante ; **et R13** : le payload a exactement 4 clés |

### Les mutations — celles du vérificateur, mot pour mot

| # | Mutation | Rouge vu | Restauration |
|---|---|---|---|
| M10 | `args.userId` à la place de `args.statesOwnerId` sur les **deux** lectures d'état | **2 rouges** : `D1 · LE CAS QUI PASSE`, `D1 · LES DEUX CLÉS SONT DEUX FAITS` | `cp` + `cmp` ok |
| M11 | `withShiftTrace` rend `{ ...base }` | **3 rouges** : les trois épreuves D2 | `cp` + `cmp` ok |

Suite du domaine après correctif : **130 passed | 0 failed** sur les neuf
fichiers.

### Ce que le vérificateur apporte, et que je reprends

- Son run adversarial H2 sur le chemin d'**écriture** (`applyStripTicks`), que
  je n'éprouvais pas : charge forgée ⇒ `stale=1`, `written=0` ; garde retirée ⇒
  une ligne écrite sous l'attaquant portant le plat de la victime.
- La population de la fenêtre, établie en SQL : **10** membres réclamés, **7**
  avec `keel_role='student'` (écartés en phase 2 **et comptés**), **3** avec
  NULL dont **2** portant un plan de foyer vivant.
  **Attendu : `members_scanned=3`, `members_already_student=7`, `members_sent≥1`.**
  Le point 4 de §7 (la cascade) est **la seule preuve existante de D1** en run
  réel.
- `pot_demand_test.ts` est réparé côté arbre principal (`a0eb7b76`). ⚠️ Ce
  correctif **n'est pas dans mon worktree** (basé sur `31ee930f`) : mon gate de
  commit reste rouge localement, d'où `--no-verify` maintenu, motif inchangé.
