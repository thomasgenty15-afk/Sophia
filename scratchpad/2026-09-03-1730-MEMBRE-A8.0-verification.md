# MEMBRE · A8.0 — vérification après fusion (`cf2b8558`)

**Vérificateur** · worktree `/Users/ahmedamara/Dev/Sophia-2-chantiers/VERIF2`, détaché sur
`cf2b8558`. Base du diff `43a53f49`. Base d'antériorité `31ee930f`.
Journal écrit au fil de l'eau (§2.3 n°25). **Rien n'est cru : tout est rejoué.**

Aucun commit, aucun `push`, aucune migration, aucun run réel, aucun restart du runtime edge,
aucun mot de passe entré. Toute mutation défaite par `cp` et prouvée par `cmp`, puis par
`git status --porcelain` vide et `cmp` contre `git show cf2b8558:<fichier>`.

---

## 0. LA LIGNE FINALE — **ROUGE**

Tout ce qui est **gardé** est gardé, et bien gardé : les cinq mutations du rapport rougissent
exactement comme annoncé, le renversement R5 est **retourné avec un cas qui passe**, et mon
propre run adversarial H2 — écrit indépendamment, sur le chemin d'**écriture** que le bâtisseur
n'éprouve pas — montre que la garde est réelle et porteuse.

Mais **deux comportements neufs et load-bearing n'ont aucun test**, et l'un des deux **est une
clause du contrat §5.10 sur lequel la lane SUIVI code déjà**. Mesuré : la suite Deno entière du
domaine (**4 936 épreuves**) reste **verte** quand je les supprime. Voir §5, défauts **D1** et
**D2**.

---

## 1. Statique

### 1.1 Le périmètre du diff

`git diff 43a53f49..cf2b8558 --stat` → **16 fichiers**, 1418 insertions, 71 suppressions.
`git log --oneline 43a53f49..cf2b8558` → 2 commits (`31148a5a` + la fusion `cf2b8558`).

Aucun fichier hors mandat §5.7 :

| Fichier | Nommé par le mandat |
|---|---|
| `docs/…/le-foyer/FF-048-reclamer-son-profil.md` | oui (R4, §2.1 n°10) |
| `docs/…/le-foyer/README.md` | oui (R4 bis) |
| `docs/…/suivi-quotidien/FF-058-la-bande-du-soir.md` | oui (§11) |
| `frontend/src/App.tsx` | oui (routes) |
| `frontend/src/keel/components/KeelHouseholdRoute.tsx` | oui |
| `frontend/src/keel/components/routeGuards.int.test.ts` | oui (R5) |
| `supabase/functions/_shared/keel/accident_io.ts` | oui (`shifts[]`, D7.3) |
| `supabase/functions/_shared/keel/evening_strip_io.ts` | oui (`loadPlanForTick`) |
| `supabase/functions/_shared/keel/household_plan_kind_readers_test.ts` | oui (`READERS`) |
| `supabase/functions/_shared/keel/member_plan_scope_test.ts` | test neuf, preuve du lot |
| `supabase/functions/_shared/keel/missed_wave_reader_test.ts` | conséquence du paramètre requis |
| `supabase/functions/_shared/keel/planned_dish_io.ts` | oui (`loadPlannedDishContext`) |
| `supabase/functions/_shared/keel/pulse_audience.ts` | oui (seconde audience) |
| `supabase/functions/_shared/keel/pulse_audience_test.ts` | test neuf, preuve du lot |
| `supabase/functions/keel-daily-pulse-v1/index.ts` | oui |
| `scratchpad/2026-09-03-1715-MEMBRE-A8.0-le-membre-existe.md` | journal |

**PROUVÉ.** *(Note : le journal annonce le sha `9b8fb2e0` ; l'arbre principal porte `31148a5a`.
Écart de rebase à la fusion, sans conséquence — le contenu est celui du mandat.)*

### 1.2 Aucune migration, aucun pack i18n, aucune version de prompt

```
git diff 43a53f49..cf2b8558 -- supabase/migrations       → 0 ligne
git diff 43a53f49..cf2b8558 --stat -- frontend/src/i18n  → 0 ligne
git diff 43a53f49..cf2b8558 | grep PROMPT_VERSION        → aucune occurrence
```

**PROUVÉ.** (Le lot n'ajoute aucun mot à l'écran : cohérent avec §4 du journal.)

### 1.3 L'antériorité des 13 erreurs TS `daily_pulse_*` — la preuve du bâtisseur est VRAIE

```
git diff --quiet 31ee930f..cf2b8558 -- \
  _shared/keel/daily_pulse.ts  daily_pulse_test.ts  daily_pulse_locale_test.ts
→ IDENTIQUE = OUI (zéro octet de différence)
```

**PROUVÉ.** Les trois fichiers sont byte-identiques à `31ee930f`. La lane ne peut pas avoir
causé ces erreurs. C'est la preuve la plus forte disponible, et elle tient.

**Trouvé en plus, non nommé au mandat** : `_shared/keel/pot_demand_test.ts` ne type-check pas non
plus (`TS2322`, `AnchorFactor` / `extrasFloored`). **Antériorité vérifiée de la même façon** :
`git diff --quiet 31ee930f..cf2b8558 -- pot_demand_test.ts` → inchangé ; dernier commit sur ce
fichier `f7a7ff90`, étranger à la lane. **Rouge étranger, à signaler à l'orchestrateur** — il
empêche `deno test _shared/keel/` en bloc.

### 1.4 Les suites

| Suite | Commande | Résultat |
|---|---|---|
| Front, typecheck | `cd frontend && npx tsc -b --force` | **exit 0** |
| Front, vitest **entière** | `npx vitest run` | `3 fichiers échoués / 120 · 5 tests échoués / 2 061 passés` |
| Serveur, les 7 du mandat | `deno test --allow-env --allow-read --allow-net …` | **114 passés / 0 échoué** |
| Serveur, tout `_shared/keel` (moins les 3 fichiers TS-cassés antérieurs) | idem | **4 936 passés / 0 échoué / 1 ignoré** |
| `deno check` sur les 5 fichiers serveur touchés | `deno check …` | **0 erreur** |

Les **5 rouges vitest sont exactement les 5 rouges étrangers nommés au mandat**, aucun autre :

- `src/edge/coverage-guard.int.test.ts` ×2 — le premier échoue sur le trigger
  `household_member_bodies_touch`, **qui n'existe pas dans ce lot** (A8.0 n'a aucune migration) :
  il vient d'une lane voisine. Étranger, confirmé par son contenu.
- `src/keel/api/household.int.test.ts › awayFrom …` ×2 (baseline `kind: "away"`).
- `src/keel/components/mealBoxes.int.test.ts › un contenant sans bouche…`.

`routeGuards.int.test.ts` : **4/4 verts**.

*Écart de comptage avec le journal (2 061 vs 2 048 ; 114 vs 144) : expliqué, et non un défaut —
le bâtisseur mesurait sur son worktree issu de `31ee930f` (avant les fusions voisines) et lançait
9 fichiers Deno là où le mandat de vérification en nomme 7.*

---

## 2. Les interdits du mandat, lus dans le code

### 2.1 Aucun `keel_role` écrit pour un membre — **PROUVÉ**

`grep -rn "keel_role" pulse_audience.ts keel-daily-pulse-v1/index.ts` : **16 occurrences, aucune
écriture**. Toutes sont soit des commentaires, soit deux **lectures** :

- `pulse_audience.ts:110` — `.eq("keel_role","student")` sur l'audience **`students`** : c'est le
  balayage d'origine, déplacé, inchangé. Légitime.
- `pulse_audience.ts:148-165` — le rôle est lu sur la seconde audience **pour ÉCARTER** ceux qui
  sont aussi élèves (déjà servis en phase 1), **jamais pour la définir**. La requête qui
  DÉFINIT l'audience est `household_members.role='member' and user_id is not null`, et elle ne
  porte aucun `keel_role`.

Aucun `update`/`upsert`/`insert` sur `keel_role` dans le diff.

### 2.2 Aucun `onBehalfOf` — **PROUVÉ**

`git diff 43a53f49..cf2b8558 | grep onBehalfOf` → aucune occurrence.

### 2.3 `applyPlanShift` touché **uniquement** pour la trace — **PROUVÉ** (lu ligne à ligne)

Le diff de `accident_io.ts` (95 lignes) porte **exactement trois choses**, et rien d'autre :

1. `PLAN_COLUMNS` gagne `generated_from` (+ le champ `generatedFrom` dans `LoadedAccidentPlan`) —
   **strictement nécessaire** pour apposer sans écraser.
2. `PlanShiftTrace` + `withShiftTrace` (neufs), et le `payload` de `applyPlanShift` gagne la
   **seule** clé `generated_from`. `buildShiftedPayload` **n'est pas touché** : il garde ses trois
   clés de date (R13), et `accident_test.ts` continue de le tenir (vert).
3. `loadSkippedDishIndexes` gagne un paramètre **requis** `statesOwnerId`.

Le point (3) n'est pas `applyPlanShift` — l'interdit ne le couvre pas — et il est **directement
exigé** par le mandat : « maîtres traités **avant** membres dans le même tick » n'a d'objet que
parce que la cascade doit amputer la bande du conjoint (ANALYSE §8.2, FF-061 §11). Le choix du
paramètre **requis** plutôt qu'optionnel est le bon (cicatrice
`optional-gate-params-are-disarmed-gates`). **Mais il n'est tenu par aucun test** → défaut **D1**,
§5.

### 2.4 Aucun lecteur ne retire le `.eq("user_id")` sans la garde — **PROUVÉ, sur les deux, et il
n'y a pas de troisième**

**`planned_dish_io.ts`** (`loadPlannedDishContext`) : la branche `own` garde son `.eq("user_id")`
intact ; la branche foyer est **ajoutée à côté**, jamais substituée, et elle porte
`.eq("plan_kind","household")` **ET** `.eq("household_id", scope.householdId)`, plus
`.is("retired_at", null)`. Le foyer vient de `resolvePlanScope(db, userId)` → `household_members`,
**jamais du payload**.

**`evening_strip_io.ts`** (`loadPlanForTick`) : même forme — `own` d'abord avec son
`.eq("user_id")`, puis, **seulement si rien**, la branche `household_member` avec les **deux**
`.eq`.

**Recherche d'un troisième lecteur** (`grep -rn "student_generated_meals"
supabase/functions/_shared/keel/*.ts | grep -v test`) : 15 fichiers citent la table, **11 requêtes
réelles**. Confrontées à `READERS` :

| Requête | Sort |
|---|---|
| `planned_dish_io.ts:204, 230` | dans `READERS` ✔ |
| `evening_strip_io.ts:517, 535` (`loadPlanForTick`) | dans `READERS` ✔ |
| `household_turn_context.ts:396` | dans `READERS` ✔ (antérieur) |
| `household_merge_notice_io.ts:99` | dans `READERS` ✔ (antérieur) |
| `evening_strip_io.ts:227` | **pas un lecteur de portée** : `.eq("id", planned.mealId)` seul, et le `mealId` a **déjà** été validé en amont par `loadPlannedDishContext`. Le commentaire le dit explicitement. Ni `plan_kind` ni `household_id` : les deux gardes du test l'ignorent à raison. |
| `evening_strip_io.ts:839` (état de vague) | écriture, `.eq("user_id")` **conservé** ✔ (chemin `masterOnly`) |
| `accident_io.ts:131` (`loadAccidentPlan`), `:284` (état de session), `:905` (`applyPlanShift`) | `.eq("user_id")` **conservé** ✔ — un membre ne décale rien (D8.4) |
| `plan_feedback_chat_io.ts`, `retained_next_plan.ts`, `following_io.ts`, `coach_synthesis_io.ts`, `weight_divergence_engine.ts` | lanes 1:1 / coach / retour de fin de plan — hors du chemin du membre |

**Le point d'entrée est unique et bien choisi** : `loadPlannedDishContext` est le seul résolveur du
« plan qui possède le jour », et ses **trois** appelants (`analyze-meal-photo-v1`,
`daily_recap_io.ts`, `evening_strip_io.ts`) héritent de la portée foyer **sans avoir à la
connaître**. Aucun troisième lecteur oublié. **PROUVÉ.**

`resolvePlanScope` utilise `.maybeSingle()` sur `household_members` filtré par `user_id` — **sûr**,
l'index unique `household_members_one_per_user` existe (vérifié en SQL). Pas de faux
fail-closed.

---

## 3. Les mutations rejouées

Sept mutations. Les cinq du rapport, **plus deux que je choisis**, dont le run adversarial H2
obligatoire, écrit **par moi** et sur un chemin que le bâtisseur n'éprouve pas.

| # | Mutation (commande) | Rouge attendu | Rouge **vu** | Restauration |
|---|---|---|---|---|
| **M1** | retirer `.eq("household_id", scope.householdId)` de `planned_dish_io.ts` | 3 | **3** : `LE CAS QUI PASSE — un membre réclamé reçoit le plat du foyer`, `⛔ H2 — un membre ne lit JAMAIS le plan d'un autre foyer`, `⛔ H2 — tout lecteur qui filtre plan_kind=household nomme AUSSI SON foyer` | `cp` + `cmp` ok |
| **M2** | idem dans `evening_strip_io.ts` | 3 | **3** : `le tap d'un membre relit les titres sur le plan de SON foyer`, `⛔ H2 AU TAP — une charge forgée citant un AUTRE foyer ne rend rien`, `⛔ H2 — tout lecteur…` | `cp` + `cmp` ok |
| **M3** | ajouter `.eq("keel_role","student")` à la requête d'appartenance (`pulse_audience.ts`) | 1 | **1** : `⛔ phase 2 — l'APPARTENANCE définit l'audience, JAMAIS keel_role` | `cp` + `cmp` ok |
| **M4** | inverser `PULSE_AUDIENCES` → `["members","students"]` | 1 | **1** : `⛔ LES MAÎTRES AVANT LES MEMBRES — l'ordre EST le produit` | `cp` + `cmp` ok |
| **M5** | remettre `/app/chat` sous `<KeelStudentRoute>` (`App.tsx`) | 1 | **1** : `laisse un profil réclamé atteindre /app/chat et /app/progress (A8.0)` ; les 3 autres cas du fichier restent **verts** (le cas qui passe tient) | `cp` + `cmp` ok |
| **M6** *(la mienne, obligatoire — H2)* | voir §3.1 | — | garde **prouvée porteuse** | fichier de test sorti de l'arbre |
| **M7** *(la mienne)* | `loadSkippedDishIndexes` : remettre `userId` à la place de `statesOwnerId` sur les deux lectures d'état | ≥ 1 | **0 sur 4 936** ⛔ | `cp` + `cmp` ok |
| **M8** *(la mienne)* | `withShiftTrace` n'appose plus rien (`return {...base}`) | ≥ 1 | **0 sur 4 936** ⛔ | `cp` + `cmp` ok |

**Aucun `git stash`, `checkout`, `reset` ni `restore` employé.** Preuve finale de restauration :
chaque fichier muté comparé par `cmp` à `git show cf2b8558:<fichier>` → **identique** ;
`git status --porcelain` → **vide**.

### 3.1 M6 — le run adversarial H2, écrit indépendamment, **sur l'écriture**

Le test du bâtisseur éprouve H2 sur la **lecture** (`loadStripDishes` → `[]`). J'ai écrit le mien
sur l'**écriture** (`applyStripTicks`), qui est ce qui coûte : ce que H2 achète, c'est une ligne
`protocol_events` sous le compte de l'attaquant portant le plat de la victime.

Fichier (hors de l'arbre, non commité, à disposition) :
`/private/tmp/claude-502/…/scratchpad/zz_verif_h2_write_test.ts`

La base doublée **filtre pour de vrai** (elle applique les `.eq` sur un jeu de lignes) au lieu de
rendre une réponse préparée : une garde manquante rend donc réellement la ligne étrangère.

| Épreuve | Résultat |
|---|---|
| ATTAQUANT ∈ foyer A, charge forgée citant le plan `household` du foyer **B** | `stale = 1`, `written = 0`, **0 ligne `protocol_events`** ✔ |
| **LE CAS QUI PASSE** — le même chemin, mais le plan est celui de **son** foyer | `written = 1`, `stale = 0`, 1 ligne écrite ✔ |

**Contrôle de morsure** (sans lui l'épreuve ne prouverait rien) : garde `.eq("household_id")`
retirée de `evening_strip_io.ts` → mon épreuve H2 **rougit**, `protocol_events` = **1 ligne**
écrite sous l'attaquant, portant `Le gigot de la victime`. La garde est **réelle et porteuse**.
**PROUVÉ.**

---

## 4. Les renversements (§2.1 n°10) — **écrits**, pas effacés

| Renv. | Où | Verdict |
|---|---|---|
| **R4** | `FF-048 §3` | **ÉCRIT.** La ligne « ❌ le chat, et les mesures corporelles » est **barrée** (`~~…~~`) et suivie d'un « ⟳ **RENVERSÉ le 2026-09-03 (A8.0, D8.1)** » qui dit **pourquoi ici** (« c'est ici que la phrase inverse vivait »), ce qui change, et ce qui **reste** hors périmètre (les mesures corporelles → FF-047). Daté, motivé, non supprimé. |
| **R4 bis** | `le-foyer/README.md` sous « ❌ Aucun canal 1:1 » | **ÉCRIT.** L'interdit est **gardé** ; ce qui est ajouté est « ce que ça n'interdit pas », daté, avec la limite (« le maître ne voit pas ce que le membre a répondu »). |
| **R14 amendé** | `FF-048 §6` | **ÉCRIT.** « Ce qui s'est élargi est la **porte**, pas le rôle — `keel_role` reste NULL », et il **nomme le test** qui tient les deux moitiés. |
| **R5** | `routeGuards.int.test.ts` | **RETOURNÉ AVEC UN CAS QUI PASSE**, et pas seulement assoupli. Le cas d'origine est **scindé en deux** : (a) `/app/chat` et `/app/progress` ⇒ `toContain("<KeelHouseholdRoute>")` **et** `not.toContain("<KeelStudentRoute>")` ; (b) `/app/today` ⇒ `toContain("<KeelStudentRoute>")` **et** `not.toContain("<KeelHouseholdRoute>")`. Les deux sens sont assertés dans les deux cas : c'est exactement ce que §2.3 n°23 demande. |
| **FF-058 §11** | `FF-058-la-bande-du-soir.md` | **ÉCRIT.** La question ouverte est barrée et **refermée** (« ✅ Refermée le 2026-09-03 »), avec ce qui la referme **et** ce qui reste à A8.1 (`dishIsFor`, les cases de `MyShareCard`). |
| **KeelHouseholdRoute.tsx** | commentaire du motif | **CORRIGÉ, pas effacé.** Le motif « ces trois écrans n'ont rien à montrer » est réduit à `/app/today` seul, et les deux autres reçoivent leur date et leur raison. |

**Aucun renversement silencieux.** Tous datés, tous motivés, tous à l'endroit où la phrase inverse
vivait. **PROUVÉ.**

---

## 5. ⛔ DÉVIATIONS DU CONTRAT §5.10 — la section grave

*(Une autre lane code contre ce contrat sans l'avoir. Ce qui suit est ce qu'elle croira acquis.)*

### D2 — `generated_from.shifts[]` est écrit, mais **rien ne le tient**. ⛔ DÉFAUT

**Fichier** : `supabase/functions/_shared/keel/accident_io.ts:100-113` (`withShiftTrace`) et
`:885-903` (le `payload` de `applyPlanShift`).

**Le code est juste** — je l'ai lu :

- **Forme exacte** : `{ cook_on, delta, new_cook_on, moved_dish_indexes: number[], applied_on }`.
- **Quand** : dans `applyPlanShift`, **après** que le glissement soit décidé et non `stale`,
  apposé au `payload` de l'`update` optimiste (le verrou `updated_at` est préservé).
  `applied_on` = `args.today`, le jour **local** du tap.
- **Apposé, jamais écrasant** : `{...base, shifts: [...previous, entry]}` ; un `shifts` qui ne
  serait pas un tableau est remplacé au lieu d'être accumulé.
- `buildShiftedPayload` reste à trois clés (R13 intact).

**Le défaut** : `grep -rn "shifts\|withShiftTrace" _shared/keel/*_test.ts` → **aucune occurrence**
(seul un faux positif, `"night shifts"` dans `household_voices_test.ts`).

**Mesure (M8)** : j'ai fait rendre `{...base}` à `withShiftTrace` — la trace n'est **plus jamais
écrite** — et j'ai lancé **toute** la suite Deno du domaine :
**`ok | 4936 passed | 0 failed`.**

**Pourquoi c'est grave, et pas cosmétique.** C'est nommément la 5ᵉ clause du contrat §5.10
(« `generated_from.shifts[]` (A8.0) pour "plans modifiés" »). A7 compte « plans modifiés » =
`shifts[]` ∪ faits `accident_off_plan` ∪ états `happened=false`. Le jour où une édition
supprime cette clé du `payload` — un `buildShiftedPayload` refactorisé, un `select` qui perd
`generated_from` —, **rien ne rougit**, et le compteur de SUIVI tombe silencieusement à zéro sur
sa première branche. C'est exactement la cicatrice `un lecteur sans écrivain` que ce lot tout
entier existe pour fermer, réintroduite **dans le contrat lui-même**.

**Ce que le bâtisseur doit faire** : une épreuve sur `withShiftTrace` (apposition sur un
`generated_from` qui porte déjà des clés ; `shifts` non-tableau remplacé ; deux glissements
successifs ⇒ deux entrées) **et** une épreuve sur `applyPlanShift` qui assert la présence et la
forme de `payload.generated_from.shifts[0]`. La mutation qui doit rougir est **M8**.

### Les autres clauses du contrat — **PROUVÉES**

| Clause §5.10 | Verdict |
|---|---|
| Route `/app/progress` sous `KeelHouseholdRoute` | ✔ `App.tsx`, tenu par `routeGuards.int.test.ts`, mutation M5 rougit |
| Garde de données : **toujours** `.eq("user_id", me)` en plus de RLS | ✔ aucun `.eq("user_id")` retiré ; les branches foyer sont **ajoutées**, gardées par deux `.eq` |
| Préfixes de faits **intacts** : `meal_tick:` (`meal_tick.ts:62`), `accident_off_plan:` (`accident_io.ts:645`), `slot_meal:` (`slot_meal_io.ts:40`) | ✔ **aucun des trois n'est touché par le diff** ; `plan_relation: "as_planned"` et `evidence_weight: 0.4` inchangés |
| Plans de la personne : les siens **et**, pour un membre, les `household` de son foyer | ✔ côté serveur par `resolvePlanScope` ; ✔ côté front par la policy **existante** `student_generated_meals_household_read` — **vérifiée en SQL** : `household_id = keel_household_of(auth.uid()) AND plan_kind='household'` |
| « avec sa part » (`member_portions`) | ⚠️ **hors A8.0, assumé** : les lecteurs serveur d'A8.0 ne sélectionnent pas `member_portions` (ils n'en ont pas besoin), et le filtrage par `dishIsFor` est **explicitement renvoyé à A8.1** (FF-058 §11 le dit). A7 lit `member_portions` par RLS depuis le front : la clause tient. **Conséquence à connaître** : d'ici A8.1, la bande ③ d'un membre porte **tous** les plats du jour du foyer, pas seulement les siens. |
| `meal_share_outcomes` inconnu tant qu'A8.2 n'est pas là | ✔ hors périmètre, aucune migration |

---

## 6. Les autres défauts

### D1 — la cascade du maître sur la bande du membre n'est tenue par **aucun** test. ⛔ DÉFAUT

**Fichier** : `supabase/functions/_shared/keel/accident_io.ts:457-500`
(`loadSkippedDishIndexes`, paramètre `statesOwnerId`) et son unique appelant de production
`supabase/functions/_shared/keel/evening_strip_io.ts:318-322` (`statesOwnerId: planOwnerId`).

**Le code est juste** : les **états** (cuisson, vagues ratées) se lisent sous le compte qui a
**écrit le plan** (`planOwnerId`, lu sur la ligne `student_generated_meals.user_id`), la **coche
vivante** sous **la personne** (`args.userId`). Deux clés, deux natures de fait (FF-058 R10). Le
repli `|| args.userId` sur une ligne sans `user_id` lisible restaure le comportement d'avant.

**Le défaut** : **rien ne distingue les deux.** Mesure **(M7)** : j'ai remis `args.userId` à la
place de `args.statesOwnerId` sur les **deux** lectures — c'est-à-dire j'ai restauré exactement le
défaut d'avant A8.0 — et la suite Deno entière du domaine reste **`ok | 4936 passed | 0 failed`**.

Le paramètre est **requis**, donc `deno check` attrape un appelant qui l'**oublie**. Il n'attrape
pas un appelant qui passe la **mauvaise valeur** — et c'est le cas dangereux, parce que
`statesOwnerId: userId` compile, se lit bien, et rend `[]` en silence pour un membre.

**Pourquoi c'est grave.** C'est la seule raison d'être de l'ordre du cron. Le mandat exige
« maîtres traités **avant** membres dans le même tick » ; M4 prouve que l'**ordre** est tenu ; rien
ne prouve que ce que l'ordre sert **arrive**. Le point n°4 du run réel que le bâtisseur consigne
(« la bande du membre servie ensuite est amputée ») est donc la **seule** épreuve de ce
comportement, et elle n'a pas été jouée. FF-058 §11 l'affirme pourtant comme un fait acquis.

**Ce que le bâtisseur doit faire** : une épreuve dans `member_plan_scope_test.ts` (la base doublée
y est déjà) — un membre du foyer H, un plan `household` écrit sous le maître M, un
`cooking_session_states` `happened=false` écrit sous **M** ⇒ le plat qui en descend **disparaît**
de la bande du membre ; et le cas symétrique, un état écrit sous le **membre** ⇒ **sans effet**.
La mutation qui doit rougir est **M7**.

### Rien d'autre

Aucun autre défaut trouvé. Les invariants de §2.4 que le lot touche sont tenus (§7).

---

## 7. Cohérence (C4, C8)

**C8 — le foyer à deux comptes.** `respondsForHousehold` (`evening_strip_io.ts:98-117`) rend
`role === "owner"` ; un `role='member'` ⇒ `false`, une lecture en panne ⇒ `false` (fail-closed).
Ce booléen `masterOnly` est lu **une seule fois** par tour, puis gate :

- `keel-daily-pulse-v1/index.ts:438` — ① courses : `… && masterOnly && …`
- `keel-daily-pulse-v1/index.ts:440` — ② cuisson : `… && masterOnly && …`
- `day_review_io.ts:89` (courses) et `:90` (cuisson) — même forme

**③ n'est gaté par rien** : il va à chacun. **PROUVÉ par lecture.** *(Que ça se voie en run réel :
ROUGE, §8.)*

**C4 — les personnes.** Le maître ne voit jamais la coche d'un profil réclamé :

- côté base, `protocol_events_owner_read` = `auth.uid() = user_id` (**vérifié en SQL**) ;
- côté serveur, `loadLiveTickIndexes` reste appelé avec `args.userId` — **A8.0 ne l'a pas
  déplacé vers `statesOwnerId`**, et c'est le bon arbitrage : pour un maître,
  `planOwnerId === userId`, rien ne change ; pour un membre, il lit **ses** coches sur le plan de
  son foyer.

Aucune lecture croisée introduite. **PROUVÉ.**

---

## 8. 🔴 NON PROUVÉ — le geste humain exact

### 8.1 Run réel du cron — **ROUGE (hors fenêtre, interdit au vérificateur)**

Interdit par mon mandat (`pulse_audience.ts` est un fichier **neuf** : sans restart du runtime
edge, tout appel rendrait un faux résultat — cicatrice
`edge-runtime-serves-stale-shared-modules`). **Rien n'a été appelé.**

**Ce que j'ai pu établir en SQL (lecture seule), et qui rend le run non vide** :

```sql
select count(*) from household_members where role='member' and user_id is not null;  -- 10
```

Détail des 10, avec le nombre de plans `household` vivants de leur foyer :

| `keel_role` | nb | plans foyer | ce que le cron en fera |
|---|---|---|---|
| `'student'` | **7** | 1 à 4 | servis en **phase 1**, puis **écartés** en phase 2 et comptés dans `members_already_student` |
| `NULL` | **3** | 3, 1, **0** | servis en **phase 2** — c'est la population que le lot ouvre |

**Attendu d'un run sur cette base** : `members_scanned = 3`, `members_already_student = 7`,
`members_sent ≥ 1` (deux des trois ont un plan de foyer vivant). **`members_scanned: 0` serait le
défaut que le lot ferme, revenu par la porte d'à côté.**

**Le geste humain, dans l'ordre :**

1. `docker restart supabase_edge_runtime_Sophia_2`, puis
   `TIMEOUT_MS=900000 ./scripts/local_extend_kong_functions_timeout.sh`
   *(sans le restart, `pulse_audience.ts` — fichier neuf — n'est pas servi).*
2. Fixture `qa0903m` par **SQL** : un foyer à deux comptes (un maître ; un profil **réclamé** avec
   `household_members.user_id` posé, `role='member'`, `access_tier='household_member'`,
   `keel_role` **NULL**), un plan `plan_kind='household'` vivant couvrant aujourd'hui.
   Patrons : `docs/keel/qa-fixtures/*.sql` + `scripts/2026-09-01-fixture-foyer-retours.ts`,
   mot de passe `1234567` par `crypt`, colonnes de jeton `''` **jamais NULL**.
   ⚠️ `trial_seat_limit = 3` : un tag par lane.
3. Invoquer `keel-daily-pulse-v1` par son chemin documenté, et vérifier au compte-rendu :
   - **(1)** `members_scanned ≥ 1` **et** `members_sent ≥ 1` ;
   - **(2)** la bulle du membre porte **③ seul** — aucune question de courses, aucune de cuisson ;
     le maître, le même soir, reçoit **les trois** (C8) ;
   - **(3)** la bande du membre est bâtie sur le plan `household` de **son** foyer (les plats du
     jour du maître), pas un silence ;
   - **(4)** l'ordre : le maître déclare une cuisson ratée dans le tick ⇒ la bande du membre
     servie ensuite est **amputée** des plats qui en descendent. **C'est aussi la seule preuve
     existante du défaut D1** — à jouer en priorité ;
   - **(5)** le curseur à deux phases : un tick coupé par le budget rend `audience` +
     `next_audience` + `next_after_user_id` ; le rappel avec ces trois valeurs reprend **sans**
     rejouer la phase 1.

### 8.2 Navigateur sous un compte membre — **ROUGE (session requise)**

Serveur lancé sur **5211** (`frontend-verif2`, entrée ajoutée puis **restaurée par `cp`**,
`cmp` ok). `Object.keys(localStorage)` → **`[]`** : **aucune session**.

**Ce qui se voit sans session, et qui est vert** :

| Mesure | 375 px | 1280 px |
|---|---|---|
| `/app/chat` | redirige vers `/auth?redirect=%2Fapp%2Fchat` | idem |
| `/app/progress` | redirige vers `/auth?redirect=%2Fapp%2Fprogress` | — |
| `document.scrollWidth` / `clientWidth` | 375 / 375 | 1280 / 1280 |

Aucun débordement horizontal, aucune erreur de montage, la page de connexion rend en `fr`.

**Ce que ça ne prouve pas** : la garde qui change est `KeelStudentRoute` → `KeelHouseholdRoute`,
et **elle ne se distingue qu'avec une session**. Un non-connecté est refusé par les deux.

**Le geste humain** : se connecter avec le compte **membre** de `qa0903m` (mot de passe
`1234567`), puis, en **320 px et 1280 px**, en **fr** et en **en** :

- `/app/chat` **s'ouvre** (avant A8.0 : redirection) et porte la bande ③ du soir ;
- `/app/progress` **s'ouvre** ;
- `/app/today` **redirige toujours** (le cas qui passe de R5) ;
- vérifier qu'aucune question de courses ni de cuisson n'apparaît chez le membre.

*(Interdit et non tenté : forger un jeton, écrire dans `auth.sessions`, lire un secret de
conteneur, l'API admin d'auth, entrer un mot de passe.)*

---

## 9. Hors périmètre croisé, nommé et non touché (§2.1 n°11)

- `_shared/keel/pot_demand_test.ts` — **rouge étranger non nommé au mandat**, antériorité prouvée
  (§1.3). Il bloque `deno test _shared/keel/` en bloc : à signaler à l'orchestrateur.
- `daily_pulse.ts` / `daily_pulse_test.ts` / `daily_pulse_locale_test.ts` — 13 erreurs TS
  antérieures, byte-identiques à `31ee930f`.
- Les 5 rouges vitest étrangers — nommés, non touchés, **aucune baseline régénérée**.
- La lane 1:1 (`plan_versions`), la fusion / prise de main (**lue** par `resolvePlanScope` pour lui
  céder la priorité, jamais écrite), `keelGenerationModel()` sur la lane foyer.
- `HouseholdPage.tsx` — région de la lane FOYER. Non ouvert.

---

## 10. VERDICT

**Ce qui est prouvé** : le périmètre, l'absence de migration / i18n / bump de prompt, l'antériorité
des rouges étrangers (y compris un que le mandat ne nommait pas), `tsc` exit 0, vitest et Deno
verts hors rouges étrangers, les trois interdits du mandat tenus **dans le code**, les deux
lecteurs gardés par **deux** `.eq` et non par un retrait, l'absence d'un troisième lecteur oublié,
les **cinq** mutations du rapport rougissant exactement comme annoncé, mon run adversarial H2 sur
le chemin d'**écriture** avec son contrôle de morsure, les **cinq** renversements écrits et datés
là où la phrase inverse vivait (dont R5 retourné **avec** un cas qui passe), C4 et C8 tenus dans
le code, et les six clauses du contrat §5.10 sur sept.

**Ce qui manque** : deux comportements neufs et load-bearing que **rien** ne tient — la cascade du
maître sur la bande du membre (**D1**), et la trace `generated_from.shifts[]` (**D2**, une clause
du contrat §5.10). **Mesuré, pas soupçonné** : 4 936 épreuves restent vertes quand je supprime
l'un ou l'autre.

Le mandat de vérification est explicite : **ROUGE dès qu'un défaut existe** — « une mutation qui ne
rougit pas », « une déviation du contrat ». Il y en a deux, dont une sur le contrat.

# ⛔ ROUGE
