# VÉRIFICATION — MEMBRE · A8.1 et A8.2, après fusion dans l'arbre principal

**Vérificateur** · worktree `/Users/ahmedamara/Dev/Sophia-2-chantiers/VERIF`, **détaché** sur
`e43eab06` (HEAD de l'arbre principal au démarrage). Rien n'est commité, nulle part.
Commits vérifiés : `7c403607` (A8.1), `2e330f85` · `e25d8578` · `26ad59b9` (A8.2).
Base d'antériorité pour les rouges étrangers : `31ee930f`.

> Écrit au fil de l'eau, dès la première mesure. Les sections se complètent en cours de route.

---

## 0. Poste

- `git status --short` dans VERIF : **vide** avant et après le détachement.
- `node_modules` → lien vers l'arbre principal ✅ · `frontend/node_modules` → lien ✅ ·
  `frontend/.env.local` présent ✅.

---

## 1. Statique — le périmètre du diff

**Union des fichiers touchés par les quatre commits** (30 chemins, dont 3 de `scratchpad/`) :

| famille | fichiers | dans le mandat ? |
|---|---|---|
| A8.1 écran/API | `keel/api/household.ts` + son `.int.test`, `api/mealTicks.int.test`, `api/myShare.int.test`, `components/DishCard.tsx`, `components/HouseholdPlanCard.tsx`, `components/plan/DishListByDay.tsx`, `components/plan/MyShareCard.tsx`, `components/plan/PlanByPerson.tsx`, `lib/dishListByDay.ts` + test, `lib/planByPersonModel.ts`, `lib/useMealTicks.ts`, `pages/StudentWeekPlanPage.tsx` | ✅ §5.8 (`useMealTicks`, `MyShareCard`, bande ③) |
| A8.2 base | `supabase/migrations/20260903172000_…sql`, `_shared/keel/meal_share_outcomes_rls_test.sql` | ✅ §5.9 |
| A8.2 RGPD | `account-export-v1/export_scope.ts`, `account-export-v1/index.ts`, `keel_gdpr_lifecycle_test.ts`, `purge-deleted-accounts/index.ts` | ✅ §5.9 (« export RGPD dans la même migration ») |
| A8.2 modèle | `_shared/keel/meal_share_outcome.ts` + test | ✅ §5.9 |
| A8.2 `leftover` | `_shared/keel/accident.ts` + test, `_shared/chat/accident_tap.ts` | ✅ §5.9 (« `leftover` gagne son lecteur ») |
| fiches | `FF-054-le-retour-de-fin-de-plan.md`, `FF-058-la-bande-du-soir.md` | ✅ §5.9 explicitement |

**Aucun fichier hors mandat.** ✅

- **Une seule migration** : `20260903172000_la_boite_du_membre_est_un_fait_par_bouche.sql`. ✅
- **Aucun pack i18n touché** (`en.ts` / `fr.ts` / `catalog.ts`) — cohérent avec « aucune clé n'est
  écrite dans ce lot ». ✅
- **Aucun bump de version de prompt** par MEMBRE (`grep '^+.*(MEAL|HOUSEHOLD)_PROMPT_VERSION'` sur
  les quatre commits : vide). ✅

(le reste s'écrit au fil des mesures)

---

## 2. Les suites, rejouées

| suite | mesure | verdict |
|---|---|---|
| `cd frontend && npx tsc -b --force` | **exit 0**, aucune sortie | ✅ |
| `npx vitest run` (entière) | `Test Files 2 failed \| 121 passed \| 5 skipped (128)` · `Tests 4 failed \| 2072 passed \| 20 skipped (2096)` | ✅ (4 rouges, tous étrangers, cf. ci-dessous) |
| `deno test` sur les 9 fichiers du domaine MEMBRE (`meal_share_outcome`, `accident`, `evening_strip`, `day_review`, `member_cascade_and_shift_trace`, `member_plan_scope`, `pulse_audience`, `household_plan_kind_readers`, `missed_wave_reader`) | **`ok \| 138 passed \| 0 failed`** | ✅ |

### Les rouges vitest — antériorité **prouvée**, pas citée

Détaché sur `31ee930f` (la base d'antériorité), `npx vitest run src/keel/api/household.int.test.ts
src/edge/coverage-guard.int.test.ts` rend **exactement les quatre mêmes** :

```
Test Files  2 failed (2)
     Tests  4 failed | 26 passed (30)
FAIL coverage-guard … all Edge Functions are in the known list
FAIL coverage-guard … all DB triggers are in the known list
FAIL household.int.test.ts > awayFrom … ne rend que la source demandée
FAIL household.int.test.ts > awayFrom … un jour inconnu tombe sans emporter les autres
```

⚠️ **Point d'attention** : les deux rouges `awayFrom` vivent dans
`frontend/src/keel/api/household.int.test.ts`, **un fichier que A8.1 touche**. J'ai donc vérifié
plus finement : le `diff 31ee930f..HEAD` de `household.ts` ne touche **pas** `awayFrom` (le corps
est identique), et le fichier de test à `31ee930f` porte des assertions identiques mot pour mot.
Le rouge vient d'une lane voisine qui a ajouté `kind: "away"` en aval. **Pas MEMBRE.**

**Le `mealBoxes` que le bâtisseur listait comme 5ᵉ rouge étranger n'existe plus** : la suite en
compte 4. Il a été réparé entre-temps par une autre lane. Aucun rouge neuf. ✅

### Les 7 rouges Deno de la lane CUISINE — comptés, non touchés

`deno test` sur les sept fichiers nommés dans le mandat : `FAILED | 299 passed | 7 failed`,
**exactement sept**, tous des épinglages de `MEAL_PROMPT_VERSION` (« la version de prompt a bougé
avec ce lot », « le TRONC bumpe, l'enveloppe FOYER ne bouge pas », …). Aucun autre. **Pas MEMBRE**,
et rien n'a été modifié.

---

## 3. A8.1 — la POSITION (le risque que le bâtisseur nomme)

### Ce qui est prouvé par lecture

- `HouseholdDishView.dishIndex` existe et **est capturé avant le filtre** :
  `household.ts:1920` `raw.map((entry, dishIndex) => …)`, et le `.filter((d) => d.title !== "")`
  est en `1947`, **après**. ✅
- `MyShareCard.tsx:319` retrouve le plat par
  `myDishes.find((d) => d.dishIndex === dishIndex)` — **jamais** `myDishes[dishIndex]`
  (`grep -E 'myDishes\[' MyShareCard.tsx` : aucune occurrence hors commentaire). ✅
- `MyShareCard.tsx:322` appelle **`ticks.bindAt(`**, et `bind` n'apparaît nulle part dans le
  fichier. ✅
- `groupDishListByDay` **transporte** `dishIndex` sans le recalculer (`dishListByDay.ts:84`) ;
  le champ est **requis** (`dishIndex: number | null`, pas `?`) dans l'entrée du regroupement. ✅

### MUTATION — renuméroter sur la vue (celle que le mandat demande)

`household.ts:1947` : `.filter((d) => d.title !== "")` →
`.filter((d) => d.title !== "").map((d, i) => ({ ...d, dishIndex: i }))`.

```
× A8.1 — `readHouseholdDishes` numérote les plats du PLAN, pas de la vue
    > ⛔ UN PLAT SANS TITRE EST ÉCARTÉ, ET NE DÉCALE PERSONNE
× A8.1 — `readHouseholdDishes` numérote les plats du PLAN, pas de la vue
    > plusieurs trous, et le dernier plat garde sa vraie position
Tests  4 failed | 67 passed
```

**2 rouges neufs** (le bâtisseur en annonçait 5 pour sa M7, qui était une mutation plus large —
la capture retirée du `map` fait aussi tomber la compilation et des cas voisins). La garde **mord**
là où il faut : au point de lecture, le seul endroit où l'index est vrai. Restauré par `cp`,
`cmp` vert, `git status --short` vide. ✅

### 🔴 DÉFAUT D1 — `bindAt` n'a AUCUNE épreuve comportementale, et le mode d'échec exact qu'il existe pour éviter est réintroductible en silence

**Ma mutation (celle que j'ajoute)**, `useMealTicks.ts:416` :

```ts
-      bindIndex(dish, dishIndex, onDate),
-    [bindIndex],
+      bindIndex(dish, dishes.indexOf(dish as unknown as GeneratedDish), onDate),
+    [bindIndex, dishes],
```

C'est **mot pour mot** le défaut que le journal du bâtisseur décrit comme la raison d'être de
`bindAt` : « `indexOf` y aurait rendu `-1` pour chaque plat — **aucune case, en silence**, et une
carte qui a l'air de marcher ». Mesure :

```
TSC = 0
Test Files  2 failed | 121 passed | 5 skipped (128)
     Tests  4 failed | 2072 passed | 20 skipped (2096)   ← les 4 rouges ÉTRANGERS, et rien d'autre
```

**Aucun rouge.** La carte du membre ne rendrait plus une seule case, et la suite entière reste
verte. Vérifié ensuite : `grep -rln bindAt frontend/src` ne rend que trois fichiers —
`MyShareCard.tsx`, `useMealTicks.ts`, et `dishListByDay.int.test.ts`, où l'épreuve est un **grep
de source** (`expect(card).toContain("ticks.bindAt(")`) qui reste vrai sous ma mutation. Il
n'existe **aucun** test comportemental de `useMealTicks` (ce dépôt ne monte pas de composants React
en test).

**Ce que le bâtisseur doit faire** : soit extraire du hook la seule décision qui compte — « position
donnée + date ⇒ clé `meal_tick:<planId>:<idx>` ou `null` » — en fonction pure éprouvable ; soit, à
défaut, une garde de source sur le **corps** de `bindAt` (`indexOf` interdit à l'intérieur, comme
`myDishes[dishIndex]` l'est dans la carte). Une garde qui n'existe qu'au site d'appel ne dit rien
de ce que l'appelé fait de son argument. Restauré par `cp`, `cmp` vert. ✅

### A8.1 — les interdits

| interdit | vérification | verdict |
|---|---|---|
| le maître ne voit **aucune** case sur `/app/plan` | `selectMyShare` (`myShare.ts:94`) rend `null` sur `isOwner`, `MyShareCard.tsx:244` `if (!share) return null;` **avant** le `<DishListByDay` de la ligne 293 | ✅ |
| la vue par personne du maître passe `null` | `PlanByPerson.tsx` : `<DishListByDay groups={week} bindTick={null} />` ; **et** `planByPersonModel.ts` `dishIndex: null as null` — garde double, comme annoncé | ✅ |
| « ce que la maison cuisine » ne coche pas | `HouseholdPlanCard.tsx` : `bindTick={null}` | ✅ |
| `sharePresentedTo` survit | importé et appelé `MyShareCard.tsx:222` ; `myShare.int.test.ts` l'éprouve toujours | ✅ |
| la coche du membre s'écrit sous **son** `user_id` | `StudentWeekPlanPage.tsx` : `const userId = user?.id ?? null` (`useAuth`), `userId={userId}` · `householdMealId={householdMeal?.mealId ?? null}` — deux identités distinctes ; l'épreuve `mealTicks.int.test.ts` interdit que la prop `userId` matche `/household\|mine\|owner\|member/i` | ✅ |
| `writeOffPlanTapFact` / `meal-photo-upload-v1` inchangés | absents du diff des 4 commits | ✅ |
| aucune coche automatique | aucun `tickMeal(` hors `useMealTicks` ; l'épreuve des trois surfaces l'interdit explicitement | ✅ |

---

## 4. A8.2 — la migration `20260903172000`

### 🔴 DÉFAUT D2 (grave, et il a mordu SUR MOI) — la migration se COMMITTE toute seule : « en transaction annulée » est impossible telle qu'elle est écrite

Le fichier porte **son propre** `begin;` (ligne 64) et **son propre** `commit;` (ligne 511).
La recette écrite dans le journal du bâtisseur — `begin; \i <migration> rollback;` — donne donc :

```
BEGIN
WARNING:  there is already a transaction in progress   ← mon begin, avalé
BEGIN
CREATE TABLE … CREATE POLICY … CREATE FUNCTION … DO
COMMIT                                                  ← LA MIGRATION SE COMMITTE
WARNING:  there is no transaction in progress
ROLLBACK                                                ← no-op
```

Et la vérification d'après :
`select count(*) from information_schema.tables where table_name='meal_share_outcomes'` → **1**.

**La table, ses deux fonctions, sa policy et son index ont réellement été créés dans la base
locale partagée, hors registre.** C'est arrivé à moi, du premier coup, en suivant la recette du
journal ; le §1 du journal affirme pourtant « Vérifié après coup : `information_schema.tables`
compte **0** ligne ». Cette affirmation **ne peut pas** être vraie du geste décrit.

**Réparation faite immédiatement** (défaire ma propre mutation, pas un `git` quelconque) :
`drop table public.meal_share_outcomes;` puis `drop function` ×2. Base revérifiée :

```
tables=0 | fonctions=0 | registre('20260903172000')=0 | tête du registre=20260903150000
```

⚠️ **Conséquence pour la fenêtre** : quiconque relit ce journal et rejoue la recette **applique**
la migration pour de bon, **sans l'inscrire au registre** — et le `supabase migration up` d'après
échouera ou passera à côté. **Ce que le bâtisseur doit faire** : retirer `begin;`/`commit;` du
fichier (les autres migrations du dépôt s'appliquent dans la transaction du CLI), ou, à défaut,
écrire dans le journal la recette qui marche : **retirer les deux lignes avant d'envelopper**.

Toutes mes mesures ci-dessous sont faites sur une copie **dépouillée** de ses `begin;`/`commit;`
(`mig_stripped.sql` hors du dépôt), enveloppée dans `begin; … rollback;`. **Base vérifiée à 0
après chaque run.**

### Le bloc `do $$` — il passe, et la faiblesse avouée est RÉELLE

- Migration seule, transaction réellement annulée : `DO` puis `ROLLBACK`, **aucune exception**.
  Base après : `tables=0 fonctions=0`. ✅
- **MUTATION — les DEUX `check` neutralisés** (`check (outcome is not null)` et
  `constraint meal_share_outcomes_day_matches_outcome check (true)`) : le bloc **passe quand
  même** (`DO`, `ROLLBACK`, aucune erreur). **L'aveu du bâtisseur est confirmé** : les contrôles
  (e) et (f) interceptent `foreign_key_violation` en plus de `check_violation`, et les uuid bidons
  font tomber la FK d'abord. Ces deux contrôles ne gardent **rien**.

### 🔴 DÉFAUT D3 — et non, ces CHECK ne sont PAS « tenus ailleurs » : la moitié ne l'est nulle part

Le journal dit : « Ce que ces deux CHECK gardent est **tenu par le test RLS** (§2, cas 01/03/12) ».
**Mesuré, c'est faux.**

| mutation | mesure |
|---|---|
| **MUT-H** — les deux CHECK neutralisés, **test RLS complet rejoué** | **18 assertions vertes**, aucune erreur |
| **MUT-I** — seul `meal_share_outcomes_day_matches_outcome` neutralisé, `deno test meal_share_outcome_test.ts` | **16 passed, 0 failed** — vert |
| **MUT-J** — le CHECK du vocabulaire neutralisé, même test | **`⛔ le vocabulaire est CELUI DE LA BASE, lu dans la migration` FAILED** — rouge |

Raison : le test RLS passe par la **RPC**, dont les `if` rendent `bad_outcome` /
`bad_shifted_to_day` **avant** de toucher la table. Il n'atteint jamais les CHECK.

- Le CHECK du **vocabulaire** est tenu — par une garde de **texte** (`meal_share_outcome_test.ts`
  cherche `check (outcome in (` dans le fichier de migration et exige que les trois listes
  coïncident). Faible mais réelle : elle mord.
- ⛔ Le CHECK **`shifted` ⟺ `shifted_to_day`** n'est tenu **nulle part** : ni par le bloc `do $$`
  (la FK tombe avant), ni par le test RLS (la porte intercepte), ni par le Deno (il ne le nomme
  pas). On peut le retirer, tout reste vert. Or c'est lui qui empêche « une ligne `discarded`
  datée » — exactement le cas où « le lecteur devrait deviner ».

**Ce que le bâtisseur doit faire** : dans (e) et (f), insérer sur des uuid **réels** (le bloc
peut créer sa propre fixture minimale, comme le fait le test RLS) au lieu d'attraper
`foreign_key_violation` ; ou déplacer ces deux cas dans le test RLS, **après** la porte, par un
`insert` direct sous `service_role`. Un `when foreign_key_violation then null` dans un contrôle
qui vise un CHECK est une garde désarmée qui ressemble à une garde qui marche.

### Les droits — vérifiés par `has_table_privilege`, jamais par lecture du fichier

Sonde exécutée **dans la transaction annulée**, après la migration :

```
     role      | sel | ins | upd | del | trunc
 anon          |  f  |  f  |  f  |  f  |  f
 authenticated |  t  |  f  |  f  |  f  |  f      ← revoke pris DÈS la migration
 service_role  |  t  |  t  |  t  |  t  |  t

     role      | jumelle _for | porte client
 anon          |      f       |      f
 authenticated |      f       |      t          ← la jumelle lui est FERMÉE
 service_role  |      t       |      t
 public        |      f       |      f
```

- `revoke` de `authenticated` : **effectif dès la migration**. ✅
- jumelle `_for(p_user)` : **`service_role` seul** (`authenticated`, `anon`, `public` : `f`). ✅
- `relrowsecurity = t`. La policy `meal_share_outcomes_read` est en **`for select` seulement**
  (`polcmd = 'r'`), et ses trois branches sont bien celles annoncées, dont
  `target.user_id IS NULL` sur la branche du maître. ✅
- Les deux fonctions sont `SECURITY DEFINER` avec `search_path=""`. ✅ (non demandé, noté)

### Le test RLS — 18/18, avec un foyer voisin RÉEL dans la fixture

Rejoué (migration dépouillée + test, **une seule** transaction annulée) : **18 `OK`**, `ROLLBACK`,
base revérifiée `tables=0 users=0 foyers=0`.

**Le foyer voisin est bien là**, et il n'est pas décoratif : `households` porte
`a8021111-…-0002 « Maison voisine »`, `household_members` un `Voisin` `owner` dedans, et
`student_generated_meals` un **second plan** `a8023333-…-0002` sous le voisin. C'est ce second
plan qui rend le cas 10 (`not_your_plan`) et le cas 07 (« un intrus ne lit RIEN ») **non vides**. ✅

#### Les trois mutations du bâtisseur, rejouées — les trois mordent

| # | mutation | rouge obtenu |
|---|---|---|
| MUT-D | `and target.user_id is null` retiré de la 3ᵉ branche de la policy | `ERROR: 04b ⛔ et RIEN de la ligne du profil réclamé : attendu 0, obtenu 1` |
| MUT-E | `if v_role <> 'owner' or v_target.user_id is not null` → `if v_role <> 'owner'` | `ERROR: 02 le MAÎTRE ne déclare PAS pour un profil réclamé : attendu reason=not_your_line, obtenu {"ok": true}` |
| MUT-F | `and m.household_id = v_household` retiré du contrôle du plan | `ERROR: 10 un plan d'un AUTRE foyer est refusé : attendu reason=not_your_plan, obtenu {"ok": true}` |

Base vérifiée à 0 après chacune. ✅

### 🔴 DÉFAUT D4 — **MUT-G, la mienne** : la 4ᵉ colonne de la clé n'a aucune épreuve qui morde

Mutation : `primary key (generated_meal_id, dish_index, member_id, declared_by)` →
`primary key (generated_meal_id, dish_index, member_id)` (et le `on conflict` aligné).

**Résultat : 18 assertions sur 18 restent vertes.** Aucun rouge.

Le cas 11 dit pourtant, mot pour mot : « deux lignes sur le même plat, et aucune n'a écrasé
l'autre. **C'est ce que la 4ᵉ colonne de la clé achète.** » Or il compte les lignes de
`(meal, dish_index=0)` — et les deux lignes de la fixture portent des **bouches différentes**
(la sienne, et l'enfant). Avec une clé à **trois** colonnes elles coexistent tout autant :
le cas 11 ne teste pas ce qu'il dit tester.

Et la colonne **est** portante — par un chemin que la fixture ne joue pas : le maître range la
boîte d'une bouche **sans compte**, la bouche est **réclamée plus tard** (`user_id` posé), la
personne déclare à son tour. Avec une clé à trois colonnes, le `on conflict do update` **n'écrit
pas `declared_by`** : la ligne garderait le maître comme auteur en portant le sort déclaré par la
personne — un fait faux sur QUI a parlé, que l'export RGPD (indexé sur `declared_by`) rendrait
alors **au maître**.

**Ce que le bâtisseur doit faire** : un cas où **la même bouche** porte deux lignes de deux
auteurs. La fixture le permet en trois lignes : le maître déclare sur la bouche sans compte
(déjà fait, cas 03), puis `update household_members set user_id = … where member_id = …-0003`,
puis cette personne déclare sur sa propre bouche — et on assert `count = 2` **et** deux
`declared_by` distincts. Sans ça, `declared_by` dans la clé est une décision non gardée.

---

## 5. A8.2 — l'export RGPD, cousu

| couture | vérification | verdict |
|---|---|---|
| `export_scope.ts` | `mealShareOutcomes` = `generated_meal_id,dish_index,member_id,outcome,shifted_to_day,answered_at,answered_local_date` — **7 colonnes**, `declared_by` **absent**, `member_id` **présent** | ✅ |
| `account-export-v1/index.ts` — ordre positionnel | le `fetchKeelRows(admin, "meal_share_outcomes", SCOPE.mealShareOutcomes, "declared_by", …)` est **immédiatement après** celui de `meal_composition_verdicts` et **avant** le repli `recurring_meals` ; la destructuration porte `mealCompositionVerdicts, mealShareOutcomes, recurringMeals` — **même fente** des deux côtés | ✅ |
| colonne propriétaire | `"declared_by"`, pas `user_id` ni `member_id` | ✅ |
| section de bundle | `sort_de_mes_parts` sous `mes_repas_generes.json` | ✅ |
| `keel_gdpr_lifecycle_test.ts` | `meal_share_outcomes: [[["declared_by"], "sa propre cle"]]` dans `HORS_EXPORT` (ligne 1059) ; **une seule occurrence** dans tout le fichier ⇒ **absent de `NON_RECLAMEES`** (ligne 734) | ✅ |
| `purge-deleted-accounts/index.ts` | `.from("meal_share_outcomes").delete().eq("declared_by", userId)` explicite, en plus de la cascade | ✅ |
| `deno check` sur les 4 fichiers | **0 erreur** | ✅ |

🔴 **NON PROUVÉ (geste humain requis)** : `keel_gdpr_lifecycle_test.ts` — ses **trois** `Deno.test`
sont `{ ignore: SKIP_INTEGRATION }`, et `SKIP_INTEGRATION` est vrai tant que `SUPABASE_URL`,
`VITE_SUPABASE_ANON_KEY` et `SUPABASE_SERVICE_ROLE_KEY` sont absents. Je **ne les pose pas** :
la cicatrice `qa-env-exports-poison-the-suite` (114 faux rouges) est chère, et le test exige de
toute façon la **migration appliquée**, ce que le mandat m'interdit.
**Geste humain, dans la fenêtre, dans cet ordre** :
1. `select version from supabase_migrations.schema_migrations order by 1 desc limit 3` — la tête
   était `20260903150000` à 18 h 40, donc `20260903172000` est encore **dans l'ordre** ;
2. `supabase migration up` ;
3. `cd supabase/functions && SUPABASE_URL=… VITE_SUPABASE_ANON_KEY=… SUPABASE_SERVICE_ROLE_KEY=… deno test --allow-all keel_gdpr_lifecycle_test.ts` — **dans un shell jetable**, jamais dans celui qui lance la suite.

---

## 6. A8.2 — le modèle pur, mutations rejouées

| # | mutation | rouge obtenu | annoncé |
|---|---|---|---|
| M12 | `if (input.hasFreezer) out.push(frozen)` → inconditionnel | `⛔ SANS CONGÉLATEUR DÉCLARÉ…` + `⛔ UN JOUR ÉCOULÉ NE SE PROPOSE PAS` — **2 rouges** | 2 ✅ |
| M13 | `.filter((m) => m.userId === null && …)` → sans `userId === null` (**R11**) | 3 rouges dont `⛔ UNE BOUCHE AVEC UN COMPTE N'EST JAMAIS COCHABLE (R11)` **et** `LE CAS QUI PASSE` | 3 ✅ |
| M14 | les deux `continue` d'autorité retirés (le dernier écrit gagne = trancher par date) | `⛔ D8.3 — LA LIGNE DE LA PERSONNE GAGNE, ET PAS LA PLUS RÉCENTE` | 1 ✅ |
| **MK (la mienne)** | `boxStillWaiting` : `shiftedToDay !== null && shiftedToDay >= today` → `shiftedToDay !== null` (une boîte reportée à un jour **dépassé** « t'attend » encore) | `⛔ « ENCORE AU FRIGO » A UNE FIN` — **1 rouge** | — ✅ |

Restaurés par `cp`, `cmp` vert, `git status --short` vide. Et le pin de `MAX_FRIDGE_DAYS` est
sain : l'épreuve appelle `boxOptionsFor` **sans** `maxFridgeDays` (donc sur la vraie constante) et
épingle **à la fois** la liste littérale de 3 jours et `=== MAX_FRIDGE_DAYS` — changer la constante
la fait rougir, ce n'est pas un test paramétré par lui-même.

### `leftover` (26ad59b9)

| # | mutation | rouge obtenu | annoncé |
|---|---|---|---|
| M15b | `shareOf !== null && dishIndex !== null` → `shareOf !== null` | `⛔ A8.2 · LE CAS QUI REFUSE — sans bouche nommée, pas de boîte` | ✅ |
| M16 | `"leftover"` retiré de `REALIGNMENT_ACTIONS` | **`Found 6 errors.` / `TS2345 … not assignable to '"shift_dish" \| "no_cook" \| "shift_session" \| "nothing_to_change"'`** — refus de compilation | **6, exactement** ✅ |
| **ML2 (la mienne)** | `out.push({ id: "leftover", detail: args.shareOf })` → `out.push({ id: "leftover" })` (l'action entre, mais **sans son sujet**) | `⛔ A8.2 · LE CAS QUI PASSE — une part nommée sur un plat ouvre leftover` | — ✅ |

Les deux `shareOf: null` de production sont bien là et **motivés sur place** :
`accident_tap.ts:411` (provisoire — le tap ne porte pas de `member_id`) et `accident_tap.ts:663`
(définitif — une session tombée n'a pas de plat désigné).

---

## 7. Cohérence (C4, C8) et les interdits du mandat §5.9

| ligne | vérification | verdict |
|---|---|---|
| **C4** — « un membre réclamé voit **sa** part avec **ses** cases » | `MyShareCard` lie via `bindAt` sur `householdMealId` + `userId` ; plats filtrés par `dishIsFor(d, share.memberId)` | ✅ (au code ; **pas au navigateur**, cf. §9) |
| **C4** — « le maître ne voit jamais la coche d'un profil réclamé » | **mesuré deux fois** : `protocol_events` policy `protocol_events_owner_read` = `auth.uid() = user_id` (lu en base) ; et pour la table neuve, l'assertion RLS `04b` = **0** | ✅ |
| **C8** — « un ✗ du membre écrit sa ligne et jamais celle du maître » | index unique lu en base : `protocol_events_source_message_idx ON (user_id, source_message_id) WHERE source_message_id IS NOT NULL` — deux comptes portent la même clé sans collision | ✅ (structurel ; le run réel reste ROUGE) |
| interdit — **aucune coche automatique** | aucun `tickMeal(` hors `useMealTicks` (garde de source sur les 3 surfaces) ; `resolveShareOutcomes` n'écrit rien ; D8.2 écrite en FF-058 | ✅ |
| interdit — **le maître ne corrige pas la ligne d'un membre** | la porte rend `not_your_line` (RLS 02) ; le `on conflict` porte les **4** colonnes dont `declared_by = p_user`, donc n'écrase que sa propre ligne (RLS 12b/12c) ; le modèle **ne mute pas** son entrée (épreuve `meal_share_outcome_test.ts:281`) | ✅ |
| interdit — **aucun paramètre de bouche sur `applyPlanShift`** | `accident_io.ts` **absent du diff des 4 commits** ; signature relue : `{userId, mealId, cookOn, delta, fingerprint, maxFridgeDays?, …}` — aucune bouche | ✅ |
| interdit — **aucun glissement depuis un membre** | `leftover` ne pousse **aucun** `shift_*` ; `buildRealignmentSpace` la range à côté, sans l'exclure de `shift_dish` (le maître garde le choix) | ✅ |
| interdit — **lire les faits d'un profil réclamé côté maître** | RLS `04b` = 0 sous mutation MUT-D = 1 | ✅ |

### 🔴 DÉFAUT D5 — FF-058 porte maintenant **deux R15 et deux R16**, et une citation vivante pointe sur l'ancienne

`docs/fonctionnalites/suivi-quotidien/FF-058-la-bande-du-soir.md`, table du §6 :

```
250:| **R15** ⟳ | « Pas de nouvelles » se LIT, ça ne s'écrit JAMAIS (D8.2)      ← A8.2
251:| **R16** ⟳ | Trois surfaces où le membre parle…                            ← A8.2
252:| **R15**   | La ligne de courses n'apparaît que le soir d'un `buyOn`       ← préexistante
253:| **R16**   | On écrit l'état de la vague, jamais un état par article       ← préexistante
254:| **R17**   | …
```

Les deux lignes neuves ont été **insérées** au milieu de la numérotation au lieu d'être ajoutées
après `R17`. Et « FF-058 R15 » est **cité vivant à quatre endroits**, tous sur l'ancienne R15
(la vague de courses) :

- `supabase/functions/_shared/chat/accident_tap.ts:238`
- `docs/…/FF-057-la-procedure-accident.md:190`, `:315`, `:411`

Le journal A8.2 dit que R15 est « écrite **dans le tableau de règles que P7 lira** » : P7 y
trouvera **deux** R15 et lira la première.
**Ce que le bâtisseur doit faire** : renuméroter les deux lignes neuves en **R18 / R19** (le
tableau s'arrête à R17), et corriger le renvoi de FF-058 §11 si un lot voisin l'a déjà cité.

### 🔴 DÉFAUT D6 — le modèle pur perd l'identité de la BOÎTE : il ne pourra **pas** être monté sans être modifié

`ShareOutcomeRow` = `{ memberId, declaredBy, outcome, shiftedToDay, answeredLocalDate }`.
`ResolvedShare` = `{ memberId, outcome, shiftedToDay, authority }`.
**Aucun des deux ne porte `dishIndex` ni `generatedMealId`** — alors que la clé de la table est
`(generated_meal_id, dish_index, member_id, declared_by)` : **une ligne par BOÎTE**, pas par bouche.

Et `resolveShareOutcomes` regroupe sur `row.memberId` **seul** (`byMouth.set(row.memberId, …)`).
Conséquence, sans mutation, en lisant le code : une personne qui a **deux** boîtes dans le même
plan (mardi soir et jeudi midi — la fenêtre frigo est de 3 jours, un plan va jusqu'à 7)
produit **deux lignes** en base et **une seule** `ResolvedShare` : la première gagne, la seconde
est jetée en silence.

Et l'écran que le mandat spécifie — « **boîte de {jour}**, encore au frigo » — a besoin du jour
du **plat d'origine**, que `ResolvedShare` ne porte pas et ne permet pas de retrouver.

L'en-tête du module dit pourtant : « on ne manipule que des **index** et des identifiants de
bouche ». **Il n'y a aucun index dans ce module.**

**Réponse directe à la question du mandat** : **non**, le modèle tel qu'il est ne peut pas être
monté sans modification. A8.3 devra soit ajouter `dishIndex` à `ShareOutcomeRow` **et** à
`ResolvedShare` et clé `byMouth` sur `(dishIndex, memberId)` — soit appeler
`resolveShareOutcomes` **une fois par plat**, ce que rien dans le module ne dit et qu'aucune
épreuve ne tient. Le premier est le bon : l'ordre stable et le « rendu de l'autorité » n'ont de
sens que par boîte.

---

## 8. Deux points relus avant de conclure — et qui vont bien

- **`UntickForm` est bien celui de `DishCard`, exporté** (`DishCard.tsx:617`), pas recopié.
  Il porte le **formulaire A seul** (les quatre motifs + « ignorer »), exactement comme chez le
  maître. L'« invitation photo » du mandat §5.8 ne vit **pas** sur cette surface : `writeOffPlanTapFact`
  n'existe **nulle part dans le front** (`grep -rln` = 0 hit), il est **serveur**
  (`_shared/keel/meal_tick.ts`, `accident_io.ts`, `accident_tap.ts`). Le membre reçoit donc sur
  `/app/plan` exactement ce que le maître reçoit — aucun écart introduit, et la photo reste un
  élément du **chemin du soir** (à voir en run réel).
- **Aucune clé i18n neuve, et la clé réutilisée existe des deux côtés** :
  `fr.ts:4583 "meals.tick.label": "J'ai mangé ça"` · `en.ts:6363 "meals.tick.label": "I ate this"`.
  Le bloc `chantier-0903/MEMBRE` reste à ouvrir, comme annoncé. ✅

---

## 9. 🔴 NON PROUVABLE AUJOURD'HUI — ce que l'absence de câblage et l'absence de session ferment

### 9.1 Le câblage d'écran d'A8.2 n'est pas livré (ce n'est PAS compté comme un défaut de code)

Le bâtisseur le nomme lui-même, et c'est exact. J'ajoute la mesure qui en dit la portée :

- **`meal_share_outcome.ts` n'a AUCUN importeur de runtime.** `grep -rn` sur
  `supabase/functions` + `frontend/src` : le seul fichier qui le nomme hors de son propre test est
  un **commentaire** d'`accident.ts:1145`. Le module entier est du code mort aujourd'hui.
- **La RPC `keel_household_declare_share_outcome[_for]` a ZÉRO appelant TS.**
- **`leftover` est dans la liste fermée mais inatteignable en production** : les **deux** appelants
  de `buildRealignmentSpace` passent `shareOf: null`. Seul le test passe une bouche. L'action existe,
  compile, est éprouvée — et **ne s'affichera jamais** tant qu'A8.3 n'apporte pas le `member_id`
  dans la charge du tap.

**Ce qui est par conséquent NON PROUVABLE** :

1. Que l'étape « Qui n'a pas mangé ? » **n'apparaisse qu'à un maître ayant des bouches sans
   compte** — le mandat §5.9 l'exige **sur `evening_strip_test`** ; elle n'est tenue que sur le
   modèle. `evening_strip_io` n'appelle pas `whoDidNotEatOptions`.
2. Que « tout le foyer » ⇒ `shift_dish` existant.
3. Que la proposition de boîte parte **au membre seul**.
4. Que `MyShareCard` rende « boîte de {jour}, encore au frigo ».
5. Que la déclaration écrite par la porte soit **relue** par une surface (aucune lecture n'existe :
   le seul lecteur de la table en TS est l'export RGPD et la purge).
6. Que les mots existent : **aucune clé i18n**, donc aucune parité, aucune vérification 320/1280 px,
   aucun `break-words` à mesurer.

**Et la réponse à la question posée : NON, le modèle pur ne pourra pas être monté sans être
modifié.** Voir **DÉFAUT D6** (§7) : `ShareOutcomeRow` et `ResolvedShare` ne portent pas
`dishIndex`, et `resolveShareOutcomes` regroupe sur la bouche seule — deux boîtes d'une même
personne dans un même plan s'effondrent en une, et « boîte de **{jour}** » n'est pas dérivable.

### 9.2 Ce qui exige une session (ROUGE — geste humain)

Aucun mot de passe n'a été saisi, aucun jeton forgé, `auth.sessions` jamais touchée, aucun secret
de conteneur lu. Tout ce qui suit est **derrière une garde** et reste donc **non vu** :

| # | scénario exact à jouer (fixture `qa0903m`, deux comptes, mot de passe connu de l'humain) |
|---|---|
| 1 | Maître ✓ « tout comme prévu » **et** membre ✗ « j'ai commandé », **le même soir** → **deux** lignes `protocol_events`, aucune n'écrase l'autre |
| 2 | La photo du membre s'enregistre sous **son** `user_id` |
| 3 | `/app/plan` du **membre** : une case par plat, **et seulement les siens** ; `/app/plan` du **maître** : **aucune** case sur « ce que la maison cuisine » ni dans la vue par personne |
| 4 | **320 px et 1280 px, `fr` et `en`** : la case sur sa propre ligne (`pl-4`), `document.scrollWidth === document.documentElement.clientWidth`, cible ≥ 24 px tenue par le couple case + libellé (captures **à scroll 0**) |
| 5 | La case **n'apparaît pas** sous un plat du **futur** (`isReportable`) |
| 6 | Un plan du foyer contenant **un plat sans titre en position 2** : la coche du membre sur le plat suivant écrit bien `meal_tick:<planId>:3` et **pas** `:2` — c'est le seul endroit où le risque central d'A8.1 se voit de bout en bout |

### 9.3 Ce qui exige un run réel ou une migration (hors fenêtre — le mandat me l'interdit)

- `supabase migration up` de `20260903172000`, **après** relecture du registre (tête à 18 h 40 :
  `20260903150000`, donc encore dans l'ordre).
- `keel_gdpr_lifecycle_test.ts` (les 3 cas `ignore: SKIP_INTEGRATION`), **shell jetable**.
- Le test RLS **contre la base réelle**, hors transaction d'installation.
- Les scénarios de run réel du mandat §5.9 (maître ✗ « tout le foyer », membre ✗ → boîte à jeudi) :
  **impossibles aujourd'hui, faute d'écran** — cf. §9.1.

---

## 10. Les défauts, en clair

| # | où | ce qui est faux | ce que le bâtisseur doit faire |
|---|---|---|---|
| **D1** | `frontend/src/keel/lib/useMealTicks.ts:414-418` (`bindAt`) | Le mode d'échec **exact** que `bindAt` existe pour éviter (résoudre la position par `indexOf` ⇒ `-1` ⇒ **aucune case, en silence**) est réintroductible avec **tsc 0 et vitest entièrement vert**. Les seules gardes sont des greps au **site d'appel** (`ticks.bindAt(` présent), qui restent vrais. | Extraire la décision « position + date ⇒ clé ou `null` » en fonction pure éprouvable ; ou, à défaut, une garde de source sur le **corps** de `bindAt` (interdire `indexOf` dedans, comme `myDishes[dishIndex]` l'est dans la carte). |
| **D2** | `supabase/migrations/20260903172000_…sql:64` et `:511` | Le fichier porte **son propre** `begin;`/`commit;`. La recette du journal (`begin; \i <mig> rollback;`) **applique réellement** la migration à la base locale partagée, **hors registre** — mesuré : `information_schema.tables` = **1** après. L'affirmation « vérifié après coup : 0 ligne » ne peut pas être vraie du geste décrit. (Réparé par moi : `drop table` + `drop function` ×2, base revérifiée à 0.) | Retirer `begin;`/`commit;` du fichier (le CLI enveloppe déjà), **ou** écrire au journal la recette qui marche : dépouiller les deux lignes avant d'envelopper. |
| **D3** | même fichier, contrôles (e) `:443-460` et (f) `:462-478` (les deux `when foreign_key_violation then null` en `:459` et `:477`) ; contrainte `meal_share_outcomes_day_matches_outcome` `:101-105` | L'aveu est **confirmé** : les deux CHECK neutralisés, le bloc `do $$` **passe**. Et « c'est tenu par le test RLS » est **faux** : les deux CHECK neutralisés, les **18 assertions RLS restent vertes** (la RPC intercepte avant la table). Le CHECK du **vocabulaire** est sauvé par une garde de **texte** en Deno ; le CHECK **`shifted` ⟺ `shifted_to_day`** n'est tenu **nulle part**. | Dans (e) et (f), insérer sur des uuid **réels** (fixture minimale dans le bloc, comme le fait le test RLS) au lieu d'attraper `foreign_key_violation` ; ou déplacer ces deux cas dans le test RLS, en `insert` direct sous `service_role`, **après** la porte. |
| **D4** | `supabase/functions/_shared/keel/meal_share_outcomes_rls_test.sql`, cas 11 (`:201-204`) | La 4ᵉ colonne de la clé (`declared_by`) — présentée comme **la** décision de conception — n'a **aucune épreuve qui morde** : clé ramenée à 3 colonnes ⇒ **18/18 verts**. Le cas 11 compte deux lignes sur **deux bouches différentes**, ce qu'une clé à 3 colonnes autorise tout autant. | Un cas où **la même bouche** porte deux lignes de deux auteurs : le maître déclare sur la bouche sans compte (cas 03), puis `update household_members set user_id = … where member_id = …-0003`, puis cette personne déclare — assert `count = 2` **et** deux `declared_by` distincts. |
| **D5** | `docs/fonctionnalites/suivi-quotidien/FF-058-la-bande-du-soir.md:250-253` | **Deux R15 et deux R16** dans la même table. « FF-058 R15 » est cité vivant **4 fois** (dont `_shared/chat/accident_tap.ts:238`) et désigne l'**ancienne** R15 (vague de courses). P7, à qui R15 est adressée, en trouvera deux. | Renuméroter les deux lignes neuves en **R18 / R19** (le tableau s'arrête à R17). |
| **D6** | `supabase/functions/_shared/keel/meal_share_outcome.ts:198-207` et `:240-273` | `ShareOutcomeRow` et `ResolvedShare` ne portent **ni `dishIndex` ni `generatedMealId`**, alors que la table porte **une ligne par boîte**. `resolveShareOutcomes` regroupe sur `memberId` **seul** : deux boîtes d'une même personne dans un même plan s'effondrent en une, la seconde jetée en silence. Et « boîte de **{jour}** » n'est pas dérivable de `ResolvedShare`. L'en-tête dit pourtant « on ne manipule que des **index** » — il n'y en a aucun. | Ajouter `dishIndex` à `ShareOutcomeRow` **et** à `ResolvedShare`, clé `byMouth` sur `(dishIndex, memberId)`, et une épreuve à **deux boîtes pour une même personne**. C'est un préalable au lot A8.3, pas un raffinement. |

---

## 11. Ce qui est PROUVÉ, en une liste

**A8.1**
- Périmètre du diff strictement dans le mandat §5.8 ; aucune migration ; aucun pack i18n ; aucun bump de prompt.
- `tsc -b --force` **exit 0** ; vitest **2072 verts / 4 rouges tous étrangers et prouvés antérieurs sur `31ee930f`** ; Deno domaine MEMBRE **138 passed / 0 failed**.
- `dishIndex` capturé **avant** le filtre (`household.ts:1920` vs `.filter` en `:1947`), transporté sans recalcul par `groupDishListByDay`, retrouvé par `find` et **jamais** par `myDishes[dishIndex]`, lié par **`bindAt`** et jamais par `bind`.
- **Mutation demandée jouée** (renuméroter sur la vue) → **2 rouges** dans `household.int.test.ts`. Restaurée par `cp`, `cmp` vert.
- Les six interdits d'A8.1 tenus, dont la garde double de la vue par personne du maître et la garde d'identité `if (!share) return null;` **avant** la liste.

**A8.2**
- Le bloc `do $$` passe, en transaction **réellement** annulée (après avoir dépouillé les `begin;`/`commit;` du fichier), base revérifiée à 0.
- Droits **mesurés par `has_table_privilege`** : `authenticated` = `select` seul, `anon` = rien, jumelle `_for` = `service_role` **seul**. RLS active, policy `for select` seule, `SECURITY DEFINER` + `search_path=""` sur les deux fonctions.
- Test RLS : **18/18**, avec un **foyer voisin réel** (2ᵉ foyer, 2ᵉ maître, **2ᵉ plan**) — les cas 07 et 10 ne sont donc pas vides.
- Les **trois** mutations RLS du bâtisseur reproduisent **mot pour mot** ses rouges.
- Export RGPD **cousu aux quatre coutures**, ordre positionnel du `Promise.all` **aligné**, `declared_by` comme colonne propriétaire, `deno check` 0.
- Modèle pur : **M12 (2 rouges) · M13 (3 rouges) · M14 (1 rouge)** reproduits exactement, **+ MK** (la mienne) qui mord aussi.
- `leftover` : **M15b** mord, **M16** rend **exactement 6 erreurs TS**, **+ ML2** (la mienne) mord. Les deux `shareOf: null` de production sont motivés sur place.
- Cohérence **C4** et **C8** tenues au code et **mesurées en base** (`protocol_events_owner_read`, `protocol_events_source_message_idx`). Les cinq interdits du mandat §5.9 tenus, `applyPlanShift` absent du diff.

**Note de contexte** : toutes mes mesures sont épinglées sur `e43eab06`. Pendant la vérification,
l'arbre principal est passé à `b9fa1392` (une lane voisine a fusionné) ; je n'y ai pas touché.
Après tout, **`git status --short` du worktree est vide** et la base locale est **identique à ce
que je l'ai trouvée** (0 table, 0 fonction, 0 utilisateur `a802`, tête de registre
`20260903150000`).

---

## VERDICT : 🔴 **ROUGE**

Tout ce qui est rejouable **est vert** — les suites, les sept mutations du bâtisseur, et les
quatre que j'ajoute. Mais **six défauts existent**, dont trois touchent la **preuve elle-même** :
la recette « en transaction annulée » applique en fait la migration (**D2**), un CHECK n'est tenu
**nulle part** (**D3**), la 4ᵉ colonne de la clé — la décision de conception centrale du lot —
n'a **aucune épreuve qui morde** (**D4**), et le mode d'échec exact qu'A8.1 existe pour éviter est
réintroductible en silence (**D1**). Enfin **D6** répond non à la question du mandat : le modèle
pur **ne pourra pas** être monté sans être modifié.

Les seuls ROUGE « acceptables » (session requise, run réel hors fenêtre, câblage à venir) sont
consignés au §9 et **ne portent pas** le verdict : ce sont **D1 à D6** qui le portent.
